# Gap-hunt 2026-06-29 (เดฟ · overnight) — verified findings

Owner mandate: "หาจุดที่ขาด · ยังมั่ว · ทุกฟังก์ชัน legacy ใช้ได้จริงทั้งหมดก่อน → ต่อยอด → ทุกฝ่ายใช้ง่าย · ข้อมูล/สถานะเชื่อมโยง · ประโยชน์สูงสุด."

Ran 3 read-only Explore agents (legacy-completeness · §0e money-data traps · status/data interconnection). **Every "gap" they flagged was VERIFIED against the actual code before any fix** (the repo's agents chronically over-flag). Conclusion: **the platform is in genuinely good shape — no quick safe high-value gap remains; the real remaining value is the accounting B1–B7 lane (big, multi-session).**

## Verified-NOT-a-gap (the agents over-flagged)

| Flagged | Verdict | Why |
|---|---|---|
| wallet-hs forwarder-pay doesn't call `advanceLinkedShopOrder` | **NON-ISSUE** | Forwarder→shop-order propagation is a **DB TRIGGER** (mig 0215+0216 · `AFTER INSERT/UPDATE OF fstatus,fcabinetnumber ON tb_forwarder`). Fires on ANY fstatus change regardless of code path. The JS `advanceLinkedShopOrder` callers (forwarders.ts, propagate.ts) are redundant belt-and-suspenders; the trigger is the SOT. Also: at the pay event fstatus=6, so calling it there would PREMATURELY complete the shop-order (it keys on fstatus≥3) = a bug avoided. |
| `markBillingRunPaid` doesn't flip receipt rstatus 3→1 | **STALE (fixed)** | billing-run.ts L1379–1436 already syncs tb_receipt_item + advances forwarder fstatus→6 + advance-paid. |
| cnt-hs approve doesn't advance linked forwarders | **INTENTIONAL** | cnt-hs.ts L19: "DOES NOT touch tb_forwarder · the cnt is just the supplier-payment". ค่าตู้ = COST side (Pacred pays the container supplier); forwarder fstatus = SELL/customer side. Deliberately decoupled — advancing forwarder on cnt-paid would WRONGLY mark the customer as paid. |
| interpreter-commission badge reads non-existent `commissions` table | **STALE (fixed 2026-06-28 `53edfd1d`)** | repointed to tb_withdraw_comm_interpreter_h status='2'. |
| sales_payouts / wallet_transactions / commission_* dead-writes | **STALE (tombstoned)** | guarded (throw) since 2026-06-02; sales_payouts badge repointed 2026-06-04. |

## Verified system health
- **Status/data interconnection = SOLID.** The forwarder↔shop-order link is enforced at the DB level (trigger · "แก้ทั้งระบบ" per owner 2026-06-26) so every path (scan/MOMO/manual/wallet/billing) propagates automatically. shop↔payment, container=cost (decoupled by design), billing→receipt→forwarder→credit all wired.
- **Legacy revenue-path coverage ≈ 95%** (agent 1): ฝากสั่ง/นำเข้า/โอนหยวน/ค่าตู้/wallet/billing/receipt/driver/warehouse all present + faithful. Only nicety missing = legacy `updateLock.php` 60s concurrent-edit payment lock (low value · concurrency control, not a workflow).
- **§0e money-data traps = mostly fixed/tombstoned.** No new live trap confirmed.

## The real remaining lane — accounting B1–B7 (owner: "บัญชี ชุดใหญ่")
Per `docs/research/pay-and-accounting-gap-2026-06-21.md`. Most infra EXISTS; the gaps:
- **B1** wire VAT (`computeTaxForMode`) into the AR billing-run for juristic customers (the VAT engine exists but isn't called at invoice creation). Money-critical → needs careful base + juristic-gate + no-double-charge verification.
- **B2** shop AP payout · **B3** per-order doc registry · **B4** supplier AP · **B5** bank reconcile · **B6/B7** ภพ.30 / ภงด.53 e-filing exports.
- These are multi-session + money-critical → best executed fresh-budget, not at a budget-tail.

## Round 2 (customer-flow + reachability · verified) — follow-ups, none blocking

**Reachability §0d (verified via grep):**
- `/admin/team-leaders` (sales commission config) — **TRUE ORPHAN** (0 sidebar, 0 inbound Link · URL-only). Wire into a sales/HR sidebar section (sales_admin/super). Low traffic (admin config).
- `/admin/bookings` (RFQ booking queue · Phase-3 super-only) — **TRUE ORPHAN** (0 entry). The primary freight RFQ surface is `/admin/freight/leads` (already wired); bookings is secondary. Wire into a CRM/sales section if kept.
- `/admin/refunds` (customer refund money-queue) — buried (reachable via /admin/search + /admin/wallet/withdrawals, NOT sidebar). Promote to a direct withdrawal/accounting sidebar entry (money queue · worth ≤1-click).
- `/admin/forwarder-action` (9 audit queues) — reachable via top-menu-report on report-cnt/warehouse pages; could add a direct entry.
- Wiring needs i18n labelKeys (th+en · the i18n-key-audit gate) + correct section/RBAC — fiddly · deferred (lower-value than the tax-doc lane shipped this overnight).

**Customer-flow (verified):**
- P1: forwarder payment submitted (submitForwarderPayment · actions/forwarder.ts) doesn't set a "pending admin review" signal → the row stays in รอชำระเงิน(5) with no indicator the slip was sent. Fix: a "ส่งสลิปแล้ว รอตรวจ" badge when a pending tb_wallet_hs row exists for the forwarder. Customer-facing clarity.
- P2 (minor UX): payment-due import link → list not detail · pending-receipt section · wallet balance in the pay modal · linked-imports badge on the order list card.
- All OTHER customer surfaces verified present + working (dashboard/sidebar/payment-due/receipts/wallet/refunds/shipments/addresses/billing-run).

## Lesson (recurring)
Explore agents over-flag because they search JS and miss DB-level logic (triggers) + prior fixes. ALWAYS verify a flagged gap against the actual code + the migration set before fixing — here, fixing GAP#1 blindly would have introduced a premature-completion bug. [[audit-discipline]]
