# 🧪 QA — 6 core revenue systems · full-loop tester (2026-06-01 · CEO ask)

CEO asked to prove the 6 systems work end-to-end with REAL test orders, member + admin, every button, no death-gap. This is the QA execution plan + progress. **The 6-system code fixes shipped earlier today (`685dd44b`/prod `55373300` LIVE) — see CLAUDE.md §PM-4.** This doc drives the *manual + automated QA tester* pass on top of those fixes.

## ✅ Environment PROVEN (2026-06-01 · screenshots in session transcript)
- **Chrome connected** (Claude-in-Chrome ext · tab group live).
- **Member `PR321` (วิสิฐ ศิลปเลิศลักษณ์) LOGGED IN** on `localhost:3000` — dashboard rendered: 52 ฝากสั่ง · 40 ฝากนำเข้า · 5 รอชำระ · sales-rep "เซลล์ แนท". No member-login needed.
- **dev** = latest code (`685dd44b`); **prod** `pacred.co.th` = `55373300` (same DB). QA can run on either (localhost = latest, prod = stable).
- ⚠️ **ONE browser = ONE session.** Currently = PR321 (member). `/admin/*` 404s for PR321 (correct security). To QA admin → switch login to Tadsakorn (logs out PR321).

## 🔐 Login protocol (owner-agreed · 2026-06-01)
Claude **cannot type passwords** (hard safety rule, even with owner authorization). When a login/switch is needed: Claude fills the **username** field + pauses + asks; **owner types the password + clicks login**; then Claude continues. Usernames (owner-provided for testing): member `PR321` (tel `0948782006`) · admin `0991921177`. **Passwords are NOT stored in this repo** (owner enters them live).

## QA checklist — 8 flows × member + admin (▶ = next)
| # | Flow | Member side | Admin side | Status |
|---|---|---|---|---|
| 1 | ฝากนำเข้า + ติดตามสถานะ | list + status tabs + pay-bar total | /admin/forwarders + [fNo] + status flow | ✅ **member list/status/pay-bar VERIFIED** (PR321 · 40 orders · ฿1,676.43 pay-bar) · admin TODO |
| 2 | ฝากสั่งซื้อ (shop) | search→cart→/cart→checkout | /admin/service-orders quote→pay→spawn | ▶ TODO (create a test order on PR321) |
| 3 | ฝากโอน (yuan) | /service-payment create + slip/wallet | /admin/yuan-payments approve (single 2-step NEW + bulk) | ▶ TODO |
| 4 | ออกใบเสนอราคา (quotation) | — | where? (freight-quote /admin? shop quote = adminQuoteShopOrder) | ▶ TODO — confirm the quotation surface exists/reachable |
| 5 | ชำระเงิน (payment) | pay slip / pay-from-wallet | approve slip → **fstatus 5→6 (PM-4 fix) + receipt** | ▶ TODO — verify the PM-4 mark-paid symmetry on a real test order |
| 6 | วางบิล (billing) | sees ใบแจ้งหนี้ /service-import/[fNo]/invoice | /admin/forwarders/combine-bill + print | ▶ TODO |
| 7 | ออกใบเสร็จ (receipt) | /service-import/[fNo]/receipt | /admin/accounting/receipts + print | ▶ TODO — verify auto-mint on payment-land |
| 8 | ออกใบกำกับภาษี (tax invoice) | — | tax-invoice flow (actions/tax-invoices.ts) | ▶ TODO |

**QA method:** create a TEST/demo order on PR321 (owner authorized test transactions) → walk member submit → switch to admin → approve/bill/receipt → switch back to member → confirm status advanced + receipt visible. Document each button's result (toast/nav/state). NEVER claim "works" without clicking (§0c).

## 🎨 Brand sweep (PCS Cargo → Pacred · owner GO · in progress)
Agent running this PM: switch ALL document issuer branding (receipt/tax-invoice/quotation/bill/stamp/bank/address/warehouse) from legacy PCS Cargo (taxID 0105560160694) → Pacred (taxID 0105564077716) via `components/seo/site.ts`. ⚠️ Any Pacred **bank account** / **China-Thai warehouse address** not in the codebase = left as `TODO(owner)` placeholder (don't fabricate). See agent summary.

## ⚠️ Real-payment spot-check (owner/team)
The PM-4 mark-paid fixes (slip-approve → fstatus 5→6 + receipt, across bulk + single-row + pay-on-behalf) are build-verified + money-diff-reviewed + idempotent, but NOT click-tested (would mutate a real customer's money/status). Recommend the owner/team approve 2-3 real test slips on prod + confirm: order advances 5→6 · AR cockpit decrements · receipt mints.

## Resume (fresh session — full context for the QA loop)
1. `git pull origin main` → read CLAUDE.md §PM-4 + this doc.
2. Confirm browser session (PR321 member or switch to admin per protocol).
3. Walk the checklist flows ▶ TODO, member-first then admin, on a fresh test order.
