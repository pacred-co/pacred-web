# เลน D — "และที่เหลืออยู่ทั้งหมด" (2026-07-17 · verified prod · READ-ONLY)

Owner (verbatim): *"และที่เหลืออยู่ทั้งหมด"*

เครื่องมือ: `scripts/cleanup-remaining-2026-07-17.mjs` — **SELECT อย่างเดียว · ไม่มี write** · รันซ้ำได้
`node scripts/cleanup-remaining-2026-07-17.mjs`

**ไฟล์นี้ = ตรวจ + นับ. ยังไม่ได้แก้อะไรเลยสักอย่าง · ไม่มี --apply ในเลนนี้.**

> ✅ **เลขนิ่ง** — รัน 2 รอบห่างกัน 45 วิ ได้ผลเหมือนกันเป๊ะทุกบรรทัด (`diff` = ว่าง)
> = ไม่ติดกับดัก F6a (cron เขียน `momo_box_detail` ตลอด → เลขเต้น) ที่ inventory doc เตือนไว้

---

## 0. ⚠️ doc เดิมผิด 2 ข้อจาก 4 — ห้ามใช้ต่อ

| เรื่อง | `backfill-inventory` เขียนไว้ | **ของจริงวันนี้** | |
|---|---|---|---|
| **PR043** | "เหลือ 3 แถวค้าง fstatus=4 → **แจ้งชำระผ่านคิวเดิมได้เลย**" | **ติด C2 ทั้ง 3 แถว** (ยังไม่ใส่ค่าส่งไทย) → **กดแล้วเด้ง ไม่ผ่าน** | 🔴 ผิด |
| **P22324** | "**ไม่มีแทรคนำเข้าผูกเลยสักแถว** → กฎตัดสินไม่ได้" | **มีแทรคผูก** `999061141707` → 3 กล่องในระบบ (st=6 · ตู้ GZE260701-1) · **กฎตัดสินได้ = '5' = ตรงกับที่เก็บ** | 🔴 ผิด |
| **B1 เหลือ 1 แถว** | "แถวไหน · ทำไมตกหล่น" | fid 52198 (PR086) — **ไม่ได้ตกหล่น · owner สั่ง "ปล่อยไปเลย" เอง** (commit `7c62bf41`) | 🟡 ตอบแล้ว |
| **F2 9 แถว ฿32,854** | "หาว่าอยู่ตู้ไหนได้ไหม (จาก `momo_box_detail.container_name`)" | **หาไม่ได้สักแถว** — ทั้ง 9 ไม่มีใน `momo_box_detail` เลย (ไม่ใช่ของ MOMO) | ✅ ตรง (แต่ทางที่เสนอ ตัน) |

> **ทำไม doc ถึงผิด:** ทั้ง 2 ข้อเกิดจาก **สาเหตุเดียวกัน = จับคู่แทรคแบบ exact**
> (`999061141707` ≠ `999061141707-1/3`) — คลาสเดียวกับกฎ `-1/N ≡ base เปล่า` ที่
> `momo-invoice-reconcile-ground-truth` §3 เขียนเตือนไว้เอง แต่ตรงนี้ยังไม่ได้ใช้

---

## 1. 🔴 ของใหม่ที่เจอ session นี้ — จุดบอด "MOMO แตกกล่อง" ตัดตัวเชื่อม ฝากสั่งซื้อ

### อาการ
ฝากสั่งซื้อค้าง `'4'` (รอร้านจีนจัดส่ง) ทั้งที่ของ **ถึงโกดังจีน / ขึ้นตู้ไปแล้ว** → ลูกค้าเปิดดูเห็นสถานะโกหก

### ต้นตอ (verified · file:line)
`tb_order` เก็บแทรค **เปล่า** · MOMO แตกกล่องแล้ว `tb_forwarder` เก็บเป็น **`-N/M`** → **ตัวจับคู่ทั้ง 2 ฝั่งใช้ exact match**:

| ฝั่ง | ไฟล์ | โค้ด |
|---|---|---|
| TS | `lib/admin/shop-order-arrivals.ts:92` | `.in("ftrackingchn", trackings)` |
| **SQL (SOT จริง)** | `supabase/migrations/0259_shop_status_one_rule_both_sides.sql` L81/89/185 | `btrim(f.ftrackingchn) = rs.trk` |

→ หากันไม่เจอ → กฎเห็น "ยังไม่มีของถึง" → **ค้าง '4' ตลอดกาล**

> 😬 คอมเมนต์ใน `shop-order-arrivals.ts:85` เขียนว่า *"A split shipment may have siblings;
> take the strongest signal"* — **แต่ query หา sibling ไม่เจอตั้งแต่แรก** → เจตนากับโค้ดไม่ตรงกัน
>
> = **carryover ที่ CLAUDE.md เขียนค้างไว้แล้ว** ("box-split arrival-scan miss · PR139 case")
> → session นี้ **ยืนยัน file:line + วงกระทบ + ตัวเลข** ให้ครบ (เดิมมีแต่ชื่ออาการ)

### วงกระทบวันนี้ (verified · per-shop ตามกฎจริง)

| ออเดอร์ | ลูกค้า | เก็บ | กฎที่ trigger เห็น (blind) | **กฎถ้าเห็น sibling** | ฿ | สรุป |
|---|---|---|---|---|---|---|
| **P22358** | PR549 | `'4'` | `'4'` | **`'40'`** | 4,281.96 | 🔴 **ค้างจริง** — 2 กล่อง ถึงโกดังจีนแล้ว (st=2) ยังไม่ได้เลขตู้ |
| P22324 | PR075 | `'5'` | `'4'` | `'5'` | 191,550.00 | 🟢 **ถูกแล้ว · ปลอดภัย** (ดู ↓) |

**ค้างจริงที่ต้องแก้ = 1 ออเดอร์ · ฿4,281.96**

> ⚠️ **ผมเกือบรายงานเกิน — จดไว้กันซ้ำ:** รอบแรกผมนับได้ **3 ออเดอร์ค้าง ฿56,263** เพราะรวม
> sibling ข้ามร้าน + ใช้ "ทุกกล่องมีเลขตู้" แทนกฎจริง (`done = มีเลขตู้ **OR** fstatus≥4`).
> พอ derive **ต่อ-ร้าน** ตามกฎจริง → P22349/P22339 ที่ `'4'` **ถูกแล้ว** (ยังมีร้านอื่นไม่ส่งของ =
> กฎบอก '4' อยู่ดี · จุดบอดไม่ได้ทำให้ผลต่าง). **เลขจริง = 1 ไม่ใช่ 3.**

### 🟢 P22324 ปลอดภัย เพราะ guard — ไม่ใช่เพราะโชคดี
`apply_shop_order_status` (0259 L128) มี early-exit:
```sql
IF cur IS NULL OR cur NOT IN ('3','4','40') THEN RETURN; END IF;
```
`'5'` **ไม่ถูกปกครอง** → trigger ยิงกี่รอบก็ไม่แตะ → **ถอยไม่ได้โดยโครงสร้าง**
→ ที่ owner สั่ง **"ไม่ auto-demote" = ถูกแล้ว** และ **ระบบกันไว้ให้เองอยู่แล้ว** → **ไม่ต้องเคาะอะไร ปิดเรื่องได้**

> doc เดิมเขียนว่า "กฎว่า '4' · owner เคาะ" → `'4'` ตัวนั้นคือ **ผลจากตัวจับคู่ที่ตาบอด**
> ไม่ใช่กฎที่แท้จริง. กฎจริง (เห็น sibling) = `'5'` = ตรงกับที่เก็บ → **ไม่มีอะไรให้ตัดสิน**

### ⚠️ จุดบอดยังอยู่ → จะโผล่เพิ่มเรื่อยๆ
วันนี้กระทบน้อยเพราะออเดอร์ส่วนใหญ่ **มีร้านอื่นยังไม่ส่ง** บังไว้ (กฎบอก '4' อยู่ดี) —
**พอร้านที่เหลือส่งของครบ ตัวที่ถูกบังจะกลายเป็นค้างทันที** (ตอนนั้นควรเป็น '40'/'5' แต่จะค้าง '4')

---

## 2. ของค้างที่เหลือ — ยืนยันเลขจาก prod

| # | เรื่อง | จำนวนจริง | ฿ | แตะได้เลย / owner เคาะ | เครื่องมือเดิม |
|---|---|---|---|---|---|
| 1 | **PR043 · GZS260628-2 ค้าง fstatus=4** | 3 แถว (ติด C2 ทั้ง 3) | 439.67 | 🟡 **ใส่ค่าส่งไทยก่อน** (ข้อมูลขาด ไม่ใช่บัค) | คิวแจ้งชำระเดิม `adminCallPriceUser` |
| 2 | **F2 · ไม่มีเลขตู้ แต่ st≥3** | 9 แถว (หาตู้ไม่ได้สักแถว) | 32,854.47 | 🔴 **owner/โกดังเคาะ** — ไม่มีข้อมูลให้เดา | — |
| 3 | **B1 · คูณ quantity ซ้ำ** | **1 แถว** (fid 52198) | ขาย 4,900 / ทุน 4,700 | ✅ **ปิดแล้ว — owner สั่ง "ปล่อยไปเลย"** | — |
| 4 | **P22324 สถานะ** | 1 ออเดอร์ | 191,550 | ✅ **ปิดได้ — ถูกแล้ว + guard กัน** | — |
| 5 | 🔴 **จุดบอดกล่องแตก (ใหม่)** | 1 ออเดอร์ค้าง + จุดบอดถาวร | 4,281.96 | 🔴 **owner เคาะ** (แตะกฎสถานะ = ทั้งระบบ) | ต่อยอด `shop-order-arrivals.ts` + mig 0259 |
| 6 | ราคาขาย = 0 แต่ถึงไทยแล้ว | 2 แถว | เก็บไม่ได้ | 🔴 owner เคาะ (ตั้งราคา = money) | `computeAndFillForwarderImportRate` |
| 7 | ตู้สถานะปนกัน | 3 ตู้ | 91,297.40 (ที่ค้าง) | 🟠 โกดังตรวจ | — |
| 8 | **คิวตรวจสอบ** | **8 แถว · สะอาด** (ค้าง 0 · orphan 0) | — | ✅ **gate ถือได้** | — |
| 9 | ใบวางบิล paid แต่แถวค้าง 4/5 | **0 แถว** | — | ✅ **ไม่มีปัญหา** | — |
| 10 | A1 ตั้งต้นทุน | 24 ตู้ · 303 แถว | ทุนประเมิน **348,019.92** · ขาย 476,674.93 | 🔴 owner เคาะ (ซ่อมคิวก่อน) | `momo-invoice-cost-backfill` |
| 11 | A2 จ่ายค่าตู้ | `tb_cnt`=0 · `tb_cnt_item`=0 · **44 ตู้** | ? | 🔴 **owner ตอบก่อน** | `/admin/cnt-hs` (ไม่เคยใช้) |

> ⚠️ **ห้ามบวกคอลัมน์ ฿ รวมกัน** — คนละความหมาย (ทุนที่ยังไม่ลง ≠ ขายที่ค้าง ≠ เก็บเกิน)

### หมายเหตุรายข้อ

**1 · PR043** — ทั้ง 3 แถว `fshipby='2'` (Flash) · `paymethod='1'` (ต้นทาง) · `ftransportprice=฿0`
→ gate `isThShippingCostRequired` บอก **ต้องมีค่าส่งไทย** → ยัง ฿0 = **ติด C2 ถูกต้องแล้ว**
อยู่ในคิวตรวจสอบครบ 3/3 → **ใส่ค่าส่งไทย แล้วกดแจ้งชำระได้ทันที** (ไม่ต้องแก้โค้ด)

**2 · F2** — แทรคหน้าตาไม่ใช่ของ MOMO (`X9002717` · `X9002653-1..4` = 5 แถว · PR032/PR172 ฿31,583.65)
→ **ไม่มีใน `momo_box_detail` เลยสักแถว** → ทางที่ doc เสนอ (ดึงตู้จาก `container_name`) **ใช้ไม่ได้**
อีก 4 แถว (52133/52134/52114/52125) อยู่ st=6/7 = **เก็บเงินไปแล้ว** → ไม่มีเลขตู้ก็ไม่กระทบเงิน
🔴 **5 แถว st=3 คือตัวที่ต้องรู้ว่าอยู่ตู้ไหน** — ต้องถามโกดัง/เจ้าของแทรค (ระบบไม่มีข้อมูล)

**3 · B1** — เหลือ **1 แถวเดียว** = fid 52198 (PR086 · st=6 เก็บเงินแล้ว · เก็บเกิน ฿3,920)
commit `7c62bf41` เขียนไว้ตรงๆ: *"fid 52198 (PR086) SKIP ตามที่ owner สั่ง 'ปล่อยไปเลย'"*
→ **ไม่ใช่ของตกหล่น = การตัดสินใจที่ owner เคาะแล้ว** → ปิดเรื่อง (เว้นแต่ owner เปลี่ยนใจ)

**8 · คิวตรวจสอบ** — 8 แถว · fstatus=4 ล้วน · orphan 0 → **gate ใหม่ (`'4'` เป๊ะ) ถือได้จริง ไม่รั่ว**
ในนั้น **ติด C2 = 7 · พร้อมแจ้งชำระ = 1** (fid 52137 · PCS รับเองโกดัง → ไม่ต้องมีค่าส่งไทย)
→ ✅ ตรงกับ commit `1ff60fa5` ของอีกเลน (*"ผิดพลาด 8 ติดค่าส่งไทยจริง 7 ไม่ใช่ 8"*) — **คนละเส้นทาง เลขตรงกัน**

---

## 3. 🔴 Top 3 ที่ควรทำต่อ

### 1️⃣ อุดจุดบอด "กล่องแตก" ที่ตัวจับคู่ (ต้นตอ · กันทั้ง class)
ปัญหาเดียว = ต้นตอของ **ทั้ง P22324 ที่ doc อ่านผิด + P22358 ที่ค้างจริง + ตัวที่จะโผล่ในอนาคต**
→ ทำให้ตัวจับคู่รู้จัก `-N/M` **ทั้ง 2 ฝั่งพร้อมกัน** (TS + SQL 0259 — คอมเมนต์ในไฟล์เตือนเองว่า
*"THEY MUST AGREE — แก้ข้างเดียว = เปิดบัคเดิมที่ owner เจอซ้ำๆ"*)
🔴 **owner เคาะก่อน** — แตะกฎสถานะ = กระทบทั้งระบบ · ต้องมี dry-run + เทียบก่อน/หลังทุกออเดอร์

### 2️⃣ ใส่ค่าส่งไทยให้ 7 แถวในคิว (รวม PR043 3 แถว) → เก็บเงินได้ทันที
**ไม่ต้องแก้โค้ด · ไม่ใช่บัค** = ข้อมูลที่ยังขาด · gate ทำงานถูกแล้ว
→ งานคน (CS/บัญชี) ใส่ค่าส่งไทย แล้วกดแจ้งชำระ

### 3️⃣ owner ตอบ A2: จ่ายค่าตู้ยังไง? (`tb_cnt` ว่าง 0 แถว · 44 ตู้)
ปลดล็อกข้อ 2 ของ WORK ORDER (flow อัพ PDF MOMO → ตัดจ่าย) — **ต้องรู้ก่อนว่าที่ผ่านมา
จ่ายนอกระบบ (ต้องคีย์ย้อน?) หรือยังไม่จ่ายเลย** ไม่งั้นสร้าง flow ให้ของที่ไม่มีใครใช้

---

## 4. หมายเหตุความน่าเชื่อถือ (อ่านก่อนใช้ตัวเลข)

- ✅ ทุกเลข query จาก **prod สดวันนี้** · **รัน 2 รอบห่าง 45 วิ = เลขเหมือนกันเป๊ะ** (ไม่เต้นแบบ F6a)
- ✅ **mirror gate จริง ไม่คิดเอง** — `thShipRequired`/`thShipMissing` คัดลอกกฎจาก
  `lib/forwarder/domestic-shipping.ts` (PCS รับเอง / PRF+PCSF เหมาๆ / paymethod='2' COD → ไม่ต้องมี)
  ⚠️ เป็น **mirror ไม่ใช่ import** (`.mjs` import `.ts` ตรงๆ ไม่ได้) → **กฎเปลี่ยนต้อง sync มือ**
- ✅ **ตรวจซ้อน 2 ทาง** — "คิวติด C2 = 7" ได้ตรงกับอีกเลน (`1ff60fa5`) ที่คำนวณคนละทาง
- ✅ **ตัวชี้ขาด B1 = dims เท่านั้น** (ไม่ใช้ความหนาแน่น — ของหนักเกิน 1,000 kg/คิว มีจริง)
  · qty=1 → ข้าม (คูณหรือไม่คูณก็เท่ากัน = ชี้ขาดไม่ได้) · ไม่มี dims → ข้าม (ห้ามเดา)
- ⚠️ **P22358 ยังไม่ได้ browser-verify** — ยืนยันจาก DB + อ่านกฎจาก source (§0b) เท่านั้น
  (ไม่มี test login · ไม่ได้เปิดจอลูกค้าดูว่าเห็นสถานะอะไรจริง)
- ⚠️ **F2 5 แถว st=3 = ตัดสินไม่ได้จริงๆ** → รายงานว่าไม่รู้ ดีกว่าเดาแล้วแตะเงินผิด
- ⚠️ ต้นทุนประเมิน (A1 ฿348,019.92) = คิว × เรทตามชนิดตู้ — **ตัวเลขจริงต้องมาจากใบวางบิล MOMO**
  (ใบ = ความจริง · ประเมินไว้จัดลำดับเท่านั้น) · 2 ตู้ decode ชนิดไม่ได้ → ไม่ได้ประเมิน
