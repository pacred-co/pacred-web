# Learnings — performance patterns

Topics: LCP wins, bundle size reductions, Supabase query optimizations, cache strategy successes.

> Seed file — first entry pending. Add via `.claude/skills/scholar-immortal/SKILL.md` protocol when a perf hunt lands a measured win.
