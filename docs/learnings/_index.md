# 📚 Pacred Learnings — index

> Every agent / dev / session that learns something tricky writes it here.
> Future agents read this BEFORE re-discovering. Knowledge compounds.

> **Skill that writes here:** [`.claude/skills/scholar-immortal/`](../../.claude/skills/scholar-immortal/SKILL.md)
> **Protocol:** read SKILL.md; add a dated entry to the right topic file (or create a new topic file + add to this index).

Last reviewed: 2026-06-15 (php-port-patterns +1 — **the value-overloaded fstatus column + the "port-added guard broke a faithful flow" trap**: tb_forwarder.fstatus carries physical-journey [1-4] AND money/dispatch [5-7] on ONE column; credit-grant writes fstatus='6' onto the physical axis [faithful legacy forwarder.php:1431]; legacy TOLERATED it because its arrival writers had NO from-status guard [freely re-stamped 6→4 = self-healing], but Pacred ADDED a transition matrix + .lt('fstatus','5') scan filters legacy never had → BLOCKED the arrival scan on every credit order [the juristic+credit "คนงานแสกนไม่ได้" prod fire]. Rule: before hardening a faithful flow with a guard legacy never had, ask what legacy's permissiveness was load-bearing for. Fix = RESTORE the permissiveness + drive the customer PHYSICAL timeline off fdatestatus2/3/4 stamps not the fstatus integer. + the 1%-WHT-locus discipline [grep every locus + cite lines; the juristic 1% is the single calPriceForwarderMain/calcForwarderOutstanding allowance, NOT double-deducted]. parallel-agent-sprints +1 — **"แยกร่าง" at scale [10 agents]**: worktree agents routinely END TRUNCATED/UNCOMMITTED [recover via `git -C <wt> diff | git apply` or cherry-pick the committed branch], NEVER trust an agent's "gate green" [re-gate yourself: one shipped 8 TS2339s its self-gate "passed", one dropped a §0c error-banner], scope lint to changed files when concurrent gates DEADLOCK [`node node_modules/eslint/bin/eslint.js <files>`], file-disjoint partitioning + inline-Thai [no messages/*.json contention] = clean integration, `node .bin/tsx` ≠ runnable [it's a shell wrapper]. 0183 = the only migration this session [4 partial-UNIQUE create-side double-pay backstops · applied+verified prod 4/4 + dev 4/4].) 2026-06-12 (ci-and-deploy +1 — a worktree `node_modules` JUNCTION inherits the OLD main-repo install's deps, NOT your worktree HEAD's → a false "stale lockfile" diagnosis when `tsc` can't find `ws`/`bwip-js`/`google-auth-library`; the worktree's own `pnpm-lock.yaml` was fine — drop the junction + real `pnpm install` in the worktree; `grep -c '<dep>:' lockfile` BEFORE blaming the lockfile. pacred-domain-knowledge +1 — the 3-number cargo model [SELLING/COST/DECLARED] is DATA-flowing not status-flowing: numbers exist at each node, nobody threads them → editors render empty. GAP 1 fix = the auto-fill-then-editable seed pattern [stored ALWAYS wins · seed only when stored empty · "ออโต้" chip while value==seed · persist ONLY on Save §0f · pure tested helper `cargo-cost-autofill.ts` off the money path §0e] + the SHOP `roundUp2(cprice×rate×qty)` / IMPORT `round2(fcosttotalprice×qtyShare)` seed formulas.) 2026-06-09 (synthesis — build-backlog session): **freight + customs domain mined to learnings.** Reconciled the deep-source MINE specs (PJ-BOOK freight ERP · AX BOOKING wizard · AX JOB cockpit · pricing/commission/P&L · CargoThai provider · P-BEE 3-tab tax workspace · MOMO warehouse app · call-CDR) against the current-code audit → two NEW topic files: [`freight-erp-model.md`](freight-erp-model.md) (the FREIGHT product line — PJ-BOOK 10-model→`freight_*` mapping · PRICING→SALES→DOC→ACC state machine · 3-FX rate/markup/commission/P&L model · **the `freight_quote` SINGULAR ≠ `freight_quotes` PLURAL trap** = the orphaned-RFQ dead revenue loop) + [`customs-brokerage-kit.md`](customs-brokerage-kit.md) (ใบขน workflow + 4-role RLS · DECLARED-never-equals-selling discipline · Form-E/ACFTA · D/O-LOI carrier kit · NETBAY hard-blocker). **Headline:** the freight stack is ~80% scaffolded already (quotes/shipments/invoices/payments/QA/declarations/rates all shipped + rate-engine tested) — the genuine gaps are narrow: commission ledger (4 tables · ZERO migrations) · ops cockpit Kanban · doc/plan/messenger/acc tracking tables · P&L persistence · the RFQ-leads triage bridge. Build sequence: [`docs/research/build-backlog-2026-06-09.md`](../research/build-backlog-2026-06-09.md) (12 waves · W1 freight-leads = recommended first SAFE buildable-now). **Recurring rule reinforced:** the 3-number model (SELLING/COST/DECLARED) is load-bearing for BOTH cargo + freight + customs — DECLARED is an audited manual field, never auto-equals selling. Previous: 2026-06-08 (เดฟ — full-scope gap session): **reachability** — customer surface is link-clean + legacy-complete (5-agent read-only audit vs legacy PHP: 0 404 / 0 missing legacy fn / 0 §0e dead-write / 0 money bug); EVERY orphan traced to ONE dead component `components/sections/protected-sidebar.tsx` (NOT rendered — live customer nav = `components/legacy/pcs-left-menu.tsx`; wire NEW customer features THERE, never ProtectedSidebar). **process** — platform is ~90% built; before building a "new" feature, gap-analyze current-state-vs-EXISTING-plan (plans already live in `docs/research/`) instead of re-deriving (saves tokens · 4 gap agents confirmed most CEO asks already shipped, gaps = last-mile wiring). **freight** — public RFQ `freight_quote` (SINGULAR · 0134) is orphaned: leads land in DB + LINE-ping but only consumer is a head-count; admin `/admin/freight/quotes` reads a DIFFERENT table `freight_quotes` (PLURAL · 0048) → sales can't triage inbound freight leads = the missing revenue link. **juristic-signup bug (URGENT · fixed+pushed)** — "An unexpected response was received from the server" on นิติบุคคล step-3 submit = a doc file > the Server Action `bodySizeLimit` (12mb · next.config) rejected at the PLATFORM layer BEFORE the action's own 10MB check → a cryptic THROW (not a clean returned error); root = `UploadField` (register-client.tsx) had NO client-side size guard + phone photos routinely exceed 12mb; fix = in-browser image compress (canvas · ≤2400px · q0.82 · EXIF-correct via createImageBitmap · transparency-flatten · fallback-safe) + size-guard, client-only (proven Server-Action upload path untouched). Pattern: any file-upload Server Action needs a client-side size guard ≤ bodySizeLimit or it dies with this cryptic error. **Windows build gotcha (re-confirmed the hard way)** — `pnpm build` FAILS on the Windows box: the script's inline `NODE_OPTIONS=… next build` isn't cmd.exe-compatible ("'NODE_OPTIONS' is not recognized") → `next build` never starts → the bash/notification/pipe exit is a FALSE 0 (over-claimed a green build twice before catching it). ALWAYS prod-build via `NODE_OPTIONS=--max-old-space-size=8192 node node_modules/next/dist/bin/next build` + read a real `REAL_BUILD_EXIT=` echo. Roadmap: `docs/research/full-scope-gap-2026-06-08.md`. Previous: 2026-06-04 evening (ภูม session) — partner-apis-quirks +2 entries [MOMO `user_code` = legacy `tb_users.ID` integer PK zero-padded · NOT string userID; lookup-by-phone resolves PR023→PR1395 + PR99→PR089 via legacy SQL dump `(ID, userID, userTel, ...)` block at line 1261140+], [MOMO `raw.images[]` is GROUND TRUTH — เปิดดู URL ก่อนเชื่อ field อื่นใน raw; ภูม discovered MOMO operator typed "023" but the physical label shown in raw.images[0] reads "PR025"; Pacred review-grid now has thumbnail + multi-image lightbox `<ZoomLightbox>` at `/admin/api-forwarder-momo/review`]. php-port-patterns +2 entries [per-shop array loop pattern — legacy `update3.php` loops `$_POST['cNameShop'][]` array, UPDATE per cnameshop WHERE hno+cnameshop; port = Zod array arg + backend for-loop + `<ShopFieldsBoard>` UI dedup by cnameshop. `name="...[]"` in legacy HTML = backend loops], [status-aware conditional UI — legacy uses separate `update<N>.php` files per status; port = one React component + `isStatus3` / `isStatus4` flags · conditional render input enabled/disabled]. Save-point: `docs/research/poom-save-point-2026-06-04-evening.md`. Previous: 2026-05-30 night (ci-and-deploy +1 — after a multi-agent merge, run the FULL `.github/workflows/ci.yml` gate sequence locally before pushing; per-file `eslint <files>` + per-file unit tests are NOT enough — a cross-file TS error in a pre-existing function + a repo-global env-audit failure (`MOMO_SYNC_PROPAGATE_STATUS` used-but-undeclared) each slipped to CI as two consecutive red runs. Literal sequence: `pnpm lint` (0 errors) → `pnpm typecheck` (heap-bumped `scripts/tsc-check.mjs`, NOT bare `tsc` which OOMs) → `pnpm test:unit` → `pnpm audit:all` → `pnpm build` (locally needs `NODE_OPTIONS=--max-old-space-size=8192`; local default-heap OOM at static-gen worker is NOT a Vercel failure signal). env-audit exits 1 ONLY on used-but-undeclared — the "Declared but UNUSED" list is a non-failing warning.) 2026-05-30 evening (php-port-patterns +1 — legacy PHP `NULL` string-interpolation = empty string, NOT Postgres NULL; ภูม MOMO commit failed with `fusercompany NOT NULL violation` because Pacred translated legacy `$fUserCompany=NULL` literally to JS `null` but legacy effectively wrote `''` via `'$fUserCompany'` interpolation; verified prod tb_forwarder for company customers all show `fusercompany=""`. verify-deep-flow +1 — the "silent dead-write" pattern: admin actions target rebuilt empty tables instead of `tb_*` where the 21,950 real orders live; toast shows success but data goes nowhere; #1 recurring bug across 5-system fidelity audit; found in 7 surfaces (yuan-payments/service-orders/forwarders); fix protocol = pivot action to tb_*, backfill orphan rows, delete dead duplicate file. nextjs-16-quirks +1 — `react-hooks/purity` rejects raw `Date.now()` / `new Date()` in render bodies, even on Server Components; wrap in named module-scope helper functions like `nowMs()` / `nowIso90dAgo()`. partner-apis-quirks +1 — MOMO `container_no` is their INTERNAL routing batch ID (e.g. `PR20260527-SEA02`), NOT a real PCS cabinet; real cabinet lives on `momo_container_closed.raw.cid` (e.g. `GZS260529-1`); propagation must NEVER write routing batch to fcabinetnumber and must replace stale routing batches with real cabinets when known.) · 2026-05-28 ดึก (ci-and-deploy +1 — CI `tsc --noEmit` OOMs at the default 2 GB heap once Wave 25 + batch 2a + ปอน's MOMO isolated layer landed; fix is `scripts/tsc-check.mjs` wrapper that sets `NODE_OPTIONS=--max-old-space-size=8192` cross-platform, used by `pnpm typecheck` + CI + verify. Build step also needs the env bump because `next build` runs tsc internally. Don't shard tsconfigs — the heap bump is the right answer.) 2026-05-28 evening (consolidated — เดฟ + ภูม + ปอน after surgical merge of wave-25 #194-#196 + ปอน's c6ca71fb LCL restore): nextjs-16-quirks +3 — (a) `<Link>` to a protected route from a non-protected page leaks the protected layout's `CSS_BUNDLE` as `<link rel="preload">` on the current page (React 19 hoists the stylesheets from the prefetch RSC payload to the active head); fingerprint = ~126 "preloaded but not used" warnings on `/register` / `/login` / `/complete-profile` for every URL in `app/[locale]/(protected)/layout.tsx CSS_BUNDLE` even when those pages never render that layout. Fix is `usePathname()`-driven `prefetch={false}` on the NavBar / CartBadge / NotificationBell / UserMenu Links when outside `(protected)`. (b) `"use server"` files reject EVERY non-async-function value export — Zod-object exports crash routes at request-time even though build + tsc + curl smoke all pass; demote `export const schema = z.object()` → bare `const`; type-only re-exports are safe. (c) `next/script strategy="afterInteractive"` does NOT preserve order across multiple sibling `<Script>` tags; the smaller files in the chain race ahead and execute before jQuery (`vendors.min.js` 537 KB) has loaded, fingerprint = `$ is not defined` + `jQuery is not defined` + `a is not a function` in 5 different legacy files simultaneously; fix is a single inline loader that creates `<script>` elements with `async = false` (the HTML "in-order" list pattern jQuery's own CDN loader uses)). 2026-05-26 night (ci-and-deploy +2 — CSP must list every legacy external CSS/font/script origin or every protected-page console floods on prod (`fonts.googleapis.com` style + `fonts.gstatic.com` font + `cdnjs.cloudflare.com` both · `translate.google.com` script from legacy `tam-it.js`); deleting a route doesn't remove its `<Link href="…">` — Next prefetch surfaces it as `/deleted-route?_rsc=… 404` on every render (after delete, grep `href="/X"` + `messages/*.json`). php-port-patterns +1 — `convertIMGCHN` port copied the legacy nested `/images/shops/` path but the static-mount stager only copied `/shops/` — broken-image icon was invisible under noisy Bootstrap markup, surfaced when ปอน's Tailwind rebuild made cards clean; pre-existing bug ≠ regression. parallel-agent-sprints +1 — cherry-pick over merge when the source branch is BEHIND: a blind `git merge origin/podeng` would have reverted 7 production files because podeng was stale on backend work; always `git log <merge-base>..source` first, the diff-stat LIES.) 2026-05-26 (nextjs-16-quirks +2 — Sprint-25 chrome leak: marketing `<FloatingTabs />` mounted at `[locale]/layout.tsx` rendered on every protected/admin/auth page because route groups don't isolate parent chrome; fix is mount at `(public)/layout.tsx` instead, and `grep -rn 'from "@/components/sections/footer"' app/[locale]` returning anything outside `(public)/*` is a leak. Sprint-26 mobile: legacy DataTables-Responsive plugin shipped CSS in 5 stylesheets that goes silently dead when the jQuery isn't loaded — `<th class="none">` ignored, `.tr1::after { content: "...คลิกดูเพิ่มเติม" }` is a tap-to-expand promise with no handler; pure-CSS emulation in `legacy-overrides.css §11` + per-page `.pcs-<page>-page` modifier wrappers because CSS can't reach a `td` from its `th`). 2026-05-19 (pacred-domain-knowledge +1 + php-port-patterns +1 — ภูม's legacy PCS Cargo system research captured: 4 verbatim files copied to `docs/research/pcs-legacy/`, the durable business logic synthesized — 3 revenue services + `tb_*` schema + `sStatus`/`fStatus`/`wType` enums + chargeable-weight & service-fee formulas + VIP-credit & agent-commission rules + the forwarder pay-at-status-5 post-arrival COD inversion; port-mechanics facts: legacy status columns are numeric VARCHAR strings, the forwarder uses per-status DATETIME columns not a history table, `tb_wallet.wBalance` is a stored running balance). 2026-05-19 (php-port-patterns +1 — MySQL→PostgreSQL via pgloader: connects to MySQL 9.x with no `caching_sha2` workaround; one CAST rule kills the zero-date NOT-NULL trap; MySQL collation is case-INSENSITIVE so join-key columns must be case-normalised in the port; `notnull`/`isnull` are PG postfix operators not bare aliases; a Supabase data load needs the Postgres DB password — REST API keys can't run DDL/COPY). 2026-05-18 (php-port-patterns +1 — MySQL→PostgreSQL data-migration gotchas: NUL bytes in legacy `varchar` break a Postgres `COPY`; legacy `datetime NOT NULL` columns hold `0000-00-00` so temporal columns must port NULLABLE; convert via a live local MySQL not dump-text-munging. 2026-05-17 — nextjs-16-quirks +1 — a `server-only` module reaching a Client Component transitively breaks `next build` but passes `tsc`/`verify`; ci-and-deploy-gotchas +1 — worktree smoke needs `.env.local` + rebuild; supabase-rls-patterns +1 — `wallet.balance` is pending-blind, spend gates must use the available-balance helper; ci-and-deploy-gotchas +1 — a "bug on branch `dave`" can be a stale-worktree phantom, verify against the live `dave` worktree; ci-and-deploy-gotchas +1 — worktree-isolation agents you spawn branch from `origin/main`, brief them to resync to `dave`; ci-and-deploy-gotchas +1 — a `next start` + curl smoke does NOT detect a dead database, public pages degrade + protected pages redirect before the DB call)
2026-05-26 night (ci-and-deploy +2 — CSP must list every legacy external CSS/font/script origin or every protected-page console floods on prod (`fonts.googleapis.com` style + `fonts.gstatic.com` font + `cdnjs.cloudflare.com` both · `translate.google.com` script from legacy `tam-it.js`); deleting a route doesn't remove its `<Link href="…">` — Next prefetch surfaces it as `/deleted-route?_rsc=… 404` on every render (after delete, grep `href="/X"` + `messages/*.json`). php-port-patterns +1 — `convertIMGCHN` port copied the legacy nested `/images/shops/` path but the static-mount stager only copied `/shops/` — broken-image icon was invisible under noisy Bootstrap markup, surfaced when ปอน's Tailwind rebuild made cards clean; pre-existing bug ≠ regression. parallel-agent-sprints +1 — cherry-pick over merge when the source branch is BEHIND: a blind `git merge origin/podeng` would have reverted 7 production files because podeng was stale on backend work; always `git log <merge-base>..source` first, the diff-stat LIES.) 2026-05-26 (nextjs-16-quirks +2 — Sprint-25 chrome leak: marketing `<FloatingTabs />` mounted at `[locale]/layout.tsx` rendered on every protected/admin/auth page because route groups don't isolate parent chrome; fix is mount at `(public)/layout.tsx` instead, and `grep -rn 'from "@/components/sections/footer"' app/[locale]` returning anything outside `(public)/*` is a leak. Sprint-26 mobile: legacy DataTables-Responsive plugin shipped CSS in 5 stylesheets that goes silently dead when the jQuery isn't loaded — `<th class="none">` ignored, `.tr1::after { content: "...คลิกดูเพิ่มเติม" }` is a tap-to-expand promise with no handler; pure-CSS emulation in `legacy-overrides.css §11` + per-page `.pcs-<page>-page` modifier wrappers because CSS can't reach a `td` from its `th`). 2026-05-19 (pacred-domain-knowledge +1 + php-port-patterns +1 — ภูม's legacy PCS Cargo system research captured: 4 verbatim files copied to `docs/research/pcs-legacy/`, the durable business logic synthesized — 3 revenue services + `tb_*` schema + `sStatus`/`fStatus`/`wType` enums + chargeable-weight & service-fee formulas + VIP-credit & agent-commission rules + the forwarder pay-at-status-5 post-arrival COD inversion; port-mechanics facts: legacy status columns are numeric VARCHAR strings, the forwarder uses per-status DATETIME columns not a history table, `tb_wallet.wBalance` is a stored running balance). 2026-05-19 (php-port-patterns +1 — MySQL→PostgreSQL via pgloader: connects to MySQL 9.x with no `caching_sha2` workaround; one CAST rule kills the zero-date NOT-NULL trap; MySQL collation is case-INSENSITIVE so join-key columns must be case-normalised in the port; `notnull`/`isnull` are PG postfix operators not bare aliases; a Supabase data load needs the Postgres DB password — REST API keys can't run DDL/COPY). 2026-05-18 (php-port-patterns +1 — MySQL→PostgreSQL data-migration gotchas: NUL bytes in legacy `varchar` break a Postgres `COPY`; legacy `datetime NOT NULL` columns hold `0000-00-00` so temporal columns must port NULLABLE; convert via a live local MySQL not dump-text-munging. 2026-05-17 — nextjs-16-quirks +1 — a `server-only` module reaching a Client Component transitively breaks `next build` but passes `tsc`/`verify`; ci-and-deploy-gotchas +1 — worktree smoke needs `.env.local` + rebuild; supabase-rls-patterns +1 — `wallet.balance` is pending-blind, spend gates must use the available-balance helper; ci-and-deploy-gotchas +1 — a "bug on branch `dave`" can be a stale-worktree phantom, verify against the live `dave` worktree; ci-and-deploy-gotchas +1 — worktree-isolation agents you spawn branch from `origin/main`, brief them to resync to `dave`; ci-and-deploy-gotchas +1 — a `next start` + curl smoke does NOT detect a dead database, public pages degrade + protected pages redirect before the DB call). php-port-patterns +1 — schema casing drift: tb_cnt* tables use camelCase quoted columns (`"cntID"`, `"nameBlank"`) but action code writes lowercase keys — PostgREST fuzzy-matches via REST but raw SQL/RPC future will break. verify-deep-flow +1 — round-2 case study: cnt-payment "ขออภัย เกิดข้อผิดพลาด" surfaced ONLY at submit click, never at curl/build/tsc · hardened protocol added: report wave completion as action-buttons-clicked not files-touched.

---

## Topic files

| Topic | What's captured | Last entry |
|---|---|---|
| [`money-audit-and-doc-fidelity-2026-06-25.md`](money-audit-and-doc-fidelity-2026-06-25.md) | **L1** money-audit blast-radius (verify vs prod · per-row `fusercompany` ≠ per-buyer `is_juristic` → "12 invoices double-deduct" was 0 real) · **L2** perception bug (cost-pay ≠ status; MOMO cron IS the status source) · **L3** doc signatory (`DOC_SIGNATORY` in site.ts · fixed authorized name not issuing-admin uuid · `safeLegacyAdminId(id,30)` is a uuid PREFIX → `id LIKE clipped\|\|'%'` recovers the profile) · **L4** display-only rebrand (keep stored `PCSF`/`PCSE` code+logic · only labels → PRF/PRE · update the `nameShipBy` test in the same change) · **L5** integration gate caught rebrand-broke-test + md-link `(admin)`-parens-break-parser · apply a teammate's migration before push→main | 2026-06-25 |
| [`ci-and-deploy-gotchas.md`](ci-and-deploy-gotchas.md) | GitHub Actions · Vercel · pnpm · env-audit · git pathspec literal-brackets · Node fetch timeouts (IPv4-first DNS) · build/verify green ≠ prod (next-start smoke gate) · worktree smoke needs `.env.local` copy + rebuild · stale-worktree phantom bugs (verify vs the live `dave` worktree) · spawned worktree-isolation agents branch from `origin/main` not your working branch · a next-start+curl smoke can't detect a dead database · Vercel may flip dpl_id TWICE per push · Audit doc descriptions can misname legacy files · `googleapis` package crashes Next 16 tsc with heap-OOM (use `google-auth-library` + raw fetch) · Supabase prod pg pooler URL returns "tenant not found"; use the direct `db.<ref>.supabase.co:5432` URL for one-off migrations · DBD CKAN behind Incapsula WAF returns HTML body on 200 (split JSON parse out of fetch try, route 404 on parse fail) · camelCase column-rename migration on a live DB needs lockstep deploy + manual codebase rewrite (regex codemod overshoots Zod schemas + local vars) · PG RENAME COLUMN does NOT reach PL/pgSQL function bodies — every CREATE OR REPLACE FUNCTION that references a renamed column must be re-declared IN THE SAME MIGRATION with quoted `"camelCase"` identifiers (the 0113 pilot broke signup until 0114 hotfixed `generate_member_code()`) · **the shared `:3000` dev server runs from the MAIN checkout not your worktree → edits don't show until you push + `git -C <main> pull` (+ it can be dozens of commits behind); `setsid` ≠ macOS (use `nohup … &` to detach a restarted dev server); stale `.next` serves content not on disk after a big pull → kill + `rm -rf .next` + restart; a long localhost→prod-Supabase session's stale refresh-token fires a transient `Failed to fetch` burst that self-clears on restart — not a code bug** | 2026-06-05 |
| [`migration-env-drift.md`](migration-env-drift.md) | **Per-env hand-applied migrations drift** (no migrate-on-deploy / no ledger table). `git pull` moves CODE not SCHEMA — verify a teammate's new migration is APPLIED to prod before promoting (the mig-0175 prod-gap: receipt-gate code went to main, schema didn't → prod code-ahead-of-schema). A dev/shared env can be NON-CONTIGUOUSLY migrated → reconcile with `scripts/reconcile-migrations.mjs` (per-migration txn · COMMIT missing/idempotent-noop · skip benign already-exists · flag real errors · `--skip` seed-INSERT migrations to avoid dup). To verify "applied", read the REAL object name from the file — don't guess (0154=`customer_tag` not `tb_`; 0158 cost_unit_thb on `tb_forwarder_item` not `tb_order`; 0167=`freight_commission_tiers` not `_ledger`). 2026-06-11: reconciled DEV `lozntlidlqqzzcaathnm` → matches PROD. | 2026-06-11 |
| [`supabase-auth-rotation-and-realtime.md`](supabase-auth-rotation-and-realtime.md) | **Random mid-session logout = Supabase refresh-token ROTATION race** (getSession + getClaims-no-arg both `_callRefreshToken` within EXPIRY_MARGIN_MS, NO autoRefreshToken gate — verified in auth-js source; concurrent prefetch RSC renders race the one-time-use refresh token, RSC can't persist the rotated cookie → the obvious "read token via getSession, pass to getClaims(token)" fix is a verified NO-OP; durable fix = Supabase Dashboard "refresh token reuse interval" SETTING ~10 s, not code; refresh owned solely by `proxy.ts`) · **stale-data-after-save = `unstable_cache` needs `revalidateTag` not `revalidatePath`** (codebase had 744 revalidatePath / 0 revalidateTag → 60 s stale badges; `lib/cache/revalidate-chrome.ts` `bustCustomerChrome`/`bustAdminChrome` into 29 actions; Next-16 `revalidateTag(tag, {expire:0})` is 2-arg now) · emoji as `next/image` src → render as text · platform_incidents triage (most "X is not defined" = old captures of since-fixed code; store re-captures so bulk-close is safe; resolved needs acknowledged_at+assigned_to per 0077 CHECK) | 2026-06-06 |
| [`freight-erp-model.md`](freight-erp-model.md) | **The FREIGHT product line (AXELRA side · the B2B customs-clearance/transport business).** PJ-BOOK 10-model Prisma schema → Pacred `freight_*` mapping (most of the core ALREADY SHIPPED — quotes 0048 · shipments 0050 · invoices 0051 · payments 0052 · QA 0045 · declarations 0057 · rates 0145 · periods 0056); the **PRICING→SALES→DOC→ACC** 4-stage Kanban state machine; the **3-FX-rate model** (selling/cost-China/customs-USD · never one) + markup tiers 30/25/20/15/10% + ≤15k฿/container cap (advisory) + commission split 1%+5%+1%−3%WHT; the **`freight_quote` SINGULAR (public RFQ · 0134) ≠ `freight_quotes` PLURAL (B2B quotation · 0048) trap** = the orphaned-lead dead revenue loop + the `convertLeadToQuote` bridge; the AX BOOKING wizard internals (air volumetric ×167 · customer-safe SELL-only output); DONE-vs-MISSING reconciliation (gaps = commission ledger · ops cockpit · doc/plan/messenger/acc tables · P&L persistence · export reverse-flow) | 2026-06-09 |
| [`customs-brokerage-kit.md`](customs-brokerage-kit.md) | **Customs brokerage doc layer (the high-margin moat).** Document taxonomy (ใบกำกับ ✅ · ใบขน ✅schema · CI/PL ✅ · Form-E ✅draft · D/O-letter ✅ · D/O-LOI ❌); the ใบขน **4-role workflow** (CS→Pricing→Docs→Account · status draft→submitted→accepted→released · 0057) + RLS (`super`/`accounting`/`freight_export_doc`/`freight_import_doc` · customer reads own ≥submitted); the **DECLARED-value discipline** (มูลค่าสำแดง is AUDITED + edited-down-from-cost · NEVER auto-equals selling · super+accounting edit only); **Form-E/ACFTA** (no eligibility engine yet · 12-box C/O draft); the **D/O-LOI carrier kit** (ZIM/RCL/COSCO/HEDE/FUJIT/UPS + 45-day waiver/POA/amend + port-code master · NOT built · Phase C); **NETBAY** hard owner-blocker (real customs gateway `api.netbay.co.th` · creds + field-spec needed · manual filing until then); the cheapest build = wire `DeclarationDetailClient` into `/admin/accounting/customs-declarations/[id]` | 2026-06-09 |
| [`pacred-cargo-tax-invoice-flow.md`](pacred-cargo-tax-invoice-flow.md) | **CARGO ใบกำกับภาษี = the 3-number model + 4-role flow** (the build PCS+ไอแต้ม failed at). CARGO import = Freight-LCL with a consolidated ใบขนรวม under the shipping-co name → customer sees only the tax invoice. THREE distinct prices, three roles: **SELLING** (CS→invoice+VAT7%) · **COST** (Pricing→PEAK stock+profit) · **มูลค่าสำแดง/declared** (Docs→ใบขน, defaults to cost but engineered-down separately). The recurring bug = conflating them (the template declared the ex-duty SELLING price to customs because there was no cost field). Flow CS→Pricing(NEW)→Docs(NETBAY/FormE)→Account(PEAK). Constants: markup 30/25/20/15/10% · Form E/RCEP→0% · 1CBM=300KG · GZE/GZS codes. Built form `/Users/dev/Downloads/PACRED-ใบกำกับภาษี-form-v2-pricing.xlsx` (verify formulas w/ `pip install formulas`, no LibreOffice). Spec for the in-platform feature (extends ADR-0027 tb_forwarder_tax_invoice with cost + declared fields) | 2026-06-09 |
| [`duplicate-identity-cross-system.md`](duplicate-identity-cross-system.md) | **One person, two member codes** — the PR112/PR10584 dup. `profiles` (auth/admin · member_code) and legacy `tb_users` (migrated customers · userID) run side by side; join contract is one code per person. Customer-create paths dedupe phone (`findLegacyUserIdByPhone` → `phone_exists:`), but `adminCreateNew` did NOT → provisioning an admin for an existing (often cold/migrated) customer minted a 2nd code. Fix: cross-system phone guard in `adminCreateNew` + `allow_existing_phone` confirm-override on the create form; detection tool `scripts/find-cross-system-phone-dups.mjs` (37 flagged — review backlog, not auto-merged); merge template retires empty dups via reversible `userStatus='0'` (dry-run + backup first). Rule: normalize phones (`+66…`==`0…`, last 9 digits); any identity-minting path dedupes BOTH tables | 2026-06-08 |
| [`member-sidebar-contact-and-i18n-traps.md`](member-sidebar-contact-and-i18n-traps.md) | "Sales/CS photos don't show + wrong name" = **routing + fallback data, not the component** (958/1000 customers route to `admin_center` whose `adminPicture="user.jpg"` → fallback; 100% empty `adminIDCS` → CS fallback; both fallbacks pointed at the wide `pacred-logo-red.png` which `object-cover` crops into the 46px circle as a broken blob — fix = square face photos + rename "แนท"→"ส่วนกลาง" since that line is the central number; query the actual `adminIDSale`/`adminIDCS` distribution before touching an avatar component, the *fallback* is what 95% see) · Dashboard showing literal `Namespace.key` = **double-nested JSON** after a merge (`{"X":{"X":{...}}}` + `getTranslations("X")`+`t("key")` → next-intl echoes the path; grep, un-nest, verify via JSON round-trip) | 2026-06-06 |
| [`nextjs-16-quirks.md`](nextjs-16-quirks.md) | Next 16 + JSX gotchas (JSDoc `*/` · unescaped-entities · Zod UUID v4 · Turbopack route-cache · theme desync · generateStaticParams+auth → DYNAMIC_SERVER_USAGE · server-only transitive import → Turbopack build fail · Server Action cookie write → layout revalidation kicks mid-flow user out · NEXT_SERVER_ACTIONS_ENCRYPTION_KEY unset → every deploy breaks active customer tabs silently · Server Action default bodySizeLimit 1MB rejects file uploads at platform layer SILENTLY (empty-table fingerprint diagnostic) · Multi-step signup resume via async Server-Component wrapper, NOT client useEffect · Route-group chrome leak: components in `[locale]/layout.tsx` render on every route — gate to `(group)/layout.tsx` · Legacy DataTables-Responsive plugin's CSS without its JS = dead promises (`th.none` ignored, `.tr1::after` hint with no handler) — pure-CSS emulation in `legacy-overrides.css §11` · `export { x } from "./other"` inside a `"use server"` file breaks Next 16's Server-Action AST walker — wrap in local `async function` instead · multiple `<Script strategy="afterInteractive">` siblings DO NOT preserve order — the smaller files race ahead of jQuery's 537 KB `vendors.min.js` and hit `$ is not defined`; fix is one inline loader that creates `<script>` with `async = false` per HTML in-order list spec · `<Link>` to a protected route from a non-protected page leaks the protected layout's CSS bundle as `<link rel="preload">` on the current page — React 19 hoists every CSS URL in the prefetch RSC payload to the active head; fix is `usePathname()`-driven `prefetch={false}` for protected-target Links when outside `(protected)` · jQuery-initialised Slick carousel FOUC "บวม → ย่อ" on every load — pre-init all slides render stacked until client jQuery collapses them; fix = standard `:not(.slick-initialized)` anti-FOUC CSS in blocking `legacy-overrides.css` · broken-image icon "in many places" = a STATIC asset wired in code but never placed in `public/` — the code is right, the FILE is missing) | 2026-06-09 |
| [`supabase-rls-patterns.md`](supabase-rls-patterns.md) | RLS patterns + admin-client-after-ownership-verify for customer mutations · check-then-act money-race → DB unique index · `wallet.balance` is pending-blind → spend gates use `getWalletAvailableBalance` | 2026-05-17 |
| [`i18n-pitfalls.md`](i18n-pitfalls.md) | next-intl / messages/*.json gotchas | 2026-05-15 |
| [`perf-patterns.md`](perf-patterns.md) | Performance wins + measurement | (empty seed) |
| [`testing-patterns.md`](testing-patterns.md) | Pacred-specific test mocks + harness quirks | 2026-05-16 |
| [`momo-arrival-status-propagation.md`](momo-arrival-status-propagation.md) | **MOMO ingest→propagate→customer-status chain + the 3 breaks** (P0 2026-06-16 "ของถึงไทย/จีน แต่สถานะค้าง · เก็บเงินไม่ได้"). The arrival flag rides a CONTAINER record (trackingNo:null) → never reaches the per-PARCEL propagator → fix = stamp `shipment_status='AT_WAREHOUSE_TH'` onto the matched `momo_import_tracks` rows in the existing sync.ts step-2.5 walk · the ฿0 bill = per-parcel kg/cbm=0 at arrival → fall back to container `total_kg`/`total_cbm` (keep the `live-rate.ts:310` never-bill-฿0 guard) · the shop order had NO "ถึงโกดังจีน" status → added hstatus `'40'` (migration 0185 widens varchar(1)→(2); '4'<'40'<'5' string-orderable; SOT = `lib/legacy-status-map.ts` + explicit display-order since Object.keys sorts '40' to the end; advance in commit-momo-row-core.ts step-6b, forward-only 4→40, best-effort) · the "disappeared label" trap (sweep ALL ~16 1..6 label/badge/step maps + use explicit rank not `Number()`) · **Option-B (manual-review, fstatus=4) vs Option-A (auto-collect, fstatus=5 + pay-now notify)** + the exact criteria to switch (price≠฿0 always · dry-run statusAdvanceSkippedByGate · notify verified) + the `MOMO_SYNC_PROPAGATE_STATUS` env gate the integrator owns | 2026-06-16 |
| [`partner-apis-quirks.md`](partner-apis-quirks.md) | MOMO JMF / TAM / ThaiBulkSMS / LINE behavior · DBD juristic lookup · register-client was bypassing the working `/api/dbd/[taxId]` route handler (with WAF-bypass User-Agent) and calling the dead `opendata.dbd.go.th/api/v1/*` direct from the browser — switched 2026-05-27 (the real "Gap #5 TAMIT" fix; gap was mislabelled — `regis-tam.php` is the Thai juristic 3-step signup, not Thai-ID verification) | 2026-05-27 |
| [`migrated-customer-auth-identity.md`](migrated-customer-auth-identity.md) | The PR050 fire (2026-06-24) — migrated PCS customer login/identity, 4 traps + durable rules: (1) password reset MUST sync `tb_users."userPass"` via `syncLegacyUserPass` (phone-login → bridge checks the legacy hash; Auth-only resets are inert); (2) the bridge resolves the EXISTING auth user via `profiles.member_code` — NEVER re-derive the synthetic email + `createUser` (mig 0103 padding PR50→PR050 broke the match → duplicate auth users + empty profiles + forced /complete-profile + leaked synthetic email); (3) migrated customers = `status='active'`, never force re-profile; (4) never display `@users.pacred.invalid`. + 4 invariant audit queries (locked-out / orphan / email-mismatch / incomplete = all 0). | 2026-06-24 |
| [`php-port-patterns.md`](php-port-patterns.md) | Legacy `pcscargo` port gotchas · MySQL→PostgreSQL data migration (NUL bytes break COPY · zero-dates vs NOT NULL · live-DB convert · pgloader path · MySQL case-insensitive collation → normalise join keys · `notnull` is a PG operator · Supabase load needs the DB password) · legacy PCS `tb_*` schema reference (numeric-VARCHAR status cols · per-status DATETIME columns · stored running wallet balance) · helper-port path mismatch: verbatim transcription of `convertIMGCHN` kept the nested `/images/shops/` path but the static-mount stager only copied `/shops/` — broken-image icon stays invisible under legacy Bootstrap markup, surfaces when teammate's Tailwind rebuild cleans cards (pre-existing ≠ regression) | 2026-05-26 |
| [`pacred-domain-knowledge.md`](pacred-domain-knowledge.md) | Cargo flow + MOMO 9-status enum + containers schema coexistence + decoded cargo/freight ops model (GZE/GZS · type taxonomy · CBM mismatch · Form E / D-O) · **legacy PCS Cargo full decode** (3 revenue services · `tb_*` schema · `sStatus`/`fStatus`/`wType` enums · chargeable-weight & fee formulas · VIP-credit & agent-commission rules · forwarder pay-at-status-5 COD) | 2026-05-19 |
| [`parallel-agent-sprints.md`](parallel-agent-sprints.md) | 4 patterns from Sprint-9..15 retrospective — pre-audit before spawn · stale-doc cost · migration-number collision recovery · worktree-leak prompt prevention · cherry-pick over merge when source branch is BEHIND · ปอน refreshes brand-asset image FILES in-place (same filename, new content) — NEVER auto-rewrite her image src paths without checking podeng first; binary `M`-marks with byte-count delta = silent material change · L-PAS-07/08 deep-source-build harness + 4-wave integration traps · **L-PAS-09** a worktree-isolation agent that hits the session limit mid-run loses its ENTIRE worktree if uncommitted → commit incrementally · **L-PAS-10** `next build` in the same worktree as a running `next dev` corrupts the shared `.next` + kills the dev server | 2026-06-10 |

---

## Related research (full evidence, not learnings)

Learnings are *synthesized* — for the raw source material a synthesis draws from:

| Research | What it is |
|---|---|
| [`docs/research/pcs-legacy/`](../research/pcs-legacy/_index.md) | ภูม's 4-file verbatim decode of the legacy **PCS Cargo** PHP system (business flow · full `tb_*` schema · staff guidebook · dev docs) — the SOT for the D1 1:1 port. Synthesized into `pacred-domain-knowledge.md` (2026-05-19) + `php-port-patterns.md`. |
| [`docs/research/`](../research/_index.md) | The wider R&D / audit / gap-hunt folder. |

---

## How to add an entry

```bash
# 1. Pick the right topic file. Create new if no fit.
# 2. Append to the END (chronological — never replace, never edit past entries).
# 3. Use the template from .claude/skills/scholar-immortal/SKILL.md.
# 4. Commit:
git commit -m "docs(learnings): <topic> — <short summary>"
```

---

## Re-read cadence

- **Every session start** — scan this index for any new topic / date you haven't seen.
- **Before fixing a bug** — grep `docs/learnings/` for the symptom keywords (see `bug-swarm-loop` hunter 5).
- **Weekly** — เดฟ skim through new entries on Friday integration window.

---

## Patterns to look for over time

As entries accumulate, watch for:
- **Repeated entries on same topic** → the underlying issue is systemic; consider an ADR or refactor
- **Topics that haven't gained entries** → either the area is stable OR no one's working there (worth investigating which)
- **Entries that contradict each other** → behavior changed; mark older entry as superseded with a forward pointer

---

## Why this directory exists

The user (เดฟ) described it as: *"นักปราชญ์ผู้เป็นอมตะ"* — immortal scholar. Every learning persists. Future Claude Code sessions inherit. Pacred-specific knowledge that no LLM training has = our compounding moat.

Cross-link: [`docs/STRATEGY.md`](../STRATEGY.md) §12 · [`.claude/skills/scholar-immortal/SKILL.md`](../../.claude/skills/scholar-immortal/SKILL.md).

- [data-health-invariant-monitor.md](data-health-invariant-monitor.md) — the 4-layer defence stack (guard → heal → sweep → standing invariant check) + check-calibration discipline
- [pcs-admin-house-style](pcs-admin-house-style.md) — the faithful-look color language (orange header · cyan totals · dashed-red pills · green/orange buttons · sky links) · reusable across every faithful-port admin list page
- [faithful-port-carry-not-rederive](faithful-port-carry-not-rederive.md) — legacy CARRIES a whole flow on ONE frozen row + ONE reduce; we RE-DERIVE at every hop → the number/doc/identity/selection drift. Fix = PIN/SNAPSHOT/CARRY (reconcile-not-recompute · prefer the paid value · one SOT). The root of the combo-flow money-doc drift.
- [famountcount-cbm-convention](famountcount-cbm-convention.md) — fvolume 2 conventions (total vs per-box×famount) · quantities.ts SOT + mig 0263 · the บันทึกขนาด revert-engine (2026-07-19)
