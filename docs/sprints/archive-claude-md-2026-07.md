# 📚 Archive — CLAUDE.md save-points, July 2026 (2026-07-01 → 2026-07-17)

> Moved out of the live [`CLAUDE.md`](../../CLAUDE.md) on 2026-08-03 to keep it under the AGENTS.md §12
> 2000-line cap. These are historical session save-points (superseded / shipped-history). The recent
> save-points (2026-07-18+) and every permanent section remain in CLAUDE.md.

---

# 🔴🔴 2026-07-17 ดึก-4 (ปิด session · ไปต่อ Mac) — apply prod 3 อย่าง · ข้อ 1/2/4/5 ปิด · เหลือ ตัดจ่ายค่าตู้ + ตั้งต้นทุน 303 แถว — READ FIRST

> **main = dave-pacred = HEAD** · mig 0260 applied prod · **NEXT FREE mig = 0261** · gate: tsc 0 · build 0 · dep ใหม่ `unpdf` (lockfile sync แล้ว)
> resume Mac: `git fetch && git pull origin dave-pacred` + copy `.env.local` + `pnpm install`
> **หลักฐานเต็ม:** [`docs/research/momo-invoice-reconcile-ground-truth-2026-07-17.md`](../../docs/research/momo-invoice-reconcile-ground-truth-2026-07-17.md)
> · [`docs/research/backfill-inventory-2026-07-17.md`](../../docs/research/backfill-inventory-2026-07-17.md) (⚠️ อ่านคำเตือน false positive ใน F6a ก่อนเชื่อเลข "เก็บเงินเกิน")
> · [`docs/research/remaining-2026-07-17.md`](../../docs/research/remaining-2026-07-17.md)
>
> ## ✅ APPLIED PROD แล้ว (owner เคาะ · verify ผลเองทุกอัน)
> 1. **ล้างคิวตรวจสอบ 168 → 8 แถว** (ลบ 160 · backup `backup-forwarder-check-queue-2026-07-17.json`) · verify prod: เหลือ fstatus=4 ล้วน ✓
> 2. **backfill น้ำหนัก/คิว คูณ quantity ซ้ำ — 9 แถว** · น้ำหนักผี 82,629 kg · **Σ ftotalprice ฿78,767.52 ไม่ขยับ** (invariant ผ่าน)
>    → **ชิปเม้น 1782555393 (GZS260629-1): กำไร −52,710.38 → +11,270.85** · ตรงรูป MOMO Live ทุกแถว · กล่อง 32 ตรง
> 3. **mig 0260 เรทต้นทุน** รถ 4,700 · เรือ 2,500 (session ก่อน)
>
> **owner เคาะแล้ว:** fid 52198 (PR086 เก็บเกิน ฿3,920) = **ปล่อย ไม่คืนเงิน** · **PR217 = ปิด จบ**
>
> ## ✅ ปิดแล้ว (code · gate เขียว)
> **ข้อ 1** กรอบต้นทุนอยู่ใต้ตารางสินค้า (premise เดิมคลาด — มันถูกพับซ่อนใน `<details>` ไม่ได้อยู่ล่างสุด) · gate ultra ไม่แตะ
> **ข้อ 2** parser ทน "ค่าตกบรรทัด" ทั้ง class + `reconciles` gate (fail-closed) + เก็บ `cabinet`/`memberCode` + matcher `-1/N`→bare (corroborate kg/cbm)
>   + ตู้ไม่ตรง **บล็อกรายแถว** + **อัพ PDF ได้แล้ว** (`unpdf` · server-only · re-derive ฝั่ง server เสมอ · paste ยังใช้ได้)
>   · **แกะ PDF จริง 5 ใบ reconcile ผ่านหมด** (Σ = Sub-total ทุกใบ)
> **ข้อ 4** คิวรับแถวที่เก็บเงินแล้ว → แก้ **WRITE path**: gate `'4'` เป๊ะ (min==max = เซ็ตเดียวกับที่ `adminCallPriceUser` ทำงานได้) · fail-closed
> **ข้อ 5** error รายแถว (`BillFailure {fid·reason·nextAction}`) แทน "ผิดพลาด N" ลอยๆ
> **สีแดง** (owner เคาะ: **"แดงคือยังไม่ได้สแกนเข้าโกดัง"**) → ยังไม่สแกน 1/2/3 = ตระกูลแดง · สแกนแล้ว 4+ = สีสงบ ('5' = ส้ม)
>   แก้ที่ `legacy-report-cnt.css` (`.pcs-row-st*`) ที่เดียว
> **บิลเงินสด** (owner ด่วน · บิล 122 PR134) — เงินสด **ไม่โชว์ "ครบกำหนดชำระ" + "กรุณาชำระภายใน"**
>   `header.is_credit` (SOT `isCreditRow`) ใน `load-billing-run-document.ts` → print + `/b/[token]` · **display gate ล้วน ไม่แตะเงิน**
>   prod: `tb_credit` = **0 แถวทั้งระบบ** · ใบวางบิล **122/122 ใส่ครบกำหนดหมด** = เดิมใส่โดยไม่เคยเช็คเลย
>
> ## 🔴 เหลือทำ (owner สั่ง: "ทำ ตัดจ่ายค่าตู้ · ข้อ 6 · และที่เหลืออยู่ทั้งหมด")
> ⚠️ **workflow `wf_2d9c76bd-c22` ทำ 4 เลนค้างไว้ — ผม commit ติดไปแล้ว (tsc 0 ผ่าน) แต่ยังไม่ได้ review/verify เอง:**
> `lib/forwarder/basis-drift-guard.ts` (+test) · `scripts/momo-cost-backfill-by-rate-2026-07-17.ts` ·
> `scripts/cleanup-remaining-2026-07-17.mjs` · `docs/research/remaining-2026-07-17.md` · `invoice-cost-client.tsx` แก้เพิ่ม
> → **Mac: `git diff` อ่านเอง + รัน dry-run + gate ก่อนเชื่อ** (session นี้โดน agent/verifier หลอกมาแล้ว 2 รอบ — ดูบทเรียนล่าง)
>
> **A · ตัดจ่ายค่าตู้** — `tb_cnt` = **0 แถว = ไม่เคยใช้เลย** · ของเดิมมีครบ (`/admin/cnt-hs` · `actions/admin/cnt-payment.ts`
>   `adminCreateCntPayment`/`Single` · `tb_cnt`/`tb_cnt_item`/`tb_cnt_pay_*`) → **ช่องว่าง = ไม่มีสะพาน invoice-cost → cnt-hs**
>   ⚠️ = ทะเบียนบันทึกว่าโอนให้ MOMO นอกระบบแล้ว (ไม่ย้ายเงินในแอป) · `tb_cnt_item."fCabinetNumber"` มี partial-UNIQUE mig 0183 กันจ่ายซ้ำ (handle 23505 → error ไทย)
>   ⚠️ **ใบเดียวมีหลายตู้** (INV-20260708-0002 = GZS260620-2 + GZE260701-1) · **ตู้เดียวถูกบิลหลายรอบได้** (MOMO ปล่อยเป็นรอบๆ)
>   ⚠️ ทั้ง 2 ตู้ยังไม่จ่าย (`tb_cnt_item` = 0) → ตัดจ่ายได้จริง
> **C · ตั้งต้นทุน 303 แถว / 24 ตู้ ฿348,020** — เริ่มที่ **9 ตู้/47 แถวที่ถึงไทยแล้ว** (กำไรผิดจริงตอนนี้ · ขาย ฿120,238 · ทุน ฿68,604)
>   🔴 **ลำดับห้ามสลับ:** ตู้ที่คิวยังเพี้ยน **ห้ามตั้งต้นทุนก่อน** (จะล็อกต้นทุนผิดลง DB)
> **B · guard ฐานเพี้ยนห้าม re-price** — 🧨 แถวที่คิว = **2 เท่าของ MOMO เป๊ะ** · เงินตอนนี้**ถูก** แต่ re-price = เก็บเกิน (~฿119,254 · 23 base)
>   ⚠️ guard เข้มเกิน = บล็อกงานปกติทั้งระบบ (แย่กว่าไม่มี guard) → ไม่มี box_detail / box_detail มาไม่ครบ = **ต้องผ่าน**
>
> **8 แถวในคิว — ติดคนละเรื่อง (อย่าบอกเหมือนกัน):**
>   `52137` PR067 · **PCS รับเองโกดัง → ไม่ติดอะไรเลย · แจ้งชำระได้เลย** ·
>   `52194-52197` PR067 · **ยังไม่เลือกขนส่ง** (ไม่ใช่ยังไม่ใส่ค่าส่ง) · `52162-52164` PR043 · Flash → **ใส่ค่าส่งไทย**
>
> ## 🔴🔴 3 บทเรียนแพงของ session นี้ — อ่านก่อนเชื่ออะไร
> **(1) fixture ที่กุขึ้น หลอกได้ทั้ง agent + verifier + เดฟ.** `momo-invoice-parser.test.ts` มี fixture ป้าย *"Real INV-20260618-0003"*
> แต่เอาค่าจริง**หารด้วย qty** (จริง `100.00 KG/0.3108` · fixture `50.00/0.1554`) → ผมเขียนคำแก้ที่ผิด ("MOMO เปลี่ยนรูปแบบ") ลง doc + **push ไปแล้ว**
> **verifier ก็ไม่จับ** เพราะเช็คแค่ *"agent กุเองไหม"* ไม่ได้เช็คว่า *ตรง source จริงไหม* → รอบถัดมา agent เปิด **PDF จริง** → จับได้ → ถอนแล้ว
> ✅ **ข้อเท็จจริง: `lineTotal = unitPrice × cbm` ทุกใบ ทั้ง 2 เดือน · qty = จำนวนกล่อง · MOMO ไม่ได้เปลี่ยนอะไร**
> **(2) script วินิจฉัยที่เขียนสูตรเอง ≠ SOT.** probe บอก "ผิดพลาด 8 = ค่าส่งไทย" → จริง **7** (มันเช็ค `ftransportprice<=0` เอง
> ไม่ได้ใช้ `isThShippingCostMissing` ที่ยกเว้น PCS/เหมาๆ/COD) · **ตัวระบบจริงถูกอยู่แล้ว** — ผมไปเชื่อ probe เลยรายงานผิด
> **(3) `momo_box_detail` ถูก cron เขียนตลอด** → เทียบตอน sync ค้างกลางคัน = **ส่วนต่างหลอก** (inventory doc เคยบอก "เก็บเกิน ฿10,604 · 5 base"
> → `910060609755` ฿229.50 ไม่เคยเกิน · เรา 0.089812 vs MOMO 0.089900 = ตรงกัน · รันซ้ำได้ 10,374.82 = ลบ 229.50 พอดี)
> → **ก่อนเทียบต้องเช็คว่ากล่องแตกครบก่อน** · ยืนยันเก็บเกินจริงแค่ fid 52198 (เพราะมีใบแจ้งหนี้ MOMO ยืนยันซ้อน)
> **§0b สรุป: fixture / HTML / screenshot / probe = ของที่แปลงมาแล้ว ไม่ใช่ source — กลับไปที่ PDF/DB/SOT ตัวจริงเสมอ**
>
> ## 🔑 ground truth (ยืนยันแล้ว อย่าเดาใหม่)
> - ใบแจ้งหนี้มี **ตู้ในบรรทัด** (`ค่าขนส่งสินค้าจากจีน GZS260620-2`) → ชนกับ `fcabinetnumber` = ตัวจับขัดแย้ง
>   ⚠️ **แต่ไม่ใช่ทุกใบ** — 2/5 ใบเขียน `(Guangzhou - TH)` → "ไม่ระบุตู้" = **ไม่ใช่ขัดแย้ง ห้ามบล็อก**
> - **MOMO `-1/N` = แถว bare base ของเรา** (ตรงระดับสตางค์) — ไม่ normalize = ขึ้น "ขาด" หลอก 3 ตัว
> - **`momo_box_detail` ปน 2 convention ใน "ชิปเม้นเดียวกัน"** → **ตัวชี้ขาด = dims เท่านั้น**
>   (`cbm ≈ dims` = ต่อกล่อง คูณ qty ได้ · `cbm ≈ dims × qty` = ยอดรวม **ห้ามคูณ**)
>   ⚠️ **กับดัก:** ความหนาแน่น >1,000 kg/คิว จับ = **ผิด** (โลหะหนักเกินได้จริง) · `fweight == weight_kg × qty` จับ = **ผิด flag 122 แถว**
> - เรทในใบตรง mig 0260: GZS(เรือ)=2,500 · GZE(รถ)=4,700 · ต้นทุนที่ระบบคิด = ที่ MOMO เก็บจริง ระดับสตางค์
> - **ข้อ 6 ตัวเลขจริง:** **303 แถว ✅ ตรงที่ owner จำ · แต่ 24 ตู้ ไม่ใช่ 16** ("16" กับ "303" คนละสูตร ใช้ด้วยกันไม่ได้)
>   · **฿348,020 ไม่ใช่ ฿232k** (snapshot เก่า · ตู้ใหม่เข้าเรื่อยๆ = **โตขึ้น ไม่ใช่ลด**)
>   · 207 ชิปเม้นยังไม่แตกกล่อง → จริง **1** (นิยามตรง) / 59 (กว้าง) — งาน box-basis เก็บไปเกือบหมดแล้ว
>   · 28 ตู้กล่องเพี้ยน → **4 ตู้/19 base** · PR043 = **กล่องไม่เพี้ยนแล้ว** เหลือ 3 แถว fstatus=4 (52162-52164)
>   · P22324 = **ไม่มีแทรคนำเข้าผูกเลย** → กฎ 3 ขั้นตัดสินไม่ได้ → owner สั่ง "ไม่ auto-demote" **ถูกแล้ว**
>
> **➕ ค้างเดิม:** แถวรวม=สรุป + ใต้แถวแจงกล่อง · คิดเงินต่อชิปเม้น · สี list+profile · `pnpm test:unit` พังบน Windows (รันผ่าน bash)
>
> **🔑 กติกาเครื่องนี้ (Windows · Mac อาจต่าง):** worktree ไม่มี `node_modules`/`.env.local` → copy `.env.local` + `corepack pnpm install --frozen-lockfile`
> · **`.env.local` DB pw STALE** → prod `DqOzfEZVXfMHIryz` (pooler `aws-1-ap-southeast-1`) · script ต้องอยู่ใน repo ถึง resolve `pg`
> · `PYTHONIOENCODING=utf-8` (cp1252 พังกับไทย) · tsx: **ห้าม top-level await** (ห่อ async main) · `import "server-only"` = import ตรงไม่ได้
> · **JSX ห้ามใส่ `{/* */}` คั่นระหว่าง props** (อ่านเป็น spread → TS1005) · tsc รันซ้ำ 2 รอบถ้าผลไม่นิ่ง (agent เขียนไฟล์ค้าง = stale)

---



# 🔴🔴 2026-07-17 ดึก — WORK ORDER รอบใหม่ (owner สั่ง · ยังไม่ทำ) — READ FIRST

> **✅ ทำแล้ว:** เรทต้นทุน MOMO **รถ 4,700 · เรือ 2,500 (ต่อคิว) applied prod** (mig 0260 · ต้นทุนที่บันทึกไว้ไม่ขยับ Σ ฿418,130.30 · เปลี่ยนได้ที่ ตรวจรายการตู้ = `tb_cost_container` ชนะเสมอ ตาม waterfall ใหม่).
>
> **1️⃣ กรอบต้นทุน ย้ายขึ้นข้างบน** — owner: "กรอบต้นทุน เอาขึ้นมา อยู่ข้างบนต่อรายการไปเลยครับ เห็นแค่ ultra เหมือนเดิมนะครับ" → panel "ต้นทุน · มูลค่าสำแดง (Pricing · ใบขน)" ใน `/admin/forwarders/[fNo]` (`forwarder-cost-section.tsx`) ย้ายไป**ใต้ตารางรายการสินค้าทันที** (ตอนนี้อยู่ล่างสุด) · gate เดิม (`canViewCostProfit` · ultra/accounting/pricing) **ห้ามเปลี่ยน**.
>
> **2️⃣ 🔴 จ่ายต้นทุนตู้ = FLOW ใหม่ (งานใหญ่):** owner "ให้ทางบัญชี อัพไฟล์ PDF จากทาง MOMO — MOMO จะปล่อยไฟล์มาให้บัญชีเป็นรอบๆ → บัญชีอัพ PDF → **ตรวจสอบข้อมูลกับระบบเรา ว่าถูกต้องตรงกัน หรือมีอะไรขัดแย้ง** → แล้วให้ **ทำตัดจ่ายต้นทุนตู้ในระบบเราได้เลย** เพราะ **MOMO วางบิลเรามาเป็น Tracking ครับ แต่เราคิดเป็นตู้ ไปตรวจให้ตรงกันนะครับ**" · ไฟล์จริง owner แนบ: `C:\Users\Admin\Desktop\INV-20260708-0002.pdf` (210KB).
> ⚠️ **แกน mismatch = หัวใจ:** MOMO บิลต่อ **tracking** · เราคิดต้นทุนต่อ **ตู้** → reconcile ต้อง map tracking→ตู้ แล้วรายงาน ตรง/ขัดแย้ง/ขาด/เกิน **ก่อน** ให้กดตัดจ่าย.
> ของเดิมต้อง**ต่อยอด อย่าสร้างใหม่**: `/admin/api-forwarder-momo/invoice-cost` (paste invoice · match ftrackingchn · preview→apply) + `scripts/momo-invoice-cost-backfill-2026-06-26.mjs` + `/admin/cnt-hs` (จ่ายค่าตู้ · `actions/admin/cnt-payment.ts` · `tb_cnt`/`tb_cnt_item`/`tb_cnt_pay_*`).
>
> **3️⃣ 🔴 GZE260627-1 แก้เลย (owner สั่ง)** — 31 แถว · ตั้งเรท 4,700 แล้วแต่ apply ถูก reject: 25 แถว tag Sang(1)=คิดตามน้ำหนัก → 4,700 × 69,816kg = **฿328M** → sanity backstop กันไว้ · **ไม่มีต้นทุนมา 2 สัปดาห์ = กำไรเพี้ยนหนักสุดในระบบ** · ต้นตอ = **น้ำหนักมั่ว** (~2,792kg/กล่อง ที่ 0.4 คิว = เป็นไปไม่ได้ทางฟิสิกส์ → garbage หรือ aggregate row) → ซ่อมน้ำหนักจาก `momo_box_detail`/packing ก่อน แล้วค่อยคิดต้นทุน.
>
> **4️⃣ คิวตรวจสอบตู้ · สถานะไม่ตรง** — owner "บางสถานะมัน **ส่งแล้ว หรือ รอส่ง** มันจะยังไม่ส่งแจ้งชำระในรอตรวจสอบอีกได้ไงหละครับ **มันควรจะเข้าไปแค่ รายการที่จะให้ลูกค้าชำระเงิน**" → `/admin/forwarder-check` (`tb_check_forwarder`) รับแถว fstatus 6/7 เข้ามาด้วย ทั้งที่ควรรับเฉพาะที่ยังไม่เก็บเงิน (4 = ถึงไทย พร้อมแจ้งชำระ) → กรอง + backfill ล้างคิว.
>
> **5️⃣ "แจ้งชำระเงินสำเร็จ 0 · ผิดพลาด 8"** — owner แคปมา · 8 แถวกดแจ้งชำระไม่ผ่าน · ต้องหา error จริง**รายตัว** ไม่ใช่โชว์ "ผิดพลาด 8" ลอยๆ ([[wrong-error-message-hides-real-block]]).
>
> **6️⃣ backfill เคลียร์ทั้งระบบ** — owner "งานที่มีปัญหา รอเก็บเงิน หรือข้อมูลอะไรที่ไม่ถูกต้อง ใส่ backfill เติมใส่ให้ครบ เชื่อมโยงให้ถูกต้อง เคลียร์ให้เป็นปัจจุบัน" · ของค้างที่รู้แล้ว: **16 ตู้/303 แถวไม่เคยตั้งต้นทุน (~฿232k)** · 207 ชิปเม้นยังไม่แตกกล่อง · 28 ตู้วางบิลแล้วกล่องเพี้ยน (รวม `800206224068` = 13/3) · PR043 (`GZS260628-2`) · P22324 (PR075 · เก็บ '5' แต่กฎว่า '4' · owner เคาะ · ไม่ auto-demote).
>
> **➕ ค้างเดิม:** PR217 ใบเสร็จนิติ ฿27,047.45 + แนบสลิป (เงินคืน ฿273.20 ทำแล้ว) · แถวรวม=สรุป + ใต้แถวแจงกล่อง/ขนาด · คิดเงินต่อชิปเม้น · สี list+profile · `pnpm test:unit` พังบน Windows (chain 8411 > cmd 8191 · รันผ่าน bash).

---

# 🔴🔴 2026-07-17 ค่ำ — WORK ORDER ค้าง 5 เรื่อง (owner สั่ง · ยังไม่ทำ) — READ FIRST · ทำให้จบรวดเดียว

> **owner (verbatim):** "เราอยากให้ แถวรวม/หัว คือ สรุปทั้งหมดของชิปเม้นนั้น และมีทุกอย่างของชิปเม้นนั้น ทีนี้ ด้านล่างของแถวรวมให้กลายเป็นแจงรายละเอียดของในชิปเม้นนั้นๆแทน เหมือนหน้ารายการตู้ · แล้วก็เรื่องคิดเงิน ให้คิดเป็นชิปเม้นไปเลย จะคิดตามคิว หรือตามน้ำหนัก ก็ให้ดูยอดรวมของชิปเม้นนั้นๆ · ก็รวมไปเลยนะครับ คิดเงินเป็นชิปเม้น แต่มีแจง แทรคกิ้ง กล่อง ขนาด จำนวน ให้ชัดเจนยิ่งกว่าเดิม · ทุกปัญหา อย่าให้เกิดอีกนะครับ หลายลอบจัดหลายเรื่องจัด"
>
> **🧩 ต้นตอร่วมของข้อ 1/2/3/5 = เรื่องเดียวกัน:** MOMO ส่ง **กล่อง+น้ำหนัก+ขนาด ราย-กล่อง** มาครบใน `momo_box_detail` (w/l/h/weight_kg/cbm/quantity) แต่ตอนแตกแถว เรา **ก๊อปยอดรวมลงทุกแถวย่อย** → นับซ้ำ. แล้ว self-heal pass 6 (`box-detail-reconcile-plan.ts:314`) **ไม่กล้าแตะเพราะแถวรวมถือเงิน** (`priced_anchor_bare` guard ที่เดฟเขียนเอง 2026-07-16) → **ค้างถาวร** = "ทำไมยังแก้ไม่หายสักที".
>
> **หลักฐาน prod (verified · ตรงรูปกล่องที่ owner ถ่ายมา):**
> ```
> 519218029029 (PR050 · GZE260707-1)      ← ป้ายกล่องจริง "1/2" = มีแค่ 2 กล่อง
>   momo_box_detail:  -1/2 → 1 กล่อง · 16.5kg · 0.0356 คิว
>                     -2/2 → 1 กล่อง · 20.0kg · 0.035574 คิว   Σ = 2 กล่อง · 36.5kg
>   ระบบเรา: #52380 (bare)  famount=2 · 36.50kg · ฿730  ← หัวบิล/แถวรวม
>            #52477 (-1/2)  famount=2 · 36.50kg · ฿0    ← ก๊อปยอดรวม ✗
>            #52478 (-2/2)  famount=2 · 36.50kg · ฿0    ← ก๊อปยอดรวม ✗
>            Σ = 6 กล่อง · 109.5kg   ← ที่จอโชว์ "0/6"
> 800206224068 (PR079)  แถวรวม 13/3 · กล่องย่อย -/1,-/4  ← โครงเดียวกัน (owner: "13/3 บัคไหมหละครับ")
> ```
>
> **✅ ปิดแล้ว 2026-07-17 (`47435ddf`+):** box-basis reconcile 87 แถว/38 ชิปเม้น ยึด momo_box_detail (519218029029 = **2 กล่อง/36.5kg** ตรงป้ายกล่อง owner ✓ · เงินไม่ขยับ Σ ฿175,922.88) · zero-basis guard (`live-rate.ts` · ฐาน=0 ห้าม re-price → เงินบนหัวสรุป pin ไว้ = ปลดล็อก `priced_anchor_bare`) · เก็บซ้ำ 3 ชิปเม้น (SF5117630215855 944.72→490.88 · 519220849050 830→415 · 888073011722 229.50→141.05 · ยังไม่วางบิล = กันทัน) · **ถอยสถานะจาก dropdown (ultra-only)** = `actions/admin/forwarder-rollback.ts` + `lib/admin/forwarder-rollback-plan.ts` (pure · test 32).
>
> **1️⃣ แถวรวม = สรุปชิปเม้น · ใต้แถวรวม = แจงรายละเอียด** (owner แคปหน้ารายการตู้มาให้เป็นแบบ)
>    ตารางที่ต้องการ: `แทรคกิ้ง · กว้าง(ซม.) · ยาว · สูง · CBM/กล่อง · จำนวนกล่อง · CBM รวม` — ข้อมูลมีครบใน `momo_box_detail` แล้ว. หน้าที่ต้องแก้ = `/admin/forwarders` (forwarders-table.tsx · กลุ่ม `N แทรค`) ให้เหมือน report-cnt/[fNo] ที่กางกล่องย่อย.
>
> **2️⃣ คิดเงิน = ต่อชิปเม้น** (ไม่ใช่ต่อแถว) — คิว/น้ำหนัก ใช้ **ยอดรวมของทั้งชิปเม้น** เทียบเรท. ⚠️ money-critical: ต้องดู `resolveLiveForwarderRate` / `computeAndFillForwarderImportRate` / ค่าเทียบ (`userComparisonValue` default 250) — ตอนนี้คิดต่อแถว.
>
> **3️⃣ box-count fix + backfill ทุกตู้** — ค่าที่ถูกต้องต้องมาจาก `momo_box_detail` เสมอ:
>    - แถวย่อย: `famount`/`fweight`/`fvolume` = ค่าจริงราย-กล่อง (ftotalprice=0 → **money-neutral เขียนได้เลย**)
>    - แถวรวม (bare): เก็บ `ftotalprice` ไว้ (เงิน!) แต่ล้าง `famount`=0 (**famount = display-only → ปลอดภัย** ตาม CLAUDE.md 2026-07-15)
>    - 🔴 **จุดเสี่ยง:** ล้าง `fweight`/`fvolume` ของแถวรวม = ฐานคำนวณราคา → ถ้ามีอะไร re-price แถวนั้นภายหลัง → **฿730 → ฿0 เงินหาย** (= เหตุผลที่ guard เดิมไม่ยอมแตะ)
>    - **ทางแก้ที่ปลอดภัย:** ล้างฐาน **พร้อมใส่ guard "แถวที่ฐาน=0 ห้าม re-price"** ใน `computeAndFillForwarderImportRate` → เงินล็อก แตะไม่ได้
>    - แล้วปลด guard `priced_anchor_bare` ให้ self-heal ทำงานได้ + **backfill ทุกตู้** (dry-run + backup + invariant Σ ตรง momo_box_detail)
>
> **4️⃣ PR217 ใบเสร็จนามนิติ ฿27,047.45 + แนบสลิป** (owner: "fill ไปเลยครับ")
>    prod verified: โอนเข้า `105615` **27,320.66** (KBANK 225-2-91144-0 · 15/07 14:19) · ตัดจ่าย 3 ออเดอร์ (52456 6,422.92 + 52473 10,386.30 + 52481 10,511.44 = 27,320.66 · จ่ายเต็ม gross ก่อนเป็นนิติ) · **คืน 1% `105619` ฿273.20 = settled แล้ว 15/07 02:57** ✓
>    27,320.66 − 273.21 = **27,047.45** ✓ ตรงยอด owner. **ที่ขาด = ใบเสร็จ**: มีแค่ FRC2607-00027 = 10,282.44 → ต้องออกให้ครบ 27,047.45 **นามนิติบุคคล** + แนบรูปสลิป. ข้อมูลถอนคืน: ธ.กสิกรไทย **0253735996** วัชราภรณ์ ธรรมวัตร.
>
> **5️⃣ PR043 ของมาครบแล้ว แต่กล่องเพี้ยน → เก็บเงินไม่ได้** (`GZS260628-2` · 79014489632554 / 79014493552596 / YT7629647967871 ฯลฯ · 14 แถว) = โครงเดียวกับข้อ 3.
>
> **➕ owner ยังสั่ง (ค้าง):** สีสถานะใน **list + หน้า profile ลูกค้า** ให้ "จี๊ดจ๊าดชัดเจน" (report-cnt/[fNo] ทำแล้วใน `4a4aeab8` · ที่เหลือยังไม่ทำ) · "ยังมี 1/1 อยู่เลยแล้วสีแดง" (#52363 · 1/1=ครบ แต่ fstatus=5 รอชำระ → แดง = ตั้งใจ แต่ owner มองว่าครบแล้วไม่ควรแดง → **ต้องเคาะ**).

---

# 🧾 2026-07-17 (เดฟ · resync + ตรวจงานค้าง disk/agent) — 🔴 ปิดงานเหมาๆ split-box (per-shipment anchor · ซ้ำไม่ได้โดยโครงสร้าง) + reconcile 4 บิลช่องผิด + integrate ภูม/ปอน → ALL BRANCHES · read FIRST

> **🏁 CLOSE (owner: "resync main pull dave-pacred · ตรวจงานที่ค้างใน disk และ เอเจ่น เมื่อคืนติด limit แล้วสลับเมลทำต่อ · เช็คครบแล้ว รวมงานน้องและเราขึ้น main").** **main = dave-pacred = `<HEAD>`** (Vercel prod). **ตรวจงานค้าง = ครบ:** disk 0 ไฟล์ · `c6baf98d` (งาน Windows) อยู่ใน main ✓ · session สลับเมล (adoring-chandrasekhar) ทำต่อจบ (`9cf1edc0` URGENT PRF own-fleet + `6dc3210e` report-cnt สี) — **เหลือแค่เหมาๆ ที่ defer ไว้ → ปิดใน session นี้**. gate: tsc 0 · build 0 · tests 10. NEXT FREE mig = 0257 (ไม่มี migration ใหม่).
>
> **🔴 เหมาๆ split-box — ปิดจบแล้ว (`docs/research/mao-fee-split-box-anchor-2026-07-16.md` = RESOLVED):** owner "จ่ายแทนลูกค้า /admin/forwarders/52474 ไม่แจงค่าเหมาๆ · ระวังไปเก็บซ้ำ · อย่าให้เกิดขึ้นอีก".
> - **ROOT (1 ต้นตอ → 3 อาการ):** guard `"a -N box sub-row NEVER anchors"` (กันเก็บซ้ำ 2026-06-23) สมมติว่าแถวหลักมีเสมอ — แต่ MOMO แตกกล่องตั้งแต่ commit → `JYM800120650588` มีแค่ `-1/4..-4/4` **ไม่มีแถวหลัก** → ไม่มีใคร anchor → **เหมาๆ ฿0 หายเงียบ** (prod **7/60** ชิปเม้น PCSF). → (a) จ่ายแทนลูกค้าเก็บ 1,085.55 vs บิล 1,184.54 = **ขาด ฿98.99** · (b) พนักงานพิมพ์ ฿100 มือลง **`delivery_th_thb`** (free-text) ทั้งที่เอกสารแจงจาก **`mao_fee_thb`** → **"ไม่แจงค่าเหมาๆ"** · (c) `billing-run.ts:1718` total = `subtotal + maoFeeTotal + … + deliveryThThb` = **2 ช่องบวกได้** → ชนวน **บิล ฿200**.
> - **FIX = per-SHIPMENT anchor** (`lib/forwarder/mao-anchor.ts` · `resolveMaoAnchorIds` อ่านพี่น้องทุกแถวจาก DB → แถวหลักถ้ามี · ไม่มีก็ **suffix ต่ำสุด** → ส่งเข้า engine เป็น `maoAnchorIds` opt · ไม่ส่ง = เดิมเป๊ะ). batch คิด ฿100 **ก็ต่อเมื่อมีแถวนั้นอยู่** → **2 บิลถือแถวเดียวกันไม่ได้ = ซ้ำไม่ได้โดยโครงสร้าง** (ที่ owner กลัว + ที่ทำให้ defer). wire: pay-user-view · pay-user ×3 (debit จริง) · billing-run (→ ลง `mao_fee_thb` → แจงบรรทัด "ค่าส่งเหมาๆ (PCSF)" = ปิด ask itemisation). **test 10** รวมเคส "จ่ายทีละกล่องทั้ง 4 ใบ = ฿100" + "บิล A/B คนละครึ่ง = ฿100". fail-safe: DB error → set ว่าง → under-charge ไม่มีทางซ้ำ.
> - **DATA prod (`reconcile-mao-fee-column-2026-07-16.mjs` · dry-run+backup+txn+invariant):** ย้าย `delivery_th_thb → mao_fee_thb` **4 ใบ** (FRI2607-00080/00032 · FRI2606-00022 ฿100 · FRI2606-00006 ฿50) · **total คงเดิมทุกใบ = money-neutral** (script assert foot ก่อน COMMIT) · re-run 0. **ข้ามถูก:** FRI2607-00019 (PCS=รับเองโกดัง) · 00029 (carrier ว่าง) · FRI2606-00008 (Flash · แถวมีค่าส่งจริง ฿165). **prod ตรวจแล้ว 0 ใบที่มีทั้ง 2 ช่อง = ยังไม่เคยเก็บซ้ำจริง.**
>
> **🔀 integrate:** ภูม `6c7a862e` (docs + audit script missing-sibling 0001779-2 · dry-run · ไม่มี live-path) + `8e92b6f6` (dashboard รอชำระ โชว์ net หัก WHT 1% ผ่าน `computeBillWht` SOT = display-only ✓) · ปอน 3 (pay-user modal QR/ตีกลับสลิป/mockup · 0 money-write). merge สะอาด 0 conflict.
>
> **🔴 ลูกค้าฝากสั่งของไม่ได้ (owner "ลูกค้าหลายเจ้าติดปัญหา · แทนที่ควรใช้ได้ตั้งนานแล้ว") — FIXED:** PR619 เลือกสติกเกอร์ 1688 ¥0.17 × **90,000 ชิ้น** → กดแล้วเด้ง **"ยังไม่ใส่ราคา CNY"** ทั้งที่ราคาโชว์อยู่ → กรอกซ้ำก็ไม่หาย = ตายวนลูป.
> - **ROOT 1 · เพดาน 2 ตัวไม่ตรงกัน** (`url-paste-add-to-cart.tsx`) — ช่องกรอก `max = Math.max(maxClamp, sku.stock)` (ยอม 90,000) แต่ตอน submit เทียบ `q > maxClamp` โดย **`maxClamp = maxQty || 999`** (listing ไม่ส่ง maxQty → **999**) → พิมพ์ได้ · ปุ่มกดได้ · กดจริงเด้ง = **กำแพงมองไม่เห็น**. FIX: helper `skuMaxQty(stock)` **ตัวเดียว** ใช้ทั้ง input max + clamp ตอนพิมพ์ + validation ตอนส่ง → เลขที่ submit จะปฏิเสธ **พิมพ์ไม่ได้ตั้งแต่แรก**.
> - **ROOT 2 · error ผิดเรื่อง** — copy-paste `t("priceNotEnteredError")` มาใช้กับเคส**จำนวน** → โทษราคา. FIX: `qtyOutOfRangeError` บอกตัวเลือกที่ผิด + ช่วงที่รับได้ ("จำนวนของ '2水果12款' ต้องอยู่ระหว่าง 1–90,000 ชิ้น").
> - **หลักฐาน prod (เจ็บกว่าที่คิด):** ตะกร้าลูกค้าทั้งระบบ **qty สูงสุด = 150 · 0 แถวเคยเกิน 999** แต่ `tb_order` (แอดมินคีย์ให้) **max 10,000** → ลูกค้าอยากสั่งเยอะจริง แต่ **self-serve ทะลุ 999 ไม่ได้เลยสักคน** (1688 = เว็บขายส่ง → กำแพงนี้ตัดขาช่องทางหลัก) · PR619 ตะกร้า 0 / ออเดอร์ 0. learning [[wrong-error-message-hides-real-block]].
>
> **🌐 แปลไทยอัตโนมัติ ฝากสั่ง (owner "ควรแปลไทยให้มาเลย + บอกเล็กๆ Translator to thai กดแล้วแสดงต้นฉบับ แบบสากลๆ"):** ของเดิม `<TranslateButton>` = **กดแล้วค่อยแปล** (ลูกค้าอ่าน 跨境卡通贴纸… ไม่ออก + 40 ตัวเลือกต้องกด 40 ครั้ง). NEW **`components/translate/auto-translate.tsx`** — `<TranslateProvider>` ยิง **batch เดียว** (`translateTextsAction` · dedupe ≤60 · cache `translation_cache` mig 0246 → view ที่ 2 = instant) + `<AutoTranslateText>` โชว์ **ไทยเป็นค่าเริ่มต้น** + ป้ายเล็ก **"แปลอัตโนมัติ · ดูต้นฉบับ"** (แบบ Twitter/FB). wire: ชื่อสินค้า + ตัวเลือกทั้ง 40 ในกริดฝากสั่ง. **ไม่ใช่จีน = ไม่แตะ · ยังโหลดไม่เสร็จ/แปลพัง = โชว์ต้นฉบับ** (ไม่มีวันจอว่าง). display-only.
>
> **🔴 CARRYOVER:** (1) **`delivery_th_thb` ยังเป็น free-text ไม่มี cross-check กับ auto เหมาๆ** — drop หายแล้ว (workaround ไม่มีเหตุให้เกิด) แต่ควรมี guard เตือนถ้าพิมพ์ ≈ค่าเหมาๆ บนบิล PCSF ที่มี auto fee อยู่แล้ว. (2) **1783051207** (PR075 · 19 แถว · ไม่มีแถวหลัก + ไม่มี -1 → minSuffix=2 · billed fstatus=6) = ยังตกเหมาๆ · fails-closed · **owner เคาะว่าจะเก็บย้อนไหม**. (3) ภูม flag **#52315** = ตรวจแล้ว **ไม่ใช่บัค** (บิล 84 status=issued · paid_at=null → fstatus=5 ถูกแล้ว · แต่ 52309 บิลเดียวกันอยู่ 6 = ต่างกัน ควรดู). (4) carryover เก่า (credit-withdraw flow · box-split backfill · GZS260628-2 14 แถวรอยิงเข้าโกดัง).

---

# 🧾 2026-07-16 ค่ำ (เดฟ · resume จาก limit) — 🔴 URGENT ลูกค้าสั่งของไม่ได้ (เหมาๆ PRF own-fleet) + report-cnt สี ยิงเข้าแล้วขาว + เหมาๆ split-box drop (diagnosed · defer) → push dave-pacred + main · read FIRST

> **🏁 CLOSE (owner: "resync main pull dave-pacred · ต่องาน run-ค้าง · แก้งานตามรูป · จบแล้ว push dave-pacred + main").** **dave-pacred = main = `<HEAD>`** (Vercel prod). resync จาก c6baf98d. ⚠️ `.env.local` pw STALE — prod `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`). NEXT FREE mig = 0257. gate: `rm -f .next/dev/types/validator.ts` ก่อน · build อ่าน exit จริง · tsc 0. ไม่มี admin login เว็บเรา → verify code + prod DB + gate.
>
> **✅ งาน resume นี้ (push dave-pacred + main):**
> - **🔴 URGENT ลูกค้าสั่งของไม่ได้ (`9cf1edc0`)** — เลือก เหมาๆ → cart เขียน carrier="PRF" (D1 rebrand ของ PCSF) แต่ `carrier-coverage-guard.OWN_FLEET_SHIPBY` มีแค่ ["PCS","PCSF","PCSE"] → reject "ขนส่ง PRF ไม่อยู่ในรายชื่อ" → ส่งออเดอร์ไม่ได้. FIX: เพิ่ม PRF/PRE เข้า own-fleet + pay-method (pay-method มี PRF แล้ว · guard ตกหล่น = ต้นตอ). **บทเรียน: D1 rebrand (PCSF→PRF) ต้อง sweep ทุก hardcoded-list · pay-method อัปเดตแต่ guard ลืม** (L-MIG-04). guard 69/0 · pay-method 28/0.
> - **report-cnt สี (`6dc3210e`)** — owner "ยิงของเข้าโกดังไทยแล้ว ยังแดง · ต้องขาว". fstatus='4' (ถึงไทยแล้ว=ยิงเข้าแล้ว) → `pcs-row-done` (#e6f5ec near-white) ไม่ใช่ red. c6baf98d neutralise settled(6/7/8) แล้ว แต่ลืม arrived(4). '5'(รอชำระ)+'<4'(in transit) ยังแดง (ตั้งใจ).
> - **🟡 เหมาๆ split-box drop (#52474) — diagnosed · DEFER (money · owner กลัว double-charge)** ([`docs/research/mao-fee-split-box-anchor-2026-07-16.md`](../../docs/research/mao-fee-split-box-anchor-2026-07-16.md)) — จ่ายแทนลูกค้า ตกเหมาๆ ฿100 เพราะ #52474 = split box `JYM…-1/4` · rule "-N never anchors" → batch ที่มีแต่ -N ไม่มี anchor → maoFee=0. bill ถูก (100 อยู่ delivery_th_thb · mao_fee=0). 6 invoice เก็บเหมาๆ ผิดช่อง. **fix ที่ถูก = per-shipment anchor ข้าม batch** (double-charge-critical = สิ่งที่ owner กลัว) → ไม่รีบ · ต้องทำ resolver + test matrix (prior audit 2.2M tok ค้าง). NOT rushed (ห้ามทำงานบัค).
>
> **🔴 CARRYOVER:** (1) **เหมาๆ per-shipment anchor** (build resolver + test + wire pay-user/bill + backfill 6 invoice · owner เคาะ) · (2) carryover เก่ายังอยู่ (credit-withdraw flow · box-split backfill).

---

# 🧾 2026-07-16 (เดฟ · Mac · owner-driven · resume ×2 จากเมลหลักติด limit) — MOMO แตกกล่อง 3-root + reconcile 21 dup + dangler + packing-gate + pay-on-behalf doc-split + ↩ ย้อนการรับชำระ (unwind ครบวงจร) + wire-map flow A1-A6 + B2 driver-stop cleanup + integrate ภูม → ALL 4 BRANCHES · read FIRST

> **🏁 SESSION CLOSE (owner "เอางานน้องมารวม → main พร้อมกัน → สรุปปิด session ตามสูตร" · + resume Windows PR178/PR139 + /admin/incidents + MOMO box-count root).** **main = dave-pacred = Poom-pacred = InwPond007 = `ff1ab622`** (ทั้ง 4 sync · Vercel deploy prod). resume: `git fetch && git pull origin dave-pacred`. **NEXT FREE mig = 0257** (0256 customs_declaration+customs_importer_lead · applied prod+dev). gate ทุก commit: **tsc 0 · build 0** (`NODE_OPTIONS=8192 node node_modules/next/dist/bin/next build` · read BUILD_EXIT · `rm -f .next/dev/types/validator.ts` ก่อน). 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`) · pooler aws-1 · direct `db.<ref>.supabase.co:5432` ก็ได้. **ไม่มี migration ใหม่ session นี้ · NEXT FREE = 0256.** ⚠️ money items = dry-run+backup ก่อน --apply · เครื่องข้าม-session หลายเมล (Mac ↔ disk) — resync ก่อนเสมอ (session นี้ FF absorb งาน disk-machine 89+6 commit).
>
> **✅ SHIPPED (money-review diff เอง · gate 0 · owner-driven · หลาย resume):**
> - **🔴 MOMO แตกกล่อง "ทำไมยังเกิดอีก" = 3 ราก (`a4e22e92`+`e2ac5513`):** (1) **split scrape-scope** — pass 5 derive candidate จาก scrapedBases เท่านั้น → parcel เดินสถานะแล้วตกscope → ไม่แตะ ทั้งที่ box_detail มีกล่อง. FIX `findMultiBoxBases()` ดึงจาก momo_box_detail (durable) → union scrapedBases∪durable · fix script CLONE_OMIT adminidupdate. (2) **reconcile 21 dup/stale** vs box_detail (MOMO truth · invariant นับ+Σ===box_detail หรือ exact-dup · money-guard unbilled · driver refs=0) → ลบ 21 แถว/19 base. (3) **repair 21 dangling staging** (committed_forwarder_id ชี้ row ที่ลบ = เครื่องปั๊ม dup) → re-point ทั้งหมด. backfill แตก 3+1 (760235240370/KY982669997/LJ20479565/**800206224068 via dims-fallback** · money-neutral Σ preserved). [[momo-boxsplit-3-roots-2026-07-14]].
> - **🔴 billing packing-gate false-block (`ed15d61f`):** vpage บอก "ตู้ยังไม่อัพ packing #52577" ทั้งที่อัพแล้ว — banner ให้อัพที่ momo_packing_upload (mig 0254) แต่ gate อ่านแค่ container_packing_reconcile (mig 0245) = คนละตาราง. FIX `lib/admin/packing-confirmed-cabs.ts` — reconcile OR upload = ผ่าน · union 2 gate.
> - **🔴 pay-on-behalf doc-split (`3adbe26b` · PR139 นิติกดจ่ายไม่ได้):** ROOT = ลูกค้ามีงาน 7 แต่ทั้งหมด fstatus=3 → list ว่าง. FIX `pendingByStatus` → empty-state แจงต่อสถานะ "มี N รายการ อยู่สถานะ X — ชำระได้เมื่อถึงไทย+ตั้งราคา (5)" · banner นิติ dead-end→amber ชี้ทาง · labels doc-model (เงินสด=หน้านี้ ใบเสร็จ/ใบแจ้งหนี้ · ปุ่ม→เครดิต=ใบวางบิล billing-run).
> - **🔴 ↩ ย้อนการรับชำระใบวางบิล (`3adbe26b` · owner "ยกเลิกเอกสารแล้วสถานะต้องถอยเป็นเส้นตรง"):** NEW `adminReverseBillingRunPaid` = mirror markBillingRunPaid: ATOMIC CLAIM paid→issued · เครดิตคืนแม่นยำจาก audit log · fwd 6→5 · advance paydeposit clear · void ใบเสร็จ fully-covered. GUARD REFUSE ≥7 + open driver. ปุ่มบน billing-run/[id]. + data-fix #52118 (PR143) revert.
> - **🔴 wire-map flow A1-A6 (`ca03f05f` · MONEY · disk-machine · audit 6-stage → 9 gap adversarial-verify · verdict doc-chain WELL-WIRED):** A1 pay-on-behalf reverse void ใบเสร็จ (symmetry) · A2 **ยกเลิกบิล paid จากลิสต์ = REFUSE** ชี้ไปหน้าถอย · A3 slip verified→pending ตอนถอย · A4/A5 linkage บิล↔ใบเสร็จ + ออเดอร์→สลิป · A6 ลบลิงก์ตาย 404 (warehouse-history/forwarders/reports). +fix 4 §0c bare-destructure.
> - **🔴 B2 driver-stop cleanup ตอนถอย 6→5 (`2811594b` · flow-integrity):** ถอยออเดอร์กลับรอชำระแต่ stop คนขับค้าง → order โผล่รอบคนขับทั้งที่ไม่จ่าย. NEW `lib/admin/revert-driver-cleanup.ts` — refuse ถ้าออกรถ ('1') · ลบ stop ยังไม่ขึ้นรถ ('') หลังถอย + auto-complete รอบว่าง · wire 3 reverse paths. prod stranded=0.
> - **report-cnt audit:** ตู้ active ทุกตู้ · dup=0 · dangling=0 · famount=0 บัค=0 · ราคา=0 บนถึงไทย=0 · นน.=0=0 · Σ ตรง MOMO. multi-box ค้าง 0 (cron แตกเอง).
> - **integrate ภูม (`9ebc8e36`):** drivers/new ตาราง+tabs สี legacy · report-cnt zebra/ขนส่ง-pill/เลขตู้ฟ้า/ปุ่มลอย portal · Yiwu packing preview-only (money-free · server-refuse guard) · MOMO ตรวจตู้ Slice 2. display-only 0 money-write.
>
> **➕ CUSTOMS-LEADS (owner 2026-07-16 · `aec5ac7a`+`ff1ab622` · ใบขน NetBay → คิวเซลโทรตาม):** owner "ดึงใบขนจาก NetBay (รถ/เรือ/แอร์) → data ลูกค้าใช้ใบขน (ชื่อ/นิติ/ที่อยู่/HS/อากร/ท่า/supplier/แพทเทิน) เข้า DB ให้ใช้จริง · เซลโทรตามมาเปิดใบขนกับเรา · เจ้าที่มีในระบบชนด้วยเลขนิติ ได้เบอร์เลย". ใบขน NetBay = xlsx inline-string 500-col (Importer=ลูกค้า · Tariff(AZ)=HS · CF=นิติ · CH=ชื่อไทย · supplier จีน · **ไม่มีเบอร์** → เบอร์ได้จากชน tb_corporate→tb_users). **mig 0256** (customs_declaration 1แถว/ใบ [lines jsonb HS/อากร/vat/cif] + customs_importer_lead 1แถว/บริษัท [decl/CIF/HS + cross-ref matched_userid/phone/sale + workflow lead_status/call_note] · RLS service-role). **ingest** `scripts/ingest-customs-declarations-2026-07-16.mjs` (reusable · parse+crossref+re-aggregate idempotent · ⚠️ count/CIF ต่อ-ใบ · HS ต่อ-บรรทัด แยก CTE กันคูณ · global cross-ref กัน mode หลังทับ mode ก่อน). **applied prod ครบ 3: 803 ใบขน · 254 บริษัท · 🟢 27 มีในระบบ (มีเบอร์ทุกเจ้า) · 🔵 226 ใหม่ · 1 AXELRA(เครือ)** · เจ้าใหญ่ NNB 100ใบ/529HS/฿52M. **UI** `/admin/customs-leads` (super/sales/ops · sidebar Extension CRM) — self-explaining row + ☎ เบอร์(เดิม)/เซลหาต่อ(ใหม่) + call-workflow (โทร/สนใจ/เปิดแล้ว/ไม่สนใจ/โน้ต/มอบเซล §0f) + drill-down ใบขน(HS·อากร·ท่า·supplier) + tabs (🔔ต้องโทร/🟢เดิม/🔵ใหม่). gate tsc0/build0/i18n. 🔵 owner: เซลเริ่มโทร 27 เจ้าเดิมได้เลย · 226 เจ้าใหม่หาเบอร์ (DBD/กูเกิลนิติ).
>
> **➕ CONTINUED (Windows-machine resume · `c64d4bdc` · money-review diff เอง · gate 0):**
> - **🔴 pay-on-behalf auto-heal stale orphan (`c64d4bdc` · PR178 · MONEY · workflow adversarial-verify · double_collect=false):** owner "กดแนบสลิปแล้วไม่ไป" — 52133/52134 (COD J&T) ถอยมารวมบิล เอกสารยกเลิกครบ (FRI52/53 + ใบเสร็จ FRG26) + fstatus=5 แต่ settled `tb_wallet_hs 105593` (฿364.82 · typenew=6 status=2 · KBANK direct-slip) ค้าง → create-gate dead-end "ชำระไปแล้ว" (adminVoidReceipts = doc-only ไม่ un-settle · pay-user.ts:1502 · ปุ่มย้อนโชว์แค่หน้า history). NEW `reconcileStaleForwarderPayOrphan` (shared un-settle core = steps 2-5 ของ adminReverseForwarderPayment · byte-identical guards: fstatus≥6/credit/combined-slip[reforder2·paydeposit]/PCSF-50 → block · ATOMIC CLAIM 2→3 idempotent · refund เฉพาะ WALLET) wire เข้า **ทั้ง 2 pay-on-behalf create path** (WithTopUp gate + OnBehalf per-row) → stale orphan (order fstatus≤5) auto-heal แล้วเก็บต่อ · genuinely-paid≥6/group/credit/PCSF → block+route ไปปุ่มย้อน. `adminReverseForwarderPayment` ไม่ถูกแตะ. [[status-rollback-on-cancel]] (never re-open paid/completed).
> - **🟢 PR178 data-reconcile (applied prod · dry-run+backup):** un-settle 105593 (2→3 · KBANK → ไม่คืน wallet) → owner เก็บ ฿364.82 รวม (52133+52134) ได้เลย (ก่อน deploy ด้วยซ้ำ).
> - **🔴 PR139 stuck-status (applied prod · owner "เก็บเงินไม่ได้ · ดูความถูกต้อง"):** 4 order (52474/75/85/89 · JYM800120650588 box-split) ตู้ **GZE260707-1 มาถึงไทยแล้ว** (พี่น้อง 14 ตัว fstatus=5 · 20 ตัว fstatus=6) แต่ box-split rows ค้าง fstatus=3 (arrival-scan ไม่จับ) → advance 3→5 (priced+measured · money-neutral · stamp fdatestatus4/5). อีก 3 order (52667/52135/52279) ตู้ยัง in-transit ทั้งตู้ → คงที่. 🔴 code root (box-split arrival-scan miss) = carryover.
> - **🔴 MOMO box-count root (owner "ไหนบอกแก้บัคกล่องจบแล้ว ยังเจอ · หัวตาราง/หัวบิล/รายละเอียดแต่ละแทรค ผิด" · `f4193fa1` · #52559 PR086 · MONEY · workflow adversarial-verify):** คนละตัวกับ "กล่อง=0" เมื่อวาน. 2 ราก: **(R1 count SOT)** `isMomoBillHeader` drop หัวบิล เฉพาะ zero-weight → bare base ที่ carry aggregate weight (52559 · 58kg=Σboxes · price 0) ไม่ถูก drop → นับซ้ำ (Σ 11 แทน 4). FIX = money accessor เป็น **SOLE keep-signal** → drop bare-with-siblings IFF money≤0 ไม่สน weight (aggregate ไม่มี freight → drop · anchor money>0 → keep · no-money → legacy zero-weight fallback) · wire money=ftotalprice เข้า**ทุก display/count caller** (per-tracking-editor/forwarder-check/forwarder-volume/momo-container-detail/momo-containers + warehouse + forwarders-table outstanding-over-full-group เหมาๆไม่หาย) = display ถูกไม่ว่าข้อมูลเพี้ยน (กัน recur). **(R2 corrupt detail)** 52436 (-3/4) ได้ค่า aggregate (4/58/986) แทนตัวเอง → `fix-momo-boxcount-corrupt-2026-07-16.mjs` (applied prod · backup · unbilled-only · momo_box_detail=truth) set 1/13.5/0.034048 · re-price 986→229.50 (=twin 52437) · zero 52559. **verify prod Σ=4กล่อง/58kg/0.1841 ตรง MOMO เป๊ะ.** 🔴 review-only 11 ตัว (momo weight มั่ว ×10 · priced-anchor 519218029029 · billed) = owner เคาะ. **➕ SOURCE FIX (`a9825d78` · owner "แก้ต้นตอ กันทั้ง class ไม่ใช่แค่เคสที่สั่ง" · [[fix-root-prevent-whole-class]]):** split เป็น idempotent (มี sibling→skip·ไม่ heal) → base ที่เพี้ยน (partial split/dangling-staging re-commit/manual edit/backfill) ค้างตลอด · ไม่มี path ไหน own "ทำ tb_forwarder ตรง momo_box_detail". **NEW pass 6 SELF-HEAL** ใน MOMO cron (`box-detail-reconcile-plan.ts` PURE 11/11 + `box-detail-reconcile.ts` writer + wire propagate best-effort) — converge ทุก multi-box base ให้ตรง momo ทุกรอบ (zero leftover aggregate bare + fix corrupt detail) · GUARDS: UNBILLED-only · ไม่แตะ priced-anchor · momo corroborate (refuse มั่ว) · bare-zero ต้อง siblings-cover (แยก anchor จริง) · priced ต้องมี twin · idempotent · best-effort. adversarial: heals_muah=false·touches_billed=false. → **corruption จาก path ไหนก็ heal เอง = ทั้ง class เกิดไม่ได้อีก.**
> - **🔴 /admin/incidents "แก้บัคในหน้านี้ให้หมด" (`85a97a8e` · workflow adversarial-verify · tsc0/build0/tests 41+11):** 17 open = ส่วนใหญ่ noise ที่เกิดซ้ำ + 2 บัคจริง. **CODE (หยุดเกิดซ้ำ):** (A) chunk-load = deploy churn (แท็บเก่า+`?dpl=`) → NEW `isChunkLoadError` → client-report skip + error boundaries GUARDED one-time reload (sessionStorage 10s · loop-safe). (B) NEXT control-flow (notFound/redirect/HTTP-error · digest "NEXT_") ถูกจับเป็น failed_action → NEW `isNextControlFlowError` → withObservability RE-THROW ก่อน capture. (C) **wallet-reconcile cron dedup พัง** — `message` ฝัง "Top offenders this run" (แปรผัน) → fingerprint เปลี่ยนทุกรอบ → 6 incidents แทน 1 → FIX message = stable prefix เท่านั้น (offenders คงใน surfaceMeta · READ-ONLY · text-only · money-safe). (D) upload bodySizeLimit ("unexpected response" · driver self-pickup + forwarder exception + forwarder new) → wire `compressImageFile` (existing · maxDim1600 q0.82 fail-soft) + 5MB guard. **DATA cleanup** (`cleanup-incidents-queue-2026-07-16.mjs` · applied prod): 16 noise→ignored · 2 upload→resolved · **live queue → 0**. 🔴 carryover: negative wallet PR130 −646 = accounting reconcile ที่ /admin/wallet (cron จะ re-flag เป็น 1 deduped ถ้ายังค้าง).
>
> **🟡 CARRYOVER (owner เคาะ):** (0) **box-split arrival-scan miss** — MOMO box-split rows (base-N/M) ไม่ได้ advance ตามตู้ที่มาถึง (PR139 case) → fix ที่ arrival-scan ให้จับ box-suffixed tracking. (1) **A7 ย้อนการชำระฝั่งฝากสั่งซื้อ (type=2)** — สร้าง path คืนเงินกระเป๋าใหม่ · ต้อง live wallet-delta test (เลี่ยงตามกฎ "ห้ามทำงานบัค"). (2) **B1 list-void full-unwind** — A2 refuse+ชี้หน้าถอย กันช่องอันตรายแล้ว · ทำ bulk unwind จากลิสต์ = optional. (3) **2 billed-lump แตกไม่ได้:** 800020986676 (PR050) จ่ายแล้ว → ยกเลิก+คืนเงินก่อน (บัญชี) · 1782113771 (PR047)+KY4001024768574 box_detail wt เพี้ยน+dims ไม่ reconcile → ต้อง MOMO Live re-scrape. (4) Standing: MOMO_CRON_AUTOCOMMIT · commit-history WHO (committed_by ไม่ surface · P1 จาก audit) · packing-upload uploaded_by/applied_at ไม่ surface (P2) · DOC BOT slice 2-4 · กระสอบรวม · พี่ป๊อป spec #29.

---

# 🧾 2026-07-15 (เดฟ · owner-driven all-day · Windows → ไปต่อ Mac) — juristic ทุกเอกสาร+editable · เหมาๆ ฿100 per-bill · money-doc chain SOT+COD · sales-rep hollow-fix · PR215/PR217/FRI2606-00006 money · pay-on-behalf→receipt loop · integrate ภูม+ปอน ×4 → ALL 4 BRANCHES · read FIRST

> **🏁 SESSION CLOSE (owner "สรุป · เลินนิ่ง · อัพ memory+เอกสาร · ปิด session · ไปต่อ Mac").** **main = dave-pacred = Poom-pacred = InwPond007 = `865f8f9b`** (ทั้ง 4 sync · Vercel deploy แค่ main = owner "รัน vercel แค่ main ประหยัดเงิน"). resume Mac: `git fetch && git pull origin dave-pacred`. gate ทุก commit: **tsc 0 · build 0** (`NODE_OPTIONS=8192 node node_modules/next/dist/bin/next build` · read BUILD_EXIT · background · `rm -f .next/dev/types/validator.ts` ก่อน). 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`) · pooler aws-1. **ไม่มี migration ใหม่ session นี้ · NEXT FREE = 0256.** ⚠️ owner mode: push **ทุก branch** ตอนปิด (owner สั่ง) · ตอบไทย · money items = dry-run+backup ก่อน --apply.
>
> **✅ SHIPPED (money-review diff เอง · gate 0 · แต่ละอัน owner-driven):**
> - **🔴 juristic ทุกเอกสาร + editable + self-service (PR002/PR079/PR217):** owner "ลูกค้าอัพเกรดนิติทีหลัง → เปลี่ยนหัวชื่อทุกเอกสาร + พนักงานทำเองได้". (1) `adminSetBillingRunBuyerIdentity` (`8b7e7645`) — แก้ผู้ซื้อบน billing-run/[id] + sync ใบเสร็จ (invoice_item.forwarder_id→receipt_item.fid→receipt.rid) · ปุ่ม "ดึงข้อมูลนิติปัจจุบัน" · money-safe (is_juristic ขับแค่ WHT display · invoice คิดสด · receipt PIN frozen → ยอดคงเดิม). (2) **PR002 data-fix** applied prod (3 ใบวางบิล + 1 receipt → บริษัท เดอะ แฟมิลี่ แบทซ์). (3) **self-service** (`2f62fa28`) `actions/admin/juristic-docs.ts` (list + batch re-stamp) + `<JuristicDocRestampPanel>` (ติ๊ก+เลือกทั้งหมด+Apply · ใบตรงแล้วขึ้น "ตรงแล้ว✓") mount upgrade-popup + customer profile. [[juristic-doc-restamp-selfservice]].
> - **🔴 เหมาๆ ฿50→฿100 ครบ loop (MONEY):** (1) `fd6b5160`+`7514e0f5` — **modal/QR/สลิป ฿50→฿100** (forwarder-pay-modal · MAO_FLAT_FEE SOT + isMaoCarrier รับ PRF · completes the server-split fix) · เดิมลูกค้าเห็น ฿50 หลังบ้าน ฿100 = under-collect. (2) **เบิ้ลหัวบิล** dup-header guard บน listEligibleForwarders + createBillingRunInvoiceImpl (เดิมมีแค่ createForwarderOrderBill) · mao ย้ายไปแถวรอด. (3) **per-bill rule (`d06a41d5` · owner เคาะ):** computeForwarderDebitBatch anchor เดียวต่อ batch (เดิมต่อ container) → ฿100 ครั้งเดียวต่อบิล = ตรง collect-total · test 81. (4) **FRI2606-00006 (PR7429) reconcile** applied prod (`c4ac5596`) — mao ฿100→฿50 + WHT → บิล=ใบเสร็จ=฿2,085.93 (=ที่จ่ายจริง). [[momo-truth-hierarchy-and-mao-key]] (per-bill supersedes per-container).
> - **🔴 money-doc chain SOT + COD + linkage (`90a057a7` · workflow 6-agent verified prod):** ใบแจ้งหนี้(เงินสด)/ใบวางบิล(เครดิต)→ใบเสร็จ→ใบส่งของ ตรงกัน. **F1/F2 COD double-charge (MONEY):** computeForwarderDebitBatch + auto-issue-receipt เดิมบวกค่าส่งในไทยแม้ COD (paymethod=2) ทั้งที่ outstanding.ts + collect-total ตัดแล้ว → เพิ่ม `domesticLeg = paymethod===2 ? 0 : ftransportprice` + thread paymethod ทุก caller · test 91. **F5/F6** cash summary → resolveBillingIdentity · **F4** → computeBillWht · **F9/F10/F11** cross-link (forwarder detail "เอกสารของออเดอร์นี้" · receipt backlink · delivery note ref). [[money-doc-chain-cod-and-linkage]].
> - **🔴 pay-on-behalf → receipt loop (`282800c3`+`51201a7d`):** pay-user history รายการอ้างอิงกดเข้า order (ฝากนำเข้า→forwarder detail · ฝากสั่ง→shop) + **`adminIssueReceiptForForwarder` + `<IssueReceiptButton>`** — ออเดอร์ paid(fstatus≥6) ไม่มีใบเสร็จ → "＋ออกใบเสร็จ" (reuse autoIssueReceiptOnPaymentLand idempotent · money-safe). loop: จ่ายแทน→reforder→forwarder→ออกใบเสร็จ.
> - **🔴 sales-rep hollow-fix (`fe89ca03` · applied prod):** เซล ลูกนัท(admin_looknut)+แบม(admin_bam) สร้าง role=normies → ensureLegacyAdminRow mirror ล้มเหลว → ไม่อยู่ tb_admin → sales-roster (SOT: adminStatusA='1' AND adminStatusSale='1') มองไม่เห็น → เลือกไม่ได้. FIX: data (สร้าง tb_admin + bridge legacy_admin_id · roster 3→5) + code (listStaffSalesFlags **union modern staff hollow** → โผล่บน sales-team ให้ติ๊ก · toggle bridge). [[sales-rep-tb-admin-pool-hollow-fix]].
> - **🔴 PR215+PR217 money (applied prod · dry-run+backup):** **PR215/52328** ลืมเก็บค่าตีลังไม้ ฿1,900 → crate 0→1,900 · gross 9,293.44 · net 9,200.51 · บันทึก 2 สลิปเพิ่ม (1,808.79+72.20=1,880.99 · ...ATF07185/09502) · รวมจ่าย 9,200.50 ✓. **PR217** จ่าย 3 ออเดอร์เต็ม gross ก่อนนิติ → **คืน 1% เข้า wallet ฿273.20** (64.23+103.86+105.11 · type=5 · ไม่ผิด ¥-as-THB แบบ PR075). [[juristic-wht-refund-and-forgotten-crate]].
> - **จังหวัดปลายทาง lock auto (`ff281562`):** EditShipByField มีที่อยู่จัดส่ง(จังหวัด)แล้ว → lock read-only 🔒 (เดิม select ให้เลือกซ้ำ) · เหลือ select เฉพาะแถวไม่มีที่อยู่. **ลบ label "PCS ·/ส่งฟรี"** ใน dropdown → Pacred เหมาๆ ฿100.
> - **integrate ภูม+ปอน ×4 รอบ:** ภูม (MOMO ตรวจตู้ กางกล่องย่อย 1:1 + flag "MOMO มั่ว" + receipts print ต้นฉบับ/สำเนา + refwhid button · รัน **backfill refwhid 25 ใบ prod**) · ปอน (report-cnt/wallet display · sales-team ลิงก์พนักงานจริง) · **ชนจริง 1 จุด `momo-containers-client.tsx` import → เก็บทั้ง type ReactNode(ภูม)+carrier FILE-ONLY(ปอน) · owner "ชนจริงเลือกปอน"**.
>
> **➕ CONTINUED (same session · resume จาก limit · pushed):**
> - **🔴 PR211+PR143 slip-split balance (`7d500be5` · MONEY · applied prod):** ลูกค้าโอนรวมสลิปเดียว แต่ตรวจแยกต่อแทรค → งงยอด+เผลอแก้ผิด. PR211 f52312 เหมาๆ ฿50 (ftransportprice=50 → virtual mao ไม่ fire · collect 189.20+50=239.20=slip). PR143 105603 amount 177.00(แก้ผิด)→144.69 (freight 94.69+เหมาๆ 50) · 52119 mao anchor · รวม 226.89 · เหมาๆ ฿50 ครั้งเดียว (ลอบเดียว GZS260626-1). ⚠️ 2 order ออกใบเสร็จ*ด้วยกัน*. [[combo-flow-carry-not-rederive]].
> - **🔴 pay-on-behalf ย้อนการชำระ + ถอยสถานะ (`03ad2dde` · PR178 · MONEY · workflow-verified):** owner "ยกเลิกใบเสร็จแล้ว แต่ยังบอกชำระแล้ว · สถานะไม่ยอมถอย · ไม่มีปุ่มดูเอกสาร". **`adminReverseForwarderPayment(fid)`** — REFUSE fstatus≥7 · resolve settled funding row (typenew∈{5,6} status='2' reforder=fid) · cascade guard (reforder2/tb_wallet_paydeposit → REFUSE) · ATOMIC CLAIM status 2→3 · refund wallet ONLY if depositnamebank='WALLET' · forwarder revert mirror reject branch · gated super/accounting. + PayUserReverseButton (§0f) · reforder clickable Link · doc-pills (F5) · refund-rebill-guard `.neq("status","3")` (reversed pay ไม่ block re-bill · test 6/6). [[status-rollback-on-cancel]] (never re-open paid/completed).
> - **🟢 PR075 คืนเงิน = reconcile ตรง ฿0 (owner "191,000 vs โอน 190,995 · เงินหายไหน · ระบบทำ payout ได้ยัง"):** ตรวจ prod — **ไม่หาย** (191,000 = rounding display · จริง 190,995 = refund 154,173 + order 36,822 ตรงเป๊ะ · ไม่ใช่ ¥-as-THB แบบเดิม). **payout มีแล้ว:** `adminCreateWalletHsManual` kind='withdraw' หัก wallet + แนบสลิป ที่ `/admin/wallet/add` → owner กดถอน 154,173 พร้อมสลิปที่ลูกค้าแนบ (ประวัติครบ · ยอดเต็มเดิม). [[cost-editable-sell-locked]] (payout capability).
> - **🎨 สีสถานะ LOUD "แสบตาแบบ PCS" (`865f8f9b` · display-only · reverses 19/6 soften):** FSTATUS_CFG.chip + HSTATUS_CFG.chip soft bg-{hue}-100 → solid bg-{hue}-500/600 text-white font-bold (match FSTATUS_VIVID) · rowBg -50→-100 · actionable 4/5/6 เข้มสุด · สถานะที่ต้องมอง/กด เด่น หาง่าย. ⚠️ compile-verified (tsc 0/build 0) · ไม่ได้ authed-render (ไม่มี test admin login).
> - **🔧 box-count "0 กล่อง" (`865f8f9b` · owner "บัคเรื่องกล่องบ่อย · จะเก็บเงินยังไง"):** billable tb_forwarder famount=0 (MOMO box rows ที่ box-count ไม่ landed) → backfill famount จาก momo_box_detail.quantity (else 1) · famount DISPLAY-ONLY (ไม่อยู่ใน bill formula) = money-safe · applied prod 4 แถว JYM800120650588 (#52474/75/85/89 · 0→1). 🔴 code root (display fallback จาก box_detail กัน recur) = carryover.
>
> **🔴 CARRYOVER (owner เคาะ · ยังไม่ทำ):** (1) **COD historical over-collection** — ใบเสร็จ COD เก่า (FRG2606-00002/FRG2607-00008) เคยคิดค่าส่งในไทยซ้ำ → บัญชี refund/credit (code กันไปข้างหน้าแล้ว · สุ่มตรวจ 2-3 ตู้). (2) **F8** receipt CHN/TH itemization (ต้อง migration tb_receipt cols) · **F7** per-line frozen amount (presentation call) · **F11** ใบส่งของ ship-to source (delivery_address vs faddress*). (3) PR215/PR217/PR178 กด "＋ออกใบเสร็จ" ให้ครบเอกสาร (owner/staff คลิก) · PR075 owner กดถอน 154,173 ที่ /admin/wallet/add. (4) box-count code root (display fallback จาก box_detail กัน recur) · combined-slip grouping (review/confirm/receipt สลิปเดียวด้วยกัน · เหมาๆ ฿100 ครั้งเดียว). Standing: bill↔receipt PART2 WIP patch · packing manual (cross-container/orphan/MISSING) · ขนส่ง "ลูกค้าเรียกรถเอง" channel · MOMO_CRON_AUTOCOMMIT.

---

# 🧾 2026-07-14 (เดฟ · owner "ทำไมยังเกิดอีก" · MOMO แตกกล่อง recurring) — split scrape-scope ROOT-FIX + reconcile 21 dup vs box_detail + repair 21 dangling staging + backfill 3+1 + packing-gate fix + integrate น้อง → MAIN · read FIRST

> **🏁 STATE. main = dave-pacred = `<HEAD>`** (push · BUILD_EXIT=0 · tsc 0 · Vercel deploying prod). 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`). **NEXT FREE mig = 0256** (0254/0255 = ภูม momo_packing_upload · applied prod+dev · ไม่มี mig ใหม่ session นี้). prod DB pooler = **aws-1** (aws-0 พังตลอด — reassign มี multi-host fallback แล้ว) · direct `db.<ref>.supabase.co:5432` ก็ใช้ได้.
>
> **🟢 SESSION-CLOSE ADDITIONS (owner "ไม่ต้องยกเลิกบิล · fill ใส่ในใบเลย · ตรวจงานน้อง · ขึ้น main · ปิด session"):**
> - **billing packing-gate FALSE-BLOCK (`ed15d61f`):** owner วางบิลไม่ได้ · "ตู้ยังไม่อัพ packing list #52577" ทั้งที่อัพแล้ว. banner บอกอัพที่ `packing-upload` (momo_packing_upload · mig 0254) แต่ gate อ่านแค่ `container_packing_reconcile` (mig 0245) = คนละตาราง. FIX `lib/admin/packing-confirmed-cabs.ts` — packing-confirmed = มี stamp reconcile OR อยู่ใน momo_packing_upload · union · wire ทั้ง display + server gate. pure-validation.
> - **800206224068 (PR079) แตก 8 กล่อง via dims-fallback (owner "fill ใส่ในใบเลย ไม่ต้องยกเลิก"):** invoice FRI2607-00061 ถูก cancel ไปแล้ว → revert 52305 fstatus 5→4 (ไม่มีบิล active · ปลอดภัย) → `split-aggregated-momo-boxes --priced --apply`. box_detail น้ำหนักเพี้ยน (218 vs 249) แต่ **dims-volume reconcile (Σ ก×ย×ส=0.4269=aggregate)** → plan ใช้ dims-fallback allocate weight/cbm proportional → **Σ ตรงเป๊ะ (13ชิ้น·249kg·0.4269คิว·฿4980) · 8 rows fstatus=4 พร้อมวางบิล**. owner กด "สร้างใบวางบิล" PR079 → โชว์ 8 กล่อง. **800020986676 (PR050) = billed PAID (FRI2607-00054) → ไม่แตะ · ยกเลิก+คืนเงินก่อน = บัญชี.**
> - **integrate น้อง → main:** Poom-pacred (MOMO ตรวจตู้ Slice 2 เทียบ packing vs API + modal-to-body) · InwPond007 (workspace booking journey ตาม Incoterms + FCL picker) · merge สะอาด 0 conflict · ไม่มี mig ใหม่ · tsc 0 · BUILD 0.
>
> **🔴 owner "แทรคกิ้งไม่คลี่ออก ยอด/น้ำหนักแต่ละกล่องไม่แตก ทำไมยังเกิดอีก · ตั้ง env ครบแล้ว · ภูมบอกแก้หมดแล้ว" → เจอ 3 ราก (ไม่ใช่ migration/env/ภูม-พลาด · เป็นคนละรู):**
> - **(1) split scrape-scope ROOT-FIX (`a4e22e92` code · deploy):** box-split pass 5 เดิม derive candidate จาก `scrapedBases` เท่านั้น (base ที่ MOMO boards คืนบน scrape ปัจจุบัน). MOMO คืน parcel เฉพาะสถานะแรก → พอเดินสถานะ base ตกจาก scrapedBases → split ไม่แตะอีก **ทั้งที่ `momo_box_detail` มีกล่องครบแล้ว** (pass 3 เก็บ) → aggregate ค้าง 1 แถว → บางตัวถูกวางบิลเป็นก้อน. FIX: `findMultiBoxBases()` ดึง base >1 กล่องจาก box_detail (durable) → pass 5 union scrapedBases∪durable. + fix script bug `adminidupdate specified more than once` (raw SQL dup col · CLONE_OMIT). live path ปลอดภัยผ่าน object-spread.
> - **(2) reconcile 21 dup/stale vs box_detail (`e2ac5513` · MONEY-CORRECTING):** re-sync 07-09 (ก่อน Fix F 07-13) สร้างแถวซ้ำ+stale aggregate 19 base · บางตัวซ้อน 2-3 เท่า (1783582423: 24แถว/Σ2994kg vs MOMO 23กล่อง/Σ2007). `scripts/reconcile-momo-dup-rows-2026-07-14.mjs`: normalize ftrackingchn (ตัด /n)→group ต่อกล่อง→เก็บ 1/กล่อง (weight ตรง box_detail→staging_ptr→newest)→ลบเกิน · **INVARIANT: survivors นับ+Σ === box_detail หรือ exact-dup ทุกตัว ไม่งั้น flag** · MONEY GUARD ลบเฉพาะ unbilled · driver refs=0. → ลบ 21 แถว/19 base (ทุก base PASS·0 flag) · backup+txn.
> - **(3) repair 21 dangling staging (= เครื่องปั๊ม dup ใหม่):** `momo_import_tracks.committed_forwarder_id` 21 ตัวชี้ row ที่ถูกลบไปแล้ว (backfill 07-13 ลบ 19 + PR148 52461) → sync ถัดไป re-commit สร้าง dup ใหม่. `scripts/repair-momo-staging-danglers-2026-07-14.mjs` re-point ทั้ง 21 ไป survivor (norm box-match→base-anchor) · money-neutral. **dup=0 · dangling=0 · Σ ตรง MOMO ทุก base.**
> - **backfill แตก 3 (money-neutral):** 760235240370(PR079 Σ610)·KY982669997(PR145 Σ2054.36)·LJ20479565(PR9903 Σ885.05) via `split-aggregated-momo-boxes --priced --apply`.
> - **reassign PR6166→PR600 (`d3abc348`):** แก้ crash (mover connect aws-0 · prod=aws-1 → multi-host `connCandidates` + wrap ไม่ throw) + เปิดให้ **super** ทำได้ (เดิม ultra-only) · ย้าย 5 แถว/5 ตาราง · ⚠️ button ต้องมี `SUPABASE_DB_PASSWORD` ใน Vercel (ตั้งแล้ว).
>
> **🟡 CARRYOVER (owner เคาะ):** (1) **1782113771 (PR047):** box_detail น้ำหนักไม่ตรง aggregate → guard ป้องไม่แตก · ต้อง refresh box_detail จาก MOMO Live (per-piece/total inconsistency เดิม). (2) **800206224068(PR079)·800020986676(PR050):** วางบิลไปแล้วตอนก้อนรวม → guard block · จะแตกต้องยกเลิกบิลก่อน (owner-decision). (3) Standing: MOMO_CRON_AUTOCOMMIT ON แล้ว · DOC BOT slice 2-4 · กระสอบรวม · พี่ป๊อป spec #29.

---

# 🧾 2026-07-13 (เดฟ · owner-driven ต่อ · resume จาก limit ×2) — USD ล้วน + ที่อยู่ช่องเดียว + cargo auto-loop 6 money-fix + bill↔receipt PART1 address + MOMO box-split allowPriced + backfill 6 ตู้ +฿50,811 + DB sync prod→dev + integrate น้อง → main · read FIRST · 🔴 PART2 money = WIP

> **🏁 SESSION CLOSE (owner: "หยุด PART2 · สรุป · ปิด session · อย่าให้ติด limit" · 5-hr limit 96%).** **main = dave-pacred = `<HEAD>`** (push per-commit · verified ทุก commit BUILD 0 + tsc 0). resume: `git fetch && git pull origin dave-pacred`. NEWEST mig **0253 (tb_receipt.delivery_address · applied prod+dev)** · **NEXT FREE = 0254** (0254 = billing_invoice_item_amount_override อยู่ใน PART2 WIP patch · ยังไม่ apply). 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`). ⚠️ **บทเรียน: ห้ามรัน 2 workflow แก้ tree เดียวกันพร้อมกัน** (cargo+bill-receipt ชนกัน billing-run.ts รอบนี้ · โชคดีไม่ clobber แต่เสี่ยง) + **ห้ามรัน 2 build ซ้อน** (concurrent-lock = false BUILD_EXIT=1).
>
> **✅ SHIPPED main (verified · money-diff + BUILD 0):**
> - **USD ล้วน (`d8607378` · P22353):** งานเปิดสกุลไหน admin surface โชว์+กรอกสกุลนั้นล้วน (items-editor/edit/legacy-view/shop-fields-board · เรท 34.53 บาท/USD) · ¥ pricing เก็บเงียบเหมือนเดิม (CNY byte-identical) · shared `lib/forwarder/usd-order-pricing.ts`.
> - **ที่อยู่ช่องเดียว (`1c99c34d`):** billing-run-paper สลับที่อยู่จัดส่ง → แทนที่ในช่อง "ที่อยู่" เลย ไม่แยกหัวข้อ.
> - **🔴 cargo auto-loop 6 money-fix (`810dbee8` · workflow audit+adversarial · owner MOMO_CRON_AUTOCOMMIT ON):** B1 เหมาๆ N×฿100 เกิน→฿0 PRF-row (mao once) · D1 COD คิดค่าส่งซ้ำในบิล→outstanding.ts domesticLeg=paymethod2?0 · C1/C2 direct-bill (forwarder-check/report-cnt) ไม่มี ฿0/th-ship gate · D2 Flash COD export กลับด้าน · A2 re-price หลังปิดตู้ · D3 ลบ dead-code forceCod landmine.
> - **🔴 bill↔receipt PART 1 address (`85060c40`+`9861643a` · mig 0253):** แก้ที่อยู่จัดส่งบิล → propagate ทั้งใบวางบิล+ใบเสร็จ (tb_receipt.delivery_address · single-slot rule · non-money ทำบน paid ได้). prod #61 หายเพี้ยน.
> - **MOMO box-split allowPriced (`85d61df2` · 52305):** propagate pass-5 split เป็น allowPriced=true → กด "อัปเดตสถานะ" ครั้งเดียว refresh box_detail + split priced-unbilled aggregate เป็นกล่องยิงได้ · money-neutral (Σ preserved) · **จำนวนกล่องแยกถูกต่อแทรคกิ้ง (test จริง 52305: base 13→3·-5=4·Σ=13 · owner เตือน verified)** · ⚠️ box_detail น้ำหนักรายกล่อง inconsistent (base per-piece·-5 total) → count+เงินถูกเสมอ แต่น้ำหนักรายกล่องเพี้ยนจน scrape สด (follow-up).
> - **🔴 MOMO backfill 6 ตู้ under-bill (`d88ff8d3` · MONEY · owner เคาะ):** ตรวจงาน ภูม เจอ handoff SQL หาเหมารวม 21 แถว **ปลอดภัยจริงแค่ 6** (classify: box_detail corroborate aggregate) · 15 danger (agg=0/agg<cur/stale = re-value จะทำลายบิล) → apply 6 (re-value+re-price via proven engine) **Σ ฿20,271→฿71,083 (+฿50,811 ที่คิดขาด)** + paymethod normalize. **บทเรียน: MOMO aggregate เชื่อไม่ได้เสมอ · corroborate box_detail ก่อน.**
> - **DB sync prod→dev (`1a63e0d8`):** upsert 28 ตาราง business/pricing/doc prod→dev (น้อง repro บัคได้ · forwarder 166→569 · invoice 21→86 · receipt 3→65 · momo · rate cards) · dev-only rows ของภูมรอด · **แยก login lane (ไม่แตะ auth/profiles/admins · owner "เส้นใครเส้นมัน")** · prod ไม่ถูกแตะ · re-runnable.
> - **integrate น้อง:** ปอน our-work Trip.com layout · ภูม MOMO dashboard 3-กลุ่ม+วิธีใช้ · งาน parallel-session (status-rollback systemic · USD typed-rate mig 0252).
>
> **➕ ต่อ (resume · owner "ค่อยๆ ทำ ไม่ push จนกว่าบอก"):**
> - **🔴 double-count trap แก้แล้ว (my error · [[momo-backfill-check-siblings-first]]):** backfill ผมเมื่อเช้า re-value base ขึ้นเป็นก้อนรวม **โดยไม่เช็ค tb_forwarder -N siblings** → 52095/52109/52115 (3 ใน 6) base+siblings นับซ้ำ (52095 Σ 1388 vs จริง 971) = over-bill. รัน **ภูม's `fix-momo-cargo-rows-2026-07-13.mjs`** prod (ตรวจ+dry-run+backup ก่อน · แก้ type bug ::text[]→::bigint[]): PHASE1 ลบ 19 dup · PHASE2 แก้ 11 double-count (base-=Σsib · money-neutral) + re-price · **20 flag ต้องแต้ม** (MOMO เลขขัดกันเอง 52137 sib 19,991>base 150). "+฿50,811" ที่เคลม บางส่วน = double-count ไม่ใช่ under-bill จริง.
> - **✅ PR075 / P22343 refund ¥-as-THB — แก้แล้ว (`fix-p22343-refund-rate-2026-07-14.mjs` · applied prod · backup):** refund 12 รายการ ทำ 2026-07-10 **02:50 ก่อน fix ¥×hrate 18:02** → คืน **¥ เป็นบาท 1:1** (ของที่ลบ ¥29,940 → คืน ฿29,940). ที่ถูก = **¥29,940 × hrate 5.10 = ฿152,694** → **top-up ฿122,754** · wallet ฿29,940→**฿152,694** ✓. + **header ไม่เคย recompute** → htotalpricechn ¥37,160→**¥7,220** (Σ live) · htotalpriceuser ฿161,055→**฿38,301** (canonical formula). ⚠️ บทเรียน: ตอนแรกผมสรุปผิดว่า "เรท 5.0 ขาด ฿598" เพราะ **camount ถูก zero หลัง refund** → อ่าน ¥79×qty5=395 เป็น ¥79×1×5.0. **ต้องอ่าน note (จำนวน N) หรือ backup ก่อนสรุปเรท.**
> - **USD 35.0 verified:** husdrate เขียน typed rate verbatim (header-edits:387) → 35.0 ไม่ใช่ 35.006. USD order เก่า 2 อัน husdrate null (pre-fix · re-derive) · order ใหม่เก็บถูก.
> - **integrate:** ปอน booking mockup/pricing hub · ภูม MOMO sync button + จำนวน display + fix script (no migration).
>
> **➕ 2026-07-14 ต่อ (owner "ลุยครบ · เข้าทุก branch"):** **packing-ingest 21 ตู้แต้ม → tb_forwarder current** (`e7b7db4f` · applied prod · real parser ported + selftest · per-box ห้าม fan · skip billed · backup · **PR067 1782555393 20,141→5,780kg garbage แก้ · re-price 55 · ตู้จริงผูก**) · **เหมาๆ ครั้งเดียว/ลอบส่ง (`717913a7` · MONEY):** deliveryKey=fcabinetnumber → 52118+52119 ตู้เดียว ฿200→฿100 · คนละตู้ยังแยก · thread เข้า debit/invoice/pay-user/receipt → บิล=ใบเสร็จ=เก็บจริง · **box-count 74→75** (กล่องมีราคาไม่ถูก drop · display) · backfill double-count 30 ตู้ (ภูม script · type-fix) · DB sync prod→dev · integrate ภูม Poom-pacred (MOMO ตรวจตู้ A-1/2/3). build 0 · money-review PASS. **🔴 InwPond007 (ปอน) conflict** = ภูม↔ปอน rewrite MOMO import UI (momo-containers-client.tsx · 8 region) → ต้อง integrate ให้ถูก (ไม่ rush · test UI ไม่ได้). **🔴 packing carryover ทำมือ:** cross-container 7 (PR075/PR179 boxes ข้ามลอบ) · orphan placeholder 7 · MISSING 18 (4 tracking จริง).
>
> **🚚 ขนส่งเอกชน = province-aware · FILE-ONLY (`dab9b507` · owner "อิงไฟล์ PCS · ห้ามใส่นอกไฟล์"):** SOT `lib/forwarder/carrier-province-coverage.ts` (generated จาก `บริษัทขนส่ง_พื้นที่ขนส่ง(จังหวัด).xlsx` · **28 เจ้า · 77/77 จว. · 0 unresolved** · `scripts/gen-carrier-province-coverage.mjs` = owner แก้ไฟล์แล้วรันใหม่ได้ · typo auto-fix ศรีสระเกษ→ศรีสะเกษ/โคราช→นครราชสีมา/อยุธยา→พระนครศรีอยุธยา · ขยาย ทุกจังหวัด→77 · ภาคอีสาน→20 · เก็บ note ข้อจำกัด). **UNION→FILE-ONLY**: ลบ `PROVINCE_RULES` เดิม (typo-poisoned → **พระนครศรีอยุธยา/ศรีสะเกษ/เพชรบุรี/เพชรบูรณ์ ไม่เคย match เลย** · J.K.+อาร์.ซี. unreachable = บั๊กเงียบมานาน). **SERVER GUARD** `carrier-coverage-guard.ts` wired 9 write-path (field-edits/legacy/create/manual/carrier-manual/cart/MOMO-commit/order-shipby/spawn) refuse ขนส่งนอกไฟล์/ไม่ครอบคลุมจังหวัด · exempt own-fleet PCS/PCSF/PCSE + ว่าง + carry. ตัด 16 id (DHL/Kerry/Nim/ไปรษณีย์ + 12 legacy-only) · ลบ free-text ทุกหน้า · picker = closed select + โชว์ข้อจำกัด. build 0 · guard 63/0 · ship-by 39/0 · coverage 48/0.
> **🔴 3 เรื่องรอ owner เคาะ (ขนส่ง):** **(a) "ลูกค้าเรียกรถเอง" บันทึกไม่ได้แล้ว** — prod มี `เรียกรถขนส่ง`×5 · `รถรับจ้าง` · `บุ๊ครถมารับ` · `เรียกรถตู้ทึบ` = **channel ไม่ใช่ carrier** → ต้องเพิ่มตัวเลือกไหม (ไม่ขัดกฎ "ห้ามใส่ขนส่งนอกไฟล์"). (`สมใจสาย4`×19 · `PL` · `เคพีเอ็น` · `flash expess` = อยู่ในไฟล์แล้ว แค่เคยพิมพ์เอง ✓). **(b) ข้อจำกัดระดับอำเภอ = advisory ไม่ block** (ไฟล์มีแค่ระดับจังหวัด · ยะลา/เบตง จะขึ้นหาดใหญ่ทัวร์ + โน้ต "ไม่ไปเบตง") → อยาก block จริงต้องเพิ่มคอลัมน์อำเภอในไฟล์. **(c) 204/551 แถวไม่มีจังหวัด** (tb_address ขยะ "NY"×219 spam) → guard บังคับ closed list ได้ แต่เช็คจังหวัดไม่ได้.
>
> **🔴 CARRYOVER (owner):** **(1) bill↔receipt PART 2 (money · CORE complaint "แก้บิลถูก ใบเสร็จก็เป็นอีก")** = implemented แต่ STOPPED mid-verify → WIP patch `docs/wip/part2-bill-receipt-money-liveview.patch` + handoff `docs/wip/README-part2-bill-receipt-money.md` (money live-view→freeze→pin · G1 preserved · resolver + drift banner + mig 0254) — **apply+verify+money-review ก่อน push (unverified money code · ห้าม push จน build 0 + G1 pass).** (2) MOMO box_detail per-piece/total inconsistency (น้ำหนักรายกล่อง). (3) 15+20 flag rows = โกดัง/แต้ม verify. (4) owner กด "อัปเดตสถานะ" หน้า MOMO Live เพื่อ split 52305 + rows อื่น. Standing: reprice-stale-cargo · #52194 PR067 · #52089 · RECEIPT_TOKEN_SECRET Vercel.

---

# 🧾 2026-07-13 เย็น (เดฟ · resume ต่อจาก limit) — 🔴 SYSTEMIC status-rollback-on-cancel (5 gaps · workflow-audit) + USD เรท 35→35.006 (mig 0252) + integrate ภูม box-split + กู้ PR9468 → push dave-pacred ONLY · read FIRST

> **🏁 CLOSE (owner: "ต่องานค้าง · เมลหลักติด limit · จบแล้ว push แค่ dave-pacred").** **dave-pacred = `8efb463a`** (main ไม่ promote — owner สั่ง push แค่ dave-pacred). ⚠️ `.env.local` SUPABASE_DB_PASSWORD STALE — prod `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`). **NEWEST mig 0252 (husdrate) applied prod+dev · NEXT FREE = 0253.** gate: `rm -f .next/dev/types/validator.ts` ก่อน · NODE_OPTIONS=8192 build (อ่าน BUILD_EXIT จริง · ⚠️ redirect log path พังเคยให้ exit-1 หลอก · re-run clean) · tsc 0 · commit heredoc `cat > f <<'EOF'`. ไม่มี admin login ของเว็บเรา → verify ผ่าน code audit + prod DB query + gate (live-test ไม่ได้). PCS PHP รัน local ([[pcs-legacy-local-run]]).
>
> **✅ งาน resume นี้ (push dave-pacred `16e9193f`→`8efb463a`):**
> - **integrate ภูม MOMO box-split + COD** (`46021ac1` · money-safe · merge สะอาด) — งานค้างจริงตอนติด limit.
> - **🔴 SYSTEMIC status-rollback-on-cancel** (owner: "สถานะขยับแล้วยกเลิก ต้องถอยตาม flow เส้นตรง · plain edit = live") — **Workflow audit 5 surface · 13 agent · adversarial-verify → 4 gap + billing (5 จุด):** billing cancel/void(unpaid)→forwarder `6→5` (`209181e3`) · delete-forwarder→`advanceLinkedShopOrder` re-derive shop `40→4` · bulkCancel **REFUSE credit row** (กัน orphan AR) + unwind open driver stops · reject type='8' slip ตอนยกเลิก (`8efb463a`). **guard: ไม่ re-open paid/completed** (advanceLinkedShopOrder no-op ที่ hstatus∈{5,6,99} · billing revert เฉพาะ paid_at null · credit=refuse). + **data-fix PR9468 5 แถวค้าง 6→5** (เปิดบิลใหม่ได้). learning [[status-rollback-on-cancel]].
> - **USD เรทขาย 35→35.006** (`f056aeae` · mig 0252) — root: header เก็บแค่ hrate 2dp → re-derive เพี้ยน. Fix: `tb_header_order.husdrate` เก็บเรทที่พิมพ์ verbatim (display-only · hrate=money basis เดิม · ¥ order byte-identical). editor 2 ตัว (items rate + inline pencil). learning [[foreign-currency-order-rate]].
> - **Bug 1+2 (คืนเงิน ¥×rate + ราคาสินค้าไม่อัพเดท)** — verify code = ถูกแล้วตั้งแต่ 2026-07-10 (`45842dee` · refund.ts ×orderHrate + recompute · P22343 ที่เห็น = ซากข้อมูลก่อน fix ไม่ใช่ live bug).
>
> **🔴 CARRYOVER (owner เคาะ):** (1) **credit-withdraw flow** — ตอนนี้ bulkCancel "ปฏิเสธ" รายการเครดิต (safe); ถ้าอยากยกเลิกได้เลย = ทำ flow ถอนวงเงิน AR (decrement tb_credit + fcredit='' + fstatus→5). (2) ภูม box-split **backfill ตู้เก่า under-bill** ([`docs/handoff-dave-2026-07-13-momo-boxsplit-cod-backfill.md`](../../docs/handoff-dave-2026-07-13-momo-boxsplit-cod-backfill.md) · re-value ขึ้นบิล · owner เคาะ Σ delta). (3) P22343 stale header/wallet (บัญชีตรวจ). (4) MOMO_CRON_AUTOCOMMIT flip (Vercel).

---

# 🧾 2026-07-13 บ่าย (เดฟ · resume ต่อจาก limit) — integrate ภูม MOMO box-split + COD gate เข้า dave-pacred (merge สะอาด · gate เขียว) → push dave-pacred · read FIRST

> **🏁 CLOSE (owner: "ต่องานค้าง · เมลหลักติด limit · จบแล้ว push dave-pacred").** งานค้าง = **integrate `Poom-pacred` (ภูม MOMO box-split + COD) เข้า dave-pacred** (เดฟ main-session merge ค้างตอนติด limit). merge สะอาด 0 conflict → `46021ac1`. gate: tsc 0 · domestic-shipping COD test ผ่าน · momo-raw 180/0 · **build EXIT 0** (⚠️ รอบแรก redirect log พัง exit-1 หลอก · re-run clean = 0). push **dave-pacred** (+ ตามที่ owner สั่ง). **main ยังไม่ promote** (box-split เป็น money-going-forward · owner เคาะก่อนขึ้น prod ถ้าต้องการ).
>
> **✅ ภูม `9f329765` ที่ integrate (money-safe · verify DEV โดย ภูม):** (1) **box-split** — commit valuate จาก aggregate columns (แก้ under-bill ~5×) + split-at-commit (แตก N แถวตั้งแต่ดึงเข้า) + เอาปุ่ม "แตกกล่อง" ออก (`commit-momo-row-core.ts`). (2) **COD ค่าส่งไทย gate** — ระดับชิปเมนต์ (sibling COD → ยกเว้นทั้งกลุ่ม · PURE validation ไม่แตะ pricing · `domestic-shipping.ts`). billing-run +13 (ปลด COD bill). 6 ไฟล์ ไม่ทับ 3 commit ใหม่ dave-pacred.
>
> **🔴 CARRYOVER — owner เคาะ (money · ผมไม่รันเอง · [`docs/handoff-dave-2026-07-13-momo-boxsplit-cod-backfill.md`](../../docs/handoff-dave-2026-07-13-momo-boxsplit-cod-backfill.md)):** (1) **backfill BUG-1** — ตู้เก่า under-bill ยัง priced บนกล่องแรก (เช่น 52305 `800206224068` PR079 · 46.5→249kg = ขาด ~5×) → re-value(aggregate)+re-price(`computeAndFillForwarderImportRate`)+split(`allowPriced:true`) · fstatus 1-4 · **dry-run + owner เคาะ Σ delta ก่อน --apply** (ขึ้นบิล). (2) **BUG-2** paymethod normalize พี่น้อง COD (optional · ไม่ขึ้นบิล). (3) cron `liveBoxSplit allowPriced` mass-auto (owner option · ไม่ flip).

---

# 🧾 2026-07-13 (เดฟ · owner-driven · ส่งงาน cargo เช้า) — MOMO flow อุดรูรั่ว (งานหาย/สถานะเด้ง/เงินขาด/เครดิตซิงค์) + USD editor + ค่าส่งไทย Flash/COD + DOC BOT+ไฟล์ HS 5,335 · กู้จาก session-limit ครบ → ALL 5 BRANCHES · read FIRST

> **🏁 SESSION CLOSE (owner: "ต่อ closeout · ปิด session · เดี๋ยวไปทำต่อคอมที่ทำงาน").** **main = dave-pacred = Poom-pacred = InwPond007 = web-pacred = `20a3d1b9`** (all synced · main→Vercel prod). gate: **BUILD_EXIT=0** (read from log · `NODE_OPTIONS=8192 node node_modules/next/dist/bin/next build` · ⚠️ ห้าม build ซ้อน). **NEWEST mig 0251 (doc_bot_hs_source) · NEXT FREE = 0252** (0250 booking_pricing_catalog ปอน · 0251 doc_bot source col). 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`). resume คอมที่ทำงาน: `git fetch && git pull origin dave-pacred`.
>
> **🔴 OWNER LEVER (พี่กดเอง 1 คลิก · dup-safe แล้ว):** ตั้ง **`MOMO_CRON_AUTOCOMMIT=true`** ใน Vercel → MOMO auto สร้าง billable `tb_forwarder` ทันทีที่ของถึง (กัน "งานหาย" ที่ parcel หลุด `import/track` feed ก่อนมีคนกด /review — MOMO feed คืน parcel เฉพาะ status แรก). Fix F (`09012681`) อุด dup ที่ chokepoint แล้วจึงเปิดปลอดภัย.
>
> **✅ MOMO/cargo flow อุดรูรั่ว (owner "MOMO ปล่อย API 100% แล้ว · อยู่ที่เรารับตก · งานหาย/สถานะไม่เดิน/เงิน+ขนาดกล่องไม่ซิงค์ · อุดทั้ง flow ของถึง MOMO→รับ→สแกน→ตรวจตู้→วางบิล→ชำระ→ใบเสร็จ/แจ้งหนี้เครดิต").** วิธี: **Workflow audit (8 agents · adversarial-verify · 2.2M tok)** หา + verify รู แล้ว fix (money items diff-read):**
> - **`0c921734` งานหาย + สถานะเด้งกลับ (MONEY):** (A) MOMO staging upsert = ALL-OR-NOTHING — 1 แถวซ้ำ momo_tracking_no (Postgres 21000) หรือ overflow → **ทั้ง 7-วัน window พัง upsertedCount=0** งานหายทั้งวัน. fix = dedup by conflict key + chunk 500 + per-row fallback (isolate offender) · loud error เมื่อ importTrackCount>0 แต่ upRows=0 (`sync.ts`). (B) cron **DEMOTE สถานะ 4/5→3** เพราะ `propagate.ts:405` UPDATE ไม่มี fstatus guard ใน WHERE (เทียบ stale `f.fstatus`) → scan-แล้วเด้งกลับ ไม่ self-heal. fix = `.lt("fstatus", target)` เฉพาะตอน advance fstatus.
> - **`8d957546` เงินขาด + เครดิตซิงค์ (MONEY):** (C) report-cnt "กดวางบิลหลังบ้าน 4→5" **ไม่มี guard ฿0** (createBillingRunInvoice มี · path นี้ bypass) → ftotalprice=0 (ไม่มีเรทการ์ด) แจ้งหนี้ ฿0 = เก็บเงินขาด. fix = guard `ftotalprice<=0` refuse ทั้ง single+group. (D) จ่ายใบแจ้งหนี้ **เครดิต** ผ่าน markBillingRunPaid **ไม่เคลียร์ fcredit/paydeposit/tb_credit** → วงเงินไม่คืน AR ค้างตลอด. fix = mirror `actions/credit.ts` settle (clear fcredit · paydeposit='1' · reduce tb_credit oldest-first · calcForwarderOutstanding · TOCTOU · best-effort). (E) prefill วางบิลตู้เครดิต ตก fstatus-6 → `.in("fstatus",["4","5","6"])`.
> - **`09012681` Fix F dedup (MONEY):** commitMomoRowCore ไม่ dedup กับ tb_forwarder.ftrackingchn ที่มีอยู่ (auto-commit + add-missing → dup billable). fix = กันสร้างซ้ำที่ chokepoint → เปิด autocommit ได้ปลอดภัย.
> - **`886bfb51` ขนาดกล่องซิงค์:** packing-list (w×l×h) → forwarder fwidth/flength/fheight (แก้ dims หาย — owner "มีแทรกกิ้งแต่ไม่มีขนาด · ระวังของอยู่ในกระสอบ").
>
> **✅ อื่นๆ session นี้ (each build-0 · pushed):**
> - **`d00473d5` USD editor (P22353 · MONEY-display):** ออเดอร์เปิดเป็น USD → items-editor แก้ **ราคา/ชิ้นเป็น $ จริง + เรท บาท/USD แก้ได้ (เกิน 20 ได้)** · ¥ `cprice` ยัง SOT (yuanPerUnit FIXED จาก original ไม่ drift · `lib/forwarder/usd-order-pricing.ts`). แก้บั๊กที่เห็น: คนกรอก USD ลงช่อง ¥ → cprice=544 (ควร ¥3683) → total เพี้ยน. + `92e7b25f` USD-primary display บน detail/edit (บาท/USD rate · ราคาสินค้า $).
> - **`03bfe0c8` ค่าส่งไทย Flash — เลิกเติมเลขปลอม ฿50:** `resolveThShippingAutoPrice` คืน null (ไม่ auto-fill) เว้นแต่**วัดครบ (dims girth + kg) + ในพิสัย** → เรท Flash จริง (owner: เว็บ 300+ แต่ระบบ 0/50). null → gate "ห้ามลืมค่าส่งไทย" บังคับวัด+กรอกจริง.
> - **`d520ec9f` COD gate:** payMethod='2' ปลายทาง → ค่าส่งไทย ฿0 ถูกต้อง (เอกชนเก็บปลายทาง) → `isThShippingCostRequired/Missing` exempt COD (thread paymethod ผ่าน billing-run).
> - **DOC BOT + ไฟล์ HS ครบ (mig 0251):** verify DOC BOT ย้ายครบ (749 = source=prod=dev) → **import ไฟล์ owner "1.พิกัดอัพเดท.xlsx" 4,586 พิกัด** (คำศัพท์3868/nnb715[+เลี่ยง→note]/Vat3) เข้า doc_bot_hs_codes + คอลัมน์ `source` (mig 0251) = **รวม 5,335 · prod+dev** · หน้า browse `/admin/accounting/hs-library/bot` (พิกัดหลัก/รอง แบบที่อยู่หลัก-รอง · 118 กลุ่มขัดกัน · owner "เก็บหมดเป็นตัวเลือก · เลี่ยงพิกัด"). script `scripts/import-hs-excel-2026-07-10.mjs` idempotent (replace source LIKE 'ไฟล์:%').
>
> **🖥 กู้จาก session-limit (งานไม่หาย · learning [[parallel-agent-sprints]]):** session ก่อน finalize+commit+push ครบก่อนโดน limit → ของค้างในเครื่อง = raw agent version ที่ถูก supersede → **`git reset --hard origin/dave-pacred`** ได้ของ finalize ที่ gate ผ่าน (5 commit fix + USD + learnings). ⚠️ background agents รันใน main worktree (ไม่ได้ set isolation) → edit ลง tree ตรงๆ · ตรวจ origin ก่อน redo. memory [[session_2026_07_13_limit_recovery_momo_credit]].
>
> **🔴 CARRYOVER (owner):** (1) ตั้ง `MOMO_CRON_AUTOCOMMIT=true` Vercel (dup-safe แล้ว). (2) DOC BOT ต่อยอด: slice 2-4 (เลือกพิกัดหลักถาวร · wire hs-code-picker เข้า ใบขน/ใบกำกับ/เฟรท · promote→canonical hs_codes). (3) กระสอบรวม data-model (dims หายเพราะของในกระสอบ · owner-input). (4) พี่ป๊อป spec remainder (task #29). Standing: #52089 · RECEIPT_TOKEN_SECRET Vercel.

---

# 🧾 2026-07-10 (เดฟ · owner-driven all-day) — integrate ทุก branch ×หลายรอบ · 🔴 ยกเลิก tier VIP/SVIP → per-customer เรทเฉพาะตัว (materialize+resolver) · 🔴 shop-refund ¥×hrate (คืนขาด 5 เท่า) · PR549 juristic gate · cart บังคับที่อยู่ · web-pacred รูป cart → dave-pacred = main · read FIRST

> **🏁 SESSION CLOSE (owner: "ตรวจงานน้องทุกคน · อัพขึ้น main · สรุปปิด session").** **dave-pacred = main = `<HEAD>`** (pushed per-commit · main authorized · reviewed+integrated ทุก branch [InwPond007/Poom-pacred/web-pacred] หลายรอบ · standing "push เฉพาะ trunk"). gate each commit: **BUILD_EXIT=0 + tsc 0** (read from log · ⚠️ ห้าม launch build ซ้อน = concurrent-lock ให้ false exit 1 · learning §5). NEWEST mig 0250 (booking_pricing_catalog · ปอน) · **NEXT FREE = 0251** (0246-0249 = translation_cache/delivery_address/cart-currency/doc_bot_*). 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`).
>
> **✅ SHIPPED (workflow-built · money-diff + BUILD 0 + adversarial-verify · serialized):**
> - **🔴 ยกเลิก tier VIP/SVIP/VVIP → per-customer เรทเฉพาะตัว (`00969ea6`+`2edcc5e2`+`9f29063b` · owner Materialize · ราคาไม่เปลี่ยน):** waterfall `manual▸SVIP▸VIP-group▸general` → `manual▸per-customer custom(=เรทหน้า profile)▸general`. **materialize DATA ก่อน** (`scripts/materialize-vip-group-rates-2026-07-10.mjs` · 138/154 VIP-group คน → tb_rate_custom_* + coID→PR · prod+dev · backup · verify 0 on tier · PR4019=6900 เดิม) **แล้ว drop code** (resolve-rate.ts+live-rate.ts+quote actions · resolve-rate.test 95/0 · labels→เรทเฉพาะตัว · retire settings-vip+rates/custom-user banner · **tb_rate_vip_*/tb_co data KEPT reversible**). [[vip-tier-removal-and-refund-rate]] · learning [[refund-rate-svip-removal-2026-07-10]] §2.
> - **🔴 shop-refund ¥×hrate (`45842dee` · P22343 · MONEY · คืนขาด ~5 เท่า):** `refundAmountThb = cprice×qty` (¥ คิดเป็น ฿) → `cprice×qty×header.hrate` (เรทที่ลูกค้าจ่ายจริง · guard >0 · P22343 ¥299×10@5.10=฿15,249 ไม่ใช่ ฿2,990) · recompute-from-lines ทั้ง refund + adminDeleteOrderItem (เดิม decrement แค่ cprice ไม่×qty + ไม่ recompute THB) · ShippingRefundBox (hshippingchn reduce→Δ¥×hrate) · ตีลังไม้ per-item/shop = tb_order.hcrate flag (existing col). learning §1. 🔴 flag: crate PRICE per-item ต้อง column ใหม่.
> - **PR549 juristic gate (`2cb75eeb`):** ลูกค้ารออนุมัตินิติ (corporatestatus='1') banner บังทั้งหน้า → **non-blocking notice** (list+add page · โชว์ orders/form + แถบแจ้ง) · approve PR549 (1→2 · valid docs · backup) · permission = adminSetCorporateStatus/adminConvertToJuristic เป็น WRITE_ROLES (super ทำได้ · ไม่ใช่ ultra). learning §4.
> - **cart บังคับที่อยู่จัดส่ง (`69629e33`):** warehouse-default → silent "PCS" default (checkout ได้โดยไม่ตั้งที่อยู่) → gate "🔴 กรุณาตั้งค่าที่อยู่ก่อนสั่งซื้อ" + block submit + CTA · รับเองโกดัง = explicit choice · saved-address zero regression.
> - **integrate ทุก branch หลายรอบ:** web-pacred (รูป cart 404 fix · Alibaba CDN suffix gate · currency USD หน้าออเดอร์ · รูปสินค้า SOT resolver) · Poom-pacred (driver countdown/ถ่ายภาพ · วางบิลใบเสร็จคลิกเดียว · ใบเสร็จ PR removal · report-cnt ตู้ MOMO ซ้ำ fix · reprice-stale-cargo tool [owner-gated]) · InwPond007 (booking workspace UI · quotation · เงื่อนไขงาน). ทุกอัน reviewed money-safe + build 0.
>
> **🔴 NEXT / carryover (owner เคาะ):** (1) **reprice-stale-cargo** = `scripts/reprice-stale-cargo-comparison-2026-07-10.mjs` (dry-run-first · re-price cargo ที่ตั้งราคาก่อนกฎ ค่าเทียบ 250 · `docs/handoff-dave-2026-07-10-reprice-stale-cargo.md`) — รอ owner เคาะให้รัน. (2) crate PRICE per-item (ต้อง migration · ตอนนี้ flag only). (3) #52194/#52196 PR067 aggregate-error (packing tool). (4) DOC BOT reconcile → canonical hs_codes. Standing: _setpass_ref.php · #52089 · RECEIPT_TOKEN_SECRET Vercel.

---

# 🧾 2026-07-09 (เดฟ · owner-driven all-day) — currency/USD · ค่าส่งไทย ต้นทาง+Flash · packing-audit money fixes · address-link · retired-rep→central · บัญชี flow เนียน (2 popup) · yuan-rate · 🔴 DOC BOT DB→MAIN (คลัง HS) → dave-pacred = main · read FIRST

> **🏁 SESSION CLOSE (owner: "สรุป · จดเลินนิ่ง · อัพเดททุกเอกสาร · push dave-pacred + main · ปิด session").** **dave-pacred = main = `<HEAD>`** (pushed per-commit all day · main authorized · Poom-pacred/InwPond007 ahead-by-teammate → เดฟ integrates later · standing "push เฉพาะ trunk"). gate each commit: **BUILD_EXIT=0 + tsc 0** (read from log). 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`). **NEWEST mig 0249 (doc_bot_* HS import · applied prod+dev) · NEXT FREE = 0250** (0246 translation_cache · 0247 forwarder_invoice.delivery_address · 0248 tb_cart/tb_order input_currency/input_price · 0249 doc_bot_*). 🆕 **DOC BOT Supabase pass = `Pacred40x.@` · ref `blhdxmcmepmmdfmqqvdg`.**
>
> **✅ SHIPPED (12 items · workflow-built · each own money-diff + BUILD 0 + adversarial-verify · serialized):**
> - **currency selector + USD-primary display (`1f21908e`+`16cd518b` · mig 0248):** cart price-per-piece สกุลเงิน CNY/USD/THB/+ · normalize→¥-equiv cprice (pricing ¥×rsdefault UNCHANGED · CNY byte-identical) · display หน่วยหลักตามที่เปิดงาน ("$3,683.40 USD" ใหญ่ + "≈ ¥24,936 · ฿127,177" เล็ก) · persist input_currency/input_price + carry cart→order.
> - **🔴 ค่าส่งไทย ต้นทาง default + Flash จริง (`c7a79e12` MONEY · REVERSED ad31a708 COD-default):** derivePayMethodForDelivery → ต้นทาง "1" ทุก carrier/zone · auto-fill = calPriceFlash (zip zone) + margin 15% (5-20 · editable) · COD "2" = manual เมื่อลูกค้าขอ · เหมาๆ flat ฿100 · billed frozen. learning [[momo-aggregate-and-cross-project-import-2026-07-09]] §4.
> - **🔴 packing-audit money fixes:** stamp 29 ตู้ → **container_packing_reconcile** (script backfill skipped the stamp → gate 0245 บล็อกเก็บเงิน · learning §2) · **#52403 PR146 ฿91,194.64→฿15,234.75** (MOMO aggregate ทั้งตู้ลงแถวเดียว · 3525→420kg · 24.6472→4.1175 CBM · dims 30×25×183 · CBM-basis geometric · learning §1) · audit fvolume>9 unbilled → #52194/#52196 PR067 (dense/multi-row → flag โกดัง · ไม่ auto-guess) · dims 0×0×0 → "—".
> - **รายละเอียดสินค้า + แปลจีน (`46dd1879` · mig 0246):** `<ProductDetailLines>` shared block ทุกที่ (customer+admin·ทุกสถานะ) + `<TranslateButton>` in-house (Google gtx free + MyMemory fallback + translation_cache · no paid key · reusable · learning §5).
> - **Flash Import export (`3f7d0f7a`):** Flash=นัดรับที่โกดัง (ไม่ใช่เราไปส่ง) · export ไฟล์ ข้อมูลผู้รับ บน Express tab → staff อัพเข้าเวป Flash (no public API · learning §6) · COD เฉพาะ paymethod=2.
> - **address link (`8009fbb5` · mig 0247):** `<CustomerAddressPicker>` reusable — เลือกจาก tb_address ทั้งหมด + "+ เพิ่มที่อยู่ให้ลูกค้า" (link profile) · ใช้ทั้ง forwarder [fNo] (fAddress* snapshot) + **billing-run/[id] เปลี่ยนที่อยู่จัดส่ง → เอกสารเปลี่ยนตาม** (tb_forwarder_invoice.delivery_address · โชว์บน print + /b/token) · buyer_address (tax identity) ไม่แตะ · + ประวัติการจัดส่ง.
> - **retired-rep→central (`0bed9340`):** resolveActiveSalesRep — rep ออกไปแล้ว (adminStatusA≠'1') → "ทีมขายส่วนกลาง (Pacred)" ทุกที่ (sidebar/profile/dashboard/invoice/admin) · active ไม่แตะ · stored adminIDSale ไม่ bulk-rewrite · picker กัน retired.
> - **บัญชี flow เนียน (`ab6cb956`):** `<SelectedItemsConfirmDialog>` reusable itemized popup ตาม legacy — report-cnt add-check ("รายการที่เลือก N/N" · ออเดอร์/แทรค/รหัส/สถานะ → "เพิ่มไปยังรายการตรวจสอบแล้ว") + forwarder-check bill (+จำนวนเงิน+total → "เรียกเก็บเงินลูกค้า ฿X บาท" + note สถานะ→5/SMS) · total=bill เป๊ะ · logic/gate(0245)/refund-rebill/?q=c-n ไม่แตะ.
> - **yuan เรทฝากโอน (`2e2f1233`):** admin /yuan-payments/new auto-fill payrate toFixed(2)→**toFixed(4)** (ตรง rpdefault เป๊ะ · 2 ปัด >2 ทศนิยม = แสดงผิด) + label "auto จากเรทระบบวันนี้ · แก้ได้" · customer+admin ดึง rpdefault ตัวเดียว = linked (A6).
> - **🔴 DOC BOT DB → MAIN (`aab041b4` · mig 0249):** ย้าย DOC BOT Supabase ทั้ง 5 ตาราง → `doc_bot_*` (hs_codes 749/overrides 9/conversation 336/state 1/0 · prod+dev · lossless ON CONFLICT · `scripts/import-doc-bot-2026-07-09.mjs`) = คลัง HS จริง · แยกจาก canonical hs_codes 133 สะอาด · [[doc-bot-hs-library-import]].
>
> **🔴 NEXT / carryover (owner เคาะ):** (1) **#52194/#52196 PR067 GZS260629-1** = aggregate-error dense/multi-row (packing base 5กล่อง/5780kg vs ระบบ 13กล่อง) → โกดัง verify ผ่าน packing tool (gate 0245 บล็อกบิลอยู่). (2) **DOC BOT ต่อยอด** = reconcile doc_bot_hs_codes(749 เละ)→canonical hs_codes(133) + wire HS-lookup/consult จากคลังใหญ่. (3) Flash COD รวมค่าส่งปลายทางไหม (collect-total zero domestic · COD หายากแล้วเพราะ default ต้นทาง). (4) owner ทดสอบ side-by-side PCS (บัญชี flow 2-popup · yuan OCR/packing ยัง auth-click). Standing: _setpass_ref.php delete · #52089 · RECEIPT_TOKEN_SECRET Vercel.

---

# 🖥 2026-07-08 (เดฟ · full day) — RUN legacy PCS locally + owner-guided faithful fixes (ทางรถ MONEY · COD · สั่งซื้อจีน · stale-SVIP · motion) + **ฝากโอนหยวน QR+OCR in-house (mig 0244) · MOMO packing-list upload · 🔴 COMBO-FLOW continuity G1-G8 (carry-not-rederive) · pnpm/Vercel fix** → read FIRST

> **🏁 SESSION CLOSE (owner: "resync · ตรวจงานน้องภูม · รัน local pcs admin ให้ได้ · ไล่ทุกหน้า เทียบ pcs เอาให้เหมือน 100% ก่อนส่งมอบ · php นายทำได้นะ" → แล้ว "สรุป · เลินนิ่ง · ปิด session ก่อน").** all 4 branches synced at close (main = dave-pacred = Poom-pacred = InwPond007 · main→Vercel prod). gate build 0. **newest mig 0244 (ฝากโอนหยวน payee_qr_image · applied prod+dev) · NEXT FREE = 0245** (0241 purchaser adminidpurchaser · 0242 purchaser positions · 0243 report_cnt_container_level_bucket · **0244 tb_payment.payee_qr_image = รูป QR ปลายทาง 收款码 · owner 2026-07-08** · ทั้งหมด applied prod+dev). 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`).
>
> **🖥 LEGACY PCS ADMIN RUNS LOCALLY** (owner's "หาวิธีรันให้ได้" · the big deliverable · full setup + traps → [`docs/research/pcs-admin-faithful-audit-2026-07-08/RUN-LEGACY-PCS-ADMIN.md`](../../docs/research/pcs-admin-faithful-audit-2026-07-08/RUN-LEGACY-PCS-ADMIN.md) + learning [`docs/learnings/running-legacy-php-on-modern-runtime.md`](../../docs/learnings/running-legacy-php-on-modern-runtime.md)):
> - **URL** http://localhost:8899/member/pcs-admin/ · **login `admin_pop`/`pcs123456`** (ติ๊ก remember · CEO/god = superset menu) · DB local MySQL `pcsc_main` 117 ตาราง (a prior session loaded it · user `pcsc_main`/`P%F7*bu98NUB`).
> - **restart:** `PHP_CLI_SERVER_WORKERS=6 nohup /opt/homebrew/bin/php -S localhost:8899 -t /Users/dev/Desktop/pcs-realshit/REALSHITDATAPCS/pcsc/public_html > /tmp/php-server.log 2>&1 & disown` (WORKERS≥2 required · self-HTTP deadlocks single-thread · nohup+disown survives the Bash-tool 2-min timeout).
> - **PHP 7→8 compat fixes** (legacy copy ONLY · not our repo): `pcs-admin/config/config.inc.php` local basePath + **`mysqli_report(MYSQLI_REPORT_OFF)`** (PHP 8.1+ throws on loose queries) · `pcs-admin/include/function.php` ×3 self-HTTP `checkRightsName` → `include` (deadlock) · `report-cnt.php:487` div-by-zero guard. ⚠️ TWO configs — pcs-admin has its OWN (edit that one, not member/config). php 8.5 via brew.
> - all 18 tested pages render 200 no-fatal (dashboard · forwarder · report-cnt · shops · payment · acc-* · cnt-hs · driver · api-forwarder-momo · cart...).
>
> **✅ integrate ภูม + color decision:** merged Poom-pacred (7 commits · พี่ป๊อป-spec: warehouse ยิงรับเข้าไทย · momo 3-รอบ labels · billing register แถบฟ้า · ขาด/ครบ pill worker-mobile · **fstatus 4 = น้ำตาล**) · FF clean · no migration. **Owner CONFIRMED fstatus-4 "ถึงไทยแล้ว" = 🤎 น้ำตาล** (ตาม spec · supersedes my earlier "คงamber" · ภูม's is correct · kept).
>
> **🔍 FAITHFUL AUDIT started (task #32 · in progress · [`docs/research/pcs-admin-faithful-audit-2026-07-08/`](../../docs/research/pcs-admin-faithful-audit-2026-07-08/) = CEO-menu function inventory + run doc):** area 1 = forwarder/cargo core. **KEY FINDING: platform ~90-95% faithful — the Explore audit agent MASSIVELY over-flagged** (claimed 7 "top gaps" on the forwarder list · ALL already implemented or false: tax-doc col · purchaser col · origin badge · print/car badges · per-row ดู/อัปเดต · price=outstanding). Verified each vs current `forwarders-table.tsx` (post-ภูม · 1819 LOC). **Learning [[audit-discipline]]: fidelity agents enumerate legacy well but skim OUR side → invent rebuilds → verify every claimed gap vs current source before building.** → **pivoted to OWNER-GUIDED:** asked พี่ป๊อป which specific pages HE sees as "จาด/ไม่เหมือน" (10× more efficient than blind 180-page re-sweep that mostly re-confirms faithfulness).
>
> **✅ CONTINUED same day (owner-guided "จาด/ไม่เหมือน" → fixed precisely · legacy side-by-side · all 4 branches @ `89326ae9` · each: own money-diff + BUILD_EXIT=0 from log + adversarial-verify · workflows serialized):**
> - **🔴 ทางรถ = road-priced (URGENT MONEY · `d4752bd9`)** — ลูกค้า/order ค้าง SEA default → priced เรือ แต่ MOMO ส่งรถ (ตู้ GZE/EK) → ต้นทุนบาน MOMO ไม่คืน. fix: pricing reconciles `ftransporttype` to the PHYSICAL cabinet/tracking (`resolveTransportMode(fcabinetnumber ?? ftrackingchn, stored)`) ทั้ง `forwarders-edit.ts` + `live-rate.ts` (computeAndFill) → road-shipped = road-priced · persisted so stored mode+rate agree · rate via engine · ไม่ re-price collected/frozen · radios/cart/guard untouched.
> - **🔴 COD ต่างจังหวัด (`ad31a708`)** — PR130 เชียงใหม่ ควร default เก็บปลายทาง. `derivePayMethodForDelivery(carrier,{addressID,zip})`: upcountry zip → payMethod "2" COD; BKK/metro + OWN_FLEET (PCS/PCSF/PRF/PCSE) stays carrier-derived. wired 4 write paths + EditPayMethodField lock+red-COD-badge (backend-editable) + `forwarder-collect-total`/`actions/forwarder` COD guard zeroes ONLY the domestic leg (`paymethod===2?0:ftransportprice`) · freight/tax/mao unchanged.
> - **🔴 สั่งซื้อจีน admin-name (`89326ae9`)** — owner "pcs โชว์ชื่อ admin ผู้รับผิดชอบ · ไม่ต้องมอบหมาย". cart.ts AUTO-SETS `adminidpurchaser`=handling admin at create · detail IPC fallback adminidip→adminidcreate · list shows responsible admin (purchaserResp · no forced "ยังไม่มอบหมาย") · purchaser scope OR-fallback (own assigned OR unassigned-and-own-ip/creator · NOT widened · closes a pre-existing `.eq('','')`-matches-all over-exposure) · purchaser feature (mig 0241/0242) kept · NO money/status write · scope 26/26. (optional data backfill ~21,950 rows deferred — code shows fallback so display already correct.)
> - **rate-linkage stale-SVIP (`9f7c63ea` · MONEY)** — PR130 #52117 + billing-run/55 (5700 vs 5300) = same as PR002 root: SVIP card added AFTER pricing → general rate ค้าง. re-price-on-card-save extended fstatus<5 → also fstatus=5-not-paid (excl open bill) + prod backfill (#52117 5700→5300 · invoice 55 →129.51). platform stale-SVIP population now **0**.
> - **#7 ค่าส่งไทย auto-fill (`141da7df` MONEY) · ฿50/แทรค floor** (engine floors future · past-done left) · **cnt-hs/report-cnt CSV+badge fixes** · **motion ultra-smooth (`fb5a876b`)** platform-wide silky transitions (globals.css @layer base · targeted props not transition:all · fade/hover/view-transition · reduced-motion) · **receipts fidelity** (ภูม merge) · **report-cnt chip-only + dup-slip suppress on open bill** (`ed9c0188`) · **คืนเงินลูกค้า link** (`58694379`).
> - **🖥 RUN-BOTH-CHROME live now:** Pacred dev `localhost:3000` (preview `pacred-1to1` · motion visible) + legacy PCS `https://localhost/pcscargo/member/pcs-admin/` (XAMPP · `admin_tam`/`pacred1234` · "thisisunsafe" cert) — side-by-side for faithful compare. ⚠️ DELETE `C:/xampp/.../pcs-admin/_setpass_ref.php` (login helper · security hygiene).
>
> **✅ CONTINUED afternoon/evening (owner-driven · each own money-diff + BUILD_EXIT=0 + adversarial-verify · workflows serialized · all 4 branches at close):**
> - **🧾 ฝากโอนหยวน — แนบรูป QR ปลายทาง (收款码) ครบวงจร (`874067db`+ · mig 0244 `tb_payment.payee_qr_image` prod+dev):** ลูกค้าส่งรูป QR Alipay/WeChat มาให้โอน แต่เดิมเก็บผู้รับเป็น text + สลิปเท่านั้น. เพิ่มช่อง QR ปลายทาง 3 จุด (ลูกค้าตอนขอ /service-payment · admin /yuan-payments/new · admin detail แนบทีหลังได้) · **jsQR client-side auto-decode** (ตั้งช่องทาง+ดึง payload) · **🔧 เครื่องมือ OCR in-house (Tesseract.js · WASM · self-host /public/tesseract · ไม่พึ่ง API นอก · owner "ทำเองในระบบ")** = `lib/ocr/recognize.ts` + `<OcrExtract>` reusable (อ่านชื่อร้านจีน → เติมผู้รับ) · ต่อยอดทั้ง platform.
> - **💰 ฝากโอนหยวน admin manual-create = DIRECT-CUT (`96f4828b`):** ไม่ติ๊ก paydeposit = บันทึกเฉยๆ ไม่ตัด wallet + ไม่เช็คยอด (เดิมตัดทุกครั้ง → กดบันทึกไม่ได้ · จะตัดกระเป๋าผิด · งานตัดจ่ายจบแล้ว) → save ได้ · รายการขึ้น.
> - **📦 MOMO packing-list upload (`d588ec5f` · brief §4 mode-2 · MONEY):** อ่าน 37 ไฟล์ (35 Format-A มีข้อมูล · 2 Format-B ว่าง=MOMO web มั่ว). `/admin/api-forwarder-momo/packing-upload` — parser แกะเอง (SheetJS พัง·inline strings→node:zlib unzip · +test 2 fixtures) → preview จับคู่ tracking → apply CBM/น้ำหนัก/ตู้ (non-billed only · re-parse server · re-price engine · **ปิดตู้→fstatus 3 กำลังส่งมาไทย**).
> - **🔴 COMBO-FLOW CONTINUITY (owner "แก้มาหลายวันไม่จบ · ตรวจตู้→ออกบิล→จ่าย→เสร็จ→จัดส่ง ไม่ต่อเนื่อง") — source-grounded audit [`docs/research/forwarder-check-combo-continuity-audit-2026-07-08.md`] + learning [[faithful-port-carry-not-rederive]]. ROOT: legacy CARRIES on one frozen row+one reduce · เรา RE-DERIVE ทุก hop → เลข/เอกสาร/profile/selection ดริฟต์. Fixed:**
>   - **G1 (`30bf2437`):** ใบเสร็จ **ยึดยอดบิลที่จ่าย** (reconcile-not-recompute · prefer paid · log drift) — เดิมคำนวณใหม่สด · **G2 (`1b4951c0`):** SMS/notify = ยอด portal จริง (1 ข้อความ/ลูกค้า · รวม เหมาๆ · COD ถูก · เดิมขาด ฿100) · **G3+G4 (`30bf2437`):** ติ๊กตรวจตู้ (tb_check_forwarder) carry ไปบิล + บิลเลื่อน 4→5 เอง (ไม่ blank/ไม่ 4-hop) · **G7+G8 (`7259b69b`):** profile snapshot bill==ใบเสร็จ (resolveBillingIdentity SOT) + no-slip settle ต้องยืนยัน "ชำระนอกระบบ" · **G5 (`d820725c`):** ลูกค้าจ่ายบิลเองบน /b/[token] (QR net_payable + แนบสลิป · token HMAC · stages pending · settle ยัง gated). **ทุกอัน: bill formula ไม่แตะ · guards ครบ · tests ผ่าน.**
> - **🔴 pnpm/Vercel main fix (`f843f05d`):** `pnpm add tesseract.js` เขียน placeholder `tesseract.js: set this to true or false` ค้างใน pnpm-workspace.yaml → Vercel `pnpm install` exit 1 → main deploy พัง. fix: `false` + ignoredBuiltDependencies (mirror sharp) · verified install exit 0.
>
> **🔴 NEXT / carryover:** (1) **combo-flow: owner ทดสอบ side-by-side PCS** (ตรวจตู้→บิล→เสร็จ→SMS ตรงกันไหม→จ่าย→จัดส่ง). (2) **G6** (advance-paid row [fstatus=6·paydeposit=1] ไม่เข้าคิวจัดส่ง — semantic collision กับ legacy paydeposit gate · **owner เคาะ**) · **G9** (report-cnt 11-tab strip · LOOK · forwarder-check มีแล้ว). (3) K2/S4 owner-decision · adminidpurchaser backfill (optional). (4) yuan OCR/packing-upload/G5 **ยังไม่ authed-click-test** (admin login ไม่ได้) — owner dry-run. Standing: #52089 · MOMO drift ฿294k · RECEIPT_TOKEN_SECRET in Vercel · `_setpass_ref.php` delete.

# 🧩 2026-07-07 (ภูม) — พี่ป๊อป-spec gap-items build (audit 10 ข้อ → 6 done · 2 owner-input · item8 plan) → Poom-pacred · read FIRST

> **🏁 SESSION CLOSE (ภูม: "เซฟ Poom-pacred · ทำต่อพรุ่งนี้").** Branch **Poom-pacred = HEAD** (pushed · clean · local==origin). Gate เขียวทุก commit: **tsc 0 · lint 0** (`cd /c/Users/Admin/pacred-web/pacred-web` ก่อนเสมอ — cwd reset บ่อย · เจอ  2 ครั้ง session นี้). ⚠️ **screenshot MCP glitch ทั้ง session** → verify จาก legacy PHP source + gate + DOM-check แทน.
>
> **🎯 งาน:** audit พี่ป๊อป spec (`docs/research/pop-spec-2026-07-06/00-SPEC.md` + `03-BUILD-PLAN.md` เดฟ) → gap list 10 ข้อ → ภูม สั่งลุย 1-10 (ข้าม 7,9 รอถามพี่ป๊อป). **เดฟ ทำ foundation 8/8 ไปแล้วเมื่อคืน** (2 ข้อในลิสต์เลยมีอยู่แล้ว).
>
> **✅ Built + pushed (4):** **1** ขาด(ชมพู)/ครบ(ขาว) pill ต่อตู้ บน warehouse worker/shipping (reuse container-completeness) · **3** ประวัติออกบิล register แถบสีฟ้า (billing-run/page) · **4** MOMO ป้าย "3 รอบ" คร่าวๆ/packing/ปิดตู้ (sync-client) · **6** หน้า "ยิงรับเข้าไทย" แยกเฉพาะ `warehouse/worker/arrive-th` + action `warehouseArriveThScan` (lookup tracking → gate → flip 3→4 · เครดิต-6 carve-out · NO money) + การ์ดหน้าแรกแอปคลัง. **+ก่อนหน้า:** สถานะ 4 "ถึงไทยแล้ว" → **สีน้ำตาล** (#8d6e63) admin (`lib/admin/forwarder-status.ts`) + customer (`service-import/forwarder-row-view`).
>
> **✅ Verified มีแล้ว/ไม่ต้องแก้ (3):** **2** ปุ่ม "แจ้งส่งต่อบัญชี" = `report-cnt/[fNo]/handoff-button.tsx` (เดฟ · status-only) · **5** คลังห้ามเห็นต้นทุน = gated ครบทุกหน้า warehouse-reachable (`canViewCostProfit` · forwarder-cost-section `if(!canEdit) return null`) · **10** สถานะตู้ จีน-ไทย ladder = `report-cnt/[fNo]/container-journey-panel.tsx` (buildContainerJourney).
>
> **🔴 รอ owner (ภูม ถามพี่ป๊อป):** **7** ค่าส่งไทย auto-fill (โซนไหน auto · คลัง-auto vs CS-กรอก) · **9** กระสอบรวม data-model (วางบิลของชิ้นเล็กหลายลูกค้า) — build-plan เดฟ flag ว่า owner-input "ห้ามเดา".
>
> **🟡 item 8 MOMO อัพไฟล์ XLSX → ตารางหลัก (build พรุ่งนี้ · plan พร้อม):** ภูม ส่งไฟล์จริง `Downloads/PR20260614-SEA01 (1).xlsx` = **MOMO packing list ต่อตู้** (decode แล้ว · 18-col: Tracking/Code/W/L/H/ParcelCount/Weight/CBM/CG + meta CONTAINER NAME=GZS260617-1). **⚠️ GOTCHA: SheetJS พังกับไฟล์นี้** (inline strings · ไม่มี sharedStrings → คืน raw ZIP-XML) → ต้อง **unzip + parse `xl/worksheets/sheet1.xml` เอง**. **แผนเต็ม + column mapping + gotcha: [`docs/research/momo-xlsx-import-plan-2026-07-07.md`](../../docs/research/momo-xlsx-import-plan-2026-07-07.md).** money-adjacent → build careful (preview→commit · §0e).
>
> **🔴 NEXT (พรุ่งนี้):** build item 8 (parser+test → UI upload บน api-forwarder-momo/sync → ingest momo_import_tracks) · เอา owner input 7/9 มาทำ. **dev pacred = :3000** (ถ้า poom-tools ยึด :3000 → หยุดก่อน) · **431 cookie-bloat fix** = restart ด้วย `NODE_OPTIONS=--max-http-header-size=131072`.

---

# 🧾 2026-07-07 ค่ำ (เดฟ · resume ต่อจาก limit) — กู้ WIP faithful-port (refund-rebill guard) + verify flow นำเข้า faithful vs PCS จริง (list/คนขับ/ตู้/ขนส่ง) + WHT namesake = ยืนยันแก้จบ + เจอ gap report-cnt 11 exception-tab → ALL 4 BRANCHES · read FIRST

> **🏁 CLOSE (owner: "ต่อ session ค้าง · resume ก่อน limit · flow นำเข้าให้จบ · หน้าตา+logic เหมือน PCS อันดับแรก · รัน local ต่อ chrome" → "ปิด session · push all branch · สรุป").** **all 4 (main=dave-pacred=Poom-pacred=InwPond007) → `<HEAD>`** (Vercel prod). PCS PHP รัน local เป็น reference: `https://localhost/pcscargo/member/pcs-admin/` (admin_tam/pacred1234 · services `mysqld`+`httpd` via PowerShell · cert bypass `thisisunsafe` · [[pcs-legacy-local-run]]). legacy source ก็อ่านได้ที่ `C:\xampp\htdocs\pcscargo\member\pcs-admin\*.php`. 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`) · `.env.local` SUPABASE_DB_PASSWORD STALE (`Jirayus40x.` — ใช้ DqOz.../n61... แทน). NEXT FREE mig = 0244. gate: `rm -f .next/dev/types/validator.ts` ก่อน · NODE_OPTIONS=8192 build (อ่าน BUILD_EXIT · ห้าม build ตอน preview เปิด) · tsc 0 · commit heredoc `cat > f <<'EOF'`.
>
> **✅ งาน resume นี้:**
> - **กู้ WIP faithful-port session (ก่อนติด limit) — ห้ามงานหาย** — worktree มี WIP ค้าง (guard ใหม่ + 3 forwarder action edits + package.json) → verify coherent (test 5/5 · tsc 0) → commit **`ed986de5`** = `lib/admin/refund-rebill-guard.ts` `assertNotRefunded` (port legacy `forwarder.php:1290` ePayRe · กันเรียกเก็บเงินซ้ำบนออเดอร์นำเข้าที่จ่ายแล้ว · REFUSAL-only fail-closed · wired 7 flip-to-5/credit sites). build 0 · push 4 branch.
> - **verify flow นำเข้า faithful vs PCS จริง (เทียบ Chrome + code):** รายการฝากนำเข้า (source/status tabs · date filter 30วัน · bulk bar · คอลัมน์) · คนขับ (`forwarder-driver.php` list cols) · **รายงานตู้ 14 คอลัมน์ตรง** (หมายเลขตู้→สถานะจ่ายค่าตู้ + totals) · การเลือกขนส่ง (`nameShipBy` Flash=2/J&T=24/เหมาๆ=PRF · admin 47 เจ้า · zone logic) = **faithful ระดับ label/คอลัมน์แล้ว** (session ก่อนทำครบ).
> - **WHT 1% double-deduction (ชื่อ session) = ยืนยันแก้จบ (verify code):** ใบวางบิลใช้ `calcForwarderGross` (GROSS) + `computeBillWht` หัก 1% **ครั้งเดียว** (billing-run.ts:565/735/1112) · net ลูกค้า = `calcForwarderOutstanding` (ตรง legacy `calPriceForwarderMain` L1864) · fix ตั้งแต่ `3e529b42` · "12 ใบ" = over-count (หักซ้อนจริง 0) · เหลือ FRI2606-00006 (PR7429 ฿21.57 re-issue · owner/บัญชี).
> - **🔴 GAP เจอ (punch-list `cbff4fe6` · [`docs/research/import-flow-pcs-fidelity-punchlist-2026-07-07.md`](../../docs/research/import-flow-pcs-fidelity-punchlist-2026-07-07.md)):** report-cnt ขาด **11 QA/exception tabs** ของ PCS ทั้งหมด (หมายเหตุสั่งซื้อ/นำเข้า · ไม่ได้ถ่ายสินค้า · ไม่ใส่ค่าขนส่ง/เบอร์ตู้/วันปิดตู้ · ไม่เลือกขนส่งฟรี · เลือกขนส่งผิด · เครดิตเกินกำหนด · ประวัติเข้าโกดังไทย) = "ทุกทางเข้า" ยังไม่ครบ (~ครึ่งวัน · 11 query + tab strip).
>
> **🔴 CARRYOVER (owner เคาะ · resume ต่อ):** (1) build 11 report-cnt exception tabs · (2) **pixel-look sweep ต้อง render Pacred /admin คู่ PCS** — ยังไม่มี dev admin login (ตั้ง pw admin บน dev + login ก่อน = unblock) · (3) 2 flag: label "รับเองที่โกดัง (PCS)" คงหรือ rebrand · คอลัมน์เกิน (ETD/ETA · ID/ส่งแล้ว) ซ่อนหรือเก็บ.

---

# 🎨 2026-07-07 (เดฟ · cont) — FAITHFUL-PCS-LOOK + 🔴 MONEY-ROUTING v2 (cargo→LOGISTICS) + DATA-LINKAGE (owner "ลูกค้าจริงบ่นเยอะ") : phone+reset · report-cnt · slip-verify · dashboard+sidebar · gridlines · ทุกใบ 1-page · phantom-เหมาๆ · rate-guard · rate re-price-on-card-save + reverse ตู้→บิล link + PR002 3700→3300 fix → ALL 4 BRANCHES · read FIRST

> **🎯 OWNER TOP PRIORITY (standing · ส่งงาน 2 วัน · verbatim "อย่าลืมโจทย์หลัก...หน้าตา และ ฟังชั่น โลจิกส์การทำงานทั้งหมด ต้องเหมือน pcs ก่อน เรื่องอื่นค่อยว่ากัน ขอหน้ามาก่อนเลยตอนนี้ บอกภูมิด้วย สำคัญมาก"):** ทั้ง platform **หน้าตา + logic ต้องเหมือน PCS ก่อน** — เรื่องอื่นทีหลัง. **บอกภูมิด้วย.** legacy source = `C:\Users\Admin\Desktop\ALL DEV DATA\newrealdatapcs\pcscargo\member\pcs-admin\` (187 .php · §0b อ่าน source จริง). วิธี: read-only punch-list scan → build gridded-look ทีละกลุ่ม.
>
> **🏁 STATE.** all 4 branches (main = dave-pacred = Poom-pacred = InwPond007) → **`30283ba4`** (push · Vercel prod · merge ปอน SEO `79c5ece8` ที่ a560f3d6). base = `00baa558`. gate ทุก commit: **BUILD_EXIT=0 อ่านจาก log** (NODE_OPTIONS=8192 · `rm -f .next/dev/types/validator.ts` ก่อน · ⚠️ ห้าม build ตอน preview เปิด) · tsc 0 · workflow (ultracode) diagnose→build→adversarial-verify · money items ผม review diff เอง (grep .from/.select/.eq/.update/.insert) + build ยืนยันเอง. **NEXT FREE mig = 0244** (0243 report-cnt container-level bucket · applied prod+dev). 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`) · `.env.local`=PROD · DEV-SYNC ทุก mig. prod DB script pattern = `node --env-file=.env.local scripts/<x>.mjs` (service-role · SUPABASE_SERVICE_ROLE_KEY · dry-run+backup ก่อน --apply · ⚠️ Windows libuv "Assertion failed" teardown noise = ไม่กระทบ). ⚠️ commit msg heredoc ใช้ `cat > file <<'EOF'` (bash) — **ห้าม** `@'...'@` (PowerShell here-string · พังใน Bash tool · subject กลายเป็น `@`).
>
> **✅ SHIPPED (6 commits · workflow-built · money-safe · faithful-look):**
> - **phone + reset button** (`07cd4a40`) — ใต้ display name หน้า profiles ดึง userTel (juristic fallback tb_corporate) · ปุ่ม reset รหัสผ่าน ย้ายขึ้น identity header (หลัง Pricing · ก่อน รันเลข PR) · ลบ empty account-tools block.
> - **report-cnt ตู้ + เก็บเงิน** (`00c4ab00` · mig 0243) — (1) ตู้ข้ามไป "ถึงโกดังไทยแล้ว" ผิด → bucket ที่ **container-level MIN(fstatus)** (RPC get_container_summary + count_distinct_cabinets · `HAVING waiting→MIN<'4' · succeed→MIN>='4'` · SOT `lib/admin/report-cnt-bucket.ts`+test) · (2) un-dead-end checkbox→ตรวจสอบ→เก็บเงิน: `canCheckFlow = hasRole(["super","ops","accounting"])` (god-inclusive · decoupled จาก showMoney · mirror adminReportCntAddCheck gate) · money cols ยัง showMoney-gated · + 2 filters + fcover thumbnail. **PR002 รอจ่าย → เก็บเงินต่อได้.**
> - **money-doc แจงค่าแยกบรรทัด** (`962409f9`) — SOT `lib/forwarder/fee-breakdown.ts` (splitForwarderFees → freight/thaiShipping/crate/update/chnPlus/other/discount · namedFeesGross===calcForwarderGross invariant · 24 test) wired ใบวางบิล/ใบเสร็จ/ใบแจ้งหนี้ (freight line = balancing remainder → total byte-identical). ⚠️ การ **จัดบัญชี** ในนี้ (เหมาๆ→SERVICE) = ความเข้าใจ v1 **ผิด** → superseded โดย `18a68dbe` ↓.
> - **slip-verify 2-step + แก้เบิ้ล** (`8c9a5cfd`) — (1) เบิ้ล = wallet type-4 slip + FRI (ใบวางบิล) bill forwarder เดียวกัน ไม่ join → `lib/admin/topup-slip-dedup.ts` (FRI canonical · suppress wallet twin · computeTopupBadge SOT) · (2) 2-step ต่อเนื่อง: step-1 fold round-1 · step-2 `receipt-doc-no-editor.tsx` auto-run editable rID + dup-check (overrideRid → 1 seam autoIssueReceiptOnPaymentLand). **0 DB write เพิ่ม · money legs byte-identical · dedup 12/12 · wallet-hs 38/38.**
> - **dashboard การ์ดหมุน + sidebar นำเข้า** (`4d720784`) — carousel ฝากสั่ง/นำเข้า/โอน เดือน↔วันนี้ ทุก 5s · sidebar wrapServiceImport ขยาย legacy group (+รายงานตู้ /admin/report-cnt +ประวัติโกดัง /admin/forwarders/warehouse-history) · 0 new i18n key.
> - **gridlines+zebra 4 ตารางเงิน + yuan เลขที่ออเดอร์** (`ab78d212`+`1b09666e`) — yuan-payments/wallet-tx/accounting-DataTable/customers-activity ใส่ `border-collapse [&>thead>tr>th]:border … nth-child(even) bg-muted/30` · yuan +column เลขที่ออเดอร์ (=tb_payment.id · pure render). className/render-only · 0 money change.
> - **🔴 cargo → LOGISTICS (money-routing v2) + ทุกใบ 1-page** (`18a68dbe`→`a560f3d6`) — owner CORRECT: routing ตาม **lane** ไม่ใช่ fee type. **SERVICE = เฉพาะ ฝากสั่ง+ฝากโอน · LOGISTICS = ฝากนำเข้า cargo ทั้งก้อน (freight+เหมาๆ+ค่าขนส่งในไทย · ใช้เฟรทเจ้าอื่น) · TRADING = ใบกำกับ**. root = `import_cargo.defaultAccount "service"→"logistics"` (service-catalog.ts) + 4 forwarder surface route ผ่าน `serviceAccountFor("import_cargo")` (billing-run-paper · receipt-paper · forwarder-pay-modal QR ลูกค้าจ่ายจริง · forwarder-tax-invoice) → LOGISTICS. shop/yuan คง SERVICE · ใบกำกับคง TRADING. + rows/page 13→**24 ทุกใบ** (bill admin+public /b/[token] · receipt · owner ยอม overflow) · [[three-bank-accounts-routing]] v2. build 0 · bank-accounts 5/5 · service-catalog 21/21.
> - **🔴 phantom เหมาๆ ในใบเสร็จ (recurring · money)** (`143042bb`) — receipt auto-issue **recompute** เหมาๆ เอง → โชว์ ฿100 ที่ bill ไม่คิด (15140/15141: 195 vs bill 95). fix: `lib/admin/receipt-mao-fee.ts` (resolveReceiptMaoFee · override wins even 0) → markBillingRunPaid ส่ง bill's stored mao_fee_thb → **receipt == bill by construction** · direct-slip (wallet type-4) คง recompute. + data fix prod 15140/15141 →0 (`scripts/fix-phantom-mao-receipt-2026-07-07.mjs` · backup). 10/0 test.
> - **🔴 rate-guard + rate-linkage (owner "ข้อมูลไม่เชื่อมโยง · ลูกค้าบ่นเยอะ")** (`15dd0b44`+`30283ba4`) — (1) **rate-guard** `lib/forwarder/rate-mode-guard.ts` (advisory · warn-not-block · mirror minSell) เตือนตอนกรอกเรทผิด mode (งานรถ GZE แต่กรอกเรทเรือ) · derive mode จาก container/tracking (cabinet-transport SOT) · 8/0 · 0 money change. (2) **rate-linkage** = root ของ PR002 3700-vs-3300: SVIP card เพิ่ม**หลัง**ตั้งราคา → frefrate ค้าง general. fix `customer-rate.ts`: save card → **re-price forwarder open (fstatus<5·!manual·!locked · scope 1 userid+warehouse · cap 500 · best-effort · ใช้ engine ไม่ hand-write)** · + reverse link report-cnt/[fNo] → "🧾 ใบวางบิล FRI… →" (read-only). **data fix prod:** 14 forwarder+item GZS260618-1 + invoice 54 (unpaid) 3700→3300 · **FRI2607-00019 total 1268.09→1174.25** (`scripts/reprice-pr002-gzs260618-2026-07-07.mjs`).
>
> **🔎 PUNCH-LIST (faithful-look next · NO P0 · paradigm ~done):** service-orders 11 legacy row-buttons vs 4 · wallet balance QR-widget (wallet.php:216/266) · yuan status wording (owner call) · drivers/new bottom-bar 3-tab (forwarder-driver.php L737).
>
> **🔴 CARRYOVER (owner-decision · ไม่ auto):** (1) **PR10112 juristic** = เลขนิติ 0105569001734 = **PR10366 ถืออยู่แล้ว** → owner: ใช้ PR10366 / merge. (2) **52079** (ตู้ GZS260617-1 · PR002 · ฿300) อาจ over-charge เหมือนกัน (SVIP) — ยังไม่แตะ (คนละตู้) → เช็ค. (3) **rate re-price = ต่อลูกค้าที่ save card เท่านั้น** — tier-sibling re-price ตอน save/measure ของตัวเอง (conservative) → ถ้า owner อยาก mass-reprice ทั้ง tier = งานแยก. (4) owner-verify สด: report-cnt/GZS260618-1 (3300) · billing-run/54/print (1174.25 · LOGISTICS 225-2-91144-0) · forwarder-pay QR ลูกค้า = LOGISTICS. (5) owner action: RECEIPT_TOKEN_SECRET Vercel = `6FVggGCMqP4VzXqkJV4LahCwRPN4ahXNy0aEJ1wKQvHvpC0fDuij5JhJf8iBz5b7` · grant purchaser positions · carryover เดิม (อี้อู status). **NEXT = faithful-look punch-list + owner-verify money-routing สด (บอกภูมิ).**

---

# 🧩 2026-07-06 ค่ำ (เดฟ · session-close) — momo data-quality + RBAC purchaser role→POSITION axis + customer-tools (tax search / actionable dup / row-count / PR-button) → ALL 4 BRANCHES · read FIRST

> **🏁 SESSION CLOSE.** all 4 branches (main = dave-pacred = Poom-pacred = InwPond007) → **`00baa558`** (push ปิด · Vercel prod). base = `6ceb790e` (earlier continuation: pricing batch ค่าเทียบ250/ติ๊ก-default-off/locked-pair · purchaser per-order mig 0241 · yuan CustomerPicker · momo-invoice · own-warehouse cost — all pushed). gate ทุก commit: **BUILD_EXIT=0 อ่านจาก log** (NODE_OPTIONS=8192 · `rm -f .next/dev/types/validator.ts` ก่อน) · tsc 0 · workflow (ultracode) diagnose→build→adversarial-verify · money-review. **NEXT FREE mig = 0243** (0241 purchaser adminidpurchaser · 0242 purchaser positions · **applied prod+dev**). ⚠️ ห้าม `next build` ตอน preview `next start` เปิด (ทับ .next → crash). 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`) · `.env.local`=PROD · DEV-SYNC ทุก mig.
>
> **✅ SHIPPED (3 commits · workflow-built · money-safe):**
> - **momo data-quality** (`3498b8a8`) — forwarder rows จาก MOMO: (1) **รูป** `raw.images[0]`→fcover (`extractCoverFromMomoRaw` · MOMO ส่ง URL เต็ม → resolveLegacyUrl passthrough · default "" · prod ส่งรูปน้อย 1/333) · (2) **ประเภทสินค้า (money)** — review grid + cron เดิม hardcode fProductsType="1" ทุกแถว → map `raw.type`→fproductstype ผ่าน `momoTypeToProductType` (general→1·tis→2·fda→3·control→4 · unknown→1 no-regression · prod-tested) → เข้า rate-card **ต้นทุน+อากรถูก tier** · เป็น **editable default** ในหน้า review (admin ตรวจก่อน commit) · ไม่แตะ formula · (3) **ที่อยู่** verify ภูม fix ครบ (tb_address_main fallback · ไม่ force รับเองโกดัง).
> - **RBAC purchaser role→POSITION** (`e430782c` · mig 0242 · [[rbac-3axis-position-workspace]]) — owner: purchaser/หัวหน้าสั่งซื้อ เป็น *role* ตีกับ ultra/super (คนต้องมีทั้ง money-tier + work-function · role dropdown เดียวเลือกได้อย่างเดียว). **ย้ายไปแกน position/workspace** (admin_positions.workspace_role · mig 0221). **cost/profit visibility คงบน role axis 100%** (money-visibility.ts UNTOUCHED) → admin_web=role ultra/pricing(เห็นต้นทุน)+ตำแหน่ง Pricing → เห็นหมด · ผู้สั่งซื้อ=role sales+ตำแหน่ง ผู้สั่งซื้อ → scope เห็นเฉพาะงานตัวเอง ไม่เห็นต้นทุน. ถอด purchaser ออกจาก role picker (0 คนถือ → ปลอดภัย) · seed 2 positions (biz_cs). **เจอ+แก้เอง:** reassign withAdmin gate แคบไป (หัวหน้าสั่งซื้อผูกผ่าน position + base role ธรรมดา เห็นปุ่มแต่กดไม่ได้) → ขยาย gate กว้าง + body `canReassignPurchaser(workspaceRole,roles)` เป็น gate จริง. 26/26 test.
> - **customer-tools** (`00baa558`) — juristic dead-end root-fixes: (1) **customer search เลขนิติ+ชื่อบริษัท** (resolve tb_corporate userids fold เข้า .or() เดียว · digit-normalize เลขภาษี · count consistent) → หา juristic เจอ · (2) **error เลขนิติซ้ำ actionable** `tax_id_already_used:<PR>:<company>` → client โชว์ PR link + ชื่อบริษัท (แทน dead-end) · (3) **row-count selector** 50/100/250/500/1000/ทั้งหมด (`components/admin/page-size-select.tsx` + `parsePageSize` in paginate.ts · all cap 5000 · reusable · 21/21 test) · (4) **ปุ่ม "รันเลข PR ลูกค้าใหม่"** ย้ายจาก account-tools (ฝังลึก ~550 บรรทัด) ขึ้น identity header (ultra-gate + action re-assertion คงเดิม · no double-mount · §0d).
>
> **🔴 CARRYOVER (owner-decision · ไม่ auto):**
> - **🔴 PR10112 juristic:** เลข 0105569001734 (ยูนีค โกลบอล/UNIQUE GLOBAL) = **PR10366 ถืออยู่แล้ว** (นิติ · verified · 21/01/2026). เอกสาร UNG = ของ PR10366. เลขนิติผูก 1 บัญชี → owner เลือก: ใช้ PR10366 / รวม-ย้ายรหัส PR10112→PR10366. เครื่องมือพร้อมหมด (ผมไม่ auto — identity/money).
> - **owner action:** (1) `RECEIPT_TOKEN_SECRET` ใน Vercel = `6FVggGCMqP4VzXqkJV4LahCwRPN4ahXNy0aEJ1wKQvHvpC0fDuij5JhJf8iBz5b7` (ครั้งเดียว · rotate=QR เก่าพัง) · (2) **ให้ ผู้สั่งซื้อ/หัวหน้าสั่งซื้อ = ตำแหน่ง (position dropdown) ไม่ใช่ role** (mig 0242 · ผู้สั่งซื้อ=base role sales+position ผู้สั่งซื้อ) · (3) enter cost rate report-cnt GZS260618-1 · (4) เคาะ PR10112.
> - row-count selector ขยายไป admin list อื่นได้ (reusable) · pagination 13/หน้า · นิติ eyeball สด · อี้อู status (packing list รอ owner).

---

# 📦 2026-07-06 (ภูม) — `/admin/forwarders` (ฝากนำเข้า) list → legacy PCS fidelity (ตีเส้นตาราง + แสดงข้อมูลเป๊ะ) → Poom-pacred · 🔴 เดฟ review · read FIRST

> **🏁 SESSION CLOSE (ภูม: "เซฟ Poom-pacred · เดฟเอาไปตรวจ · ภูมิทำต่อพรุ่งนี้").** Branch **Poom-pacred = `794cfe08`** (= HEAD · pushed · clean · local==origin). Gate เขียวทุก commit: **tsc 0 · lint 0** (`rm -f .next/dev/types/validator.ts` ก่อน · อ่าน exit จริง ห้าม `| tail` · `cd /c/Users/Admin/pacred-web/pacred-web` ก่อนทุก git/node — cwd reset บ่อย). localhost/.env.local = **DEV** · prod = เดฟ · push เฉพาะ Poom-pacred. **2 files · no migration · 2 commits `1e03bc1d`(gridlines)→`794cfe08`(data-display).**
>
> **🎯 งาน (ภูม · "หน้าฝากนำเข้าของเรา `/admin/forwarders` เอาให้เหมือน legacy `pcscargo.co.th/member/pcs-admin/forwarder/` เป๊ะๆ เหมือนที่ทำ Dashboard `/admin` — หน้าตา/การจัดวาง/การแสดงข้อมูล/การตีเส้นตาราง").** วิเคราะห์: คอลัมน์ตรง legacy อยู่แล้ว · gap = (1) เส้นตาราง (2) การแสดงข้อมูลบางฟิลด์.
>
> **✅ ทำแล้ว (เทียบ legacy `forwarder.php` list-mode L571-712 · §0b อ่าน source จริงก่อนเคลม):**
> - **ตีเส้นตารางเต็ม** (`1e03bc1d`) — legacy `table-bordered` = เส้นทุก cell. ทำ scoped `[&>thead>tr>th]:border [&>tbody>tr>td]:border` เฉพาะ **main table** (line 772) · **sub-table ที่ nested ไม่แตะ** (กัน over-scope) · คง status row-tint.
> - **4 การแสดงข้อมูลตาม legacy** (`794cfe08` · `forwarders-table.tsx`): (1) **เลขพัสดุจีน = แดง** (L650 `bg-danger text-white` → `bg-red-600 text-white`) · (2) **หมายเหตุ (fNote) บล็อกแดง** ในรายละเอียด (L635 · conditional `r.note`) · (3) **ประเภท : ทั่วไป/มอก./อย./พิเศษ** (L622 · label จาก `pcs-admin/include/function.php` L640 nameProductsType ตัวจริง — ไม่ใช่ตัว drift ในโค้ด) · (4) **ขนส่งไทย (nameShipBy)** เหนือเลขพัสดุไทย (L656 · reuse `SHIP_BY_LABEL` จาก `actions/admin/reports-profit-types` = SOT เดียว ไม่สร้างซ้ำ).
>
> **🔧 fetch (`page.tsx`):** เพิ่ม `fshipby,fproductstype` ใน select + RawForwarderRow + Row + mapping. **§0e coid-guard:** probe DB ยืนยัน 2 คอลัมน์นี้ **มีจริง** ก่อน → query เต็มยังคืน **166 แถวครบ** (fshipby: PCS:104/PCSF:8/PCSE:3/2:1 = SHIP_BY_LABEL cover 100% · fproductstype ทั้งหมด "1"=ทั่วไป).
>
> **🔎 verify:** gate tsc 0 · lint 0 · DB 166 rows · **DOM computed-style สด** (screenshot MCP บั๊ก `clip.scale` → เช็ค DOM แทน · แม่นกว่า): red tracking = `lab(48.4 77.4 61.5)` = แดงจริง+ตัวขาว · carrier "Pacred เหมาๆ (PRF)" ขึ้นถูก · ประเภท ×49 แถว · หน้าโหลดครบ 166 + ยอดรวม ไม่มี error. **⚠️ หมายเหตุ (fNote) DEV มี 0 แถว → เห็นจริงตอน prod เท่านั้น** (โค้ด conditional ถูก · faithful).
>
> **🔴 NEXT (เดฟ review · ภูม พรุ่งนี้):** ถ้าภูม/พี่ป๊อป เปิดดูแล้วเจอจุดอื่น "ไม่เหมือน" อีก (เช่น sub-table แตกกล่องอยากได้ tracking แดงด้วย · nameShipBy บน sub-table · print/detail fidelity) → เก็บต่อ. Legacy source = `C:\xampp\htdocs\pcscargo\member\pcs-admin\forwarder.php` (list-mode) + `include/function.php` (nameShipBy L185 / nameProductsType L640).
# 🏗 2026-07-06 (เดฟ · 7h autonomous run → SESSION CLOSE) — พี่ป๊อป hand-written SPEC BUILD: 6-status cargo-import lifecycle + คีย์รับสินค้า + บัญชี → FOUNDATION 8/8 + 3 display enhancements + integrate ภูม → ALL BRANCHES · read FIRST

> **🏁 SESSION CLOSE (owner: "งานต่อไป ไปทำต่อคอมที่ทำงาน · ตรวจ+รวมงานน้อง · สรุป+อัพเดท learning/memory/skills/md ทุกไฟล์ · ปิด session push all branch").** **dave-pacred = main = Poom-pacred = InwPond007 = `<HEAD>`** (all synced ที่ปิด · main→Vercel prod). ระหว่าง run push เฉพาะ dave-pacred (owner directive) → ตอนปิด push ครบ. **integrate ภูม Poom-pacred (`136e83ed`)** = docs-only (forwarders-list legacy-fidelity save-point + worklog · โค้ด forwarders gridlines/red-tracking อยู่ dave แล้ว · FF clean · no migration) · InwPond007 0-ahead. gate: **tsc 0 · build 0** (`NODE_OPTIONS=8192 node node_modules/next/dist/bin/next build` · อ่าน BUILD_EXIT จริง · ⚠️ เครื่อง lag: build 25-27s เมื่อว่าง · shell 2-min timeout → `timeout:480000`). **NO migration this run — newest 0240 applied prod · NEXT FREE = 0241.** 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`). resume คอมที่ทำงาน: `git fetch && git pull origin dave-pacred`. **method:** orchestrate 1 agent-at-a-time (worktree isolation · prompt "NO sub-agents · NO Monitor polling" กัน zombie/stall) · integrate FF → re-build → push · money items ผมอ่าน diff เอง · **audit-first: 2/10 spec items มีอยู่แล้ว (container-journey wired · routeOrderOf) → grep ก่อน build เสมอ.**
>
> **📜 SPEC (พี่ป๊อป เขียนเองกับมือ · อ่าน "ต้นจนจบ ห้ามเดา" · faithful capture ที่ [`docs/research/pop-spec-2026-07-06/`](../../docs/research/pop-spec-2026-07-06/)** — `00-SPEC.md` structured + `01/02` raw + 6 UI images + `03-BUILD-PLAN.md` queue). = **6-หลัก+2-ย่อย lifecycle** (3 กำลังส่งมาไทย · 4 ถึงไทย🤎 [+ขาด💗 box-count] · 5 รอชำระ [+เตรียมจัดส่ง=แนบสลิปยังไม่ปล่อยคนขับ] · 6 เตรียมส่ง · 7 กำลังจัดส่ง · 8 สำเร็จ) maps 1:1 กับ `lib/admin/forwarder-status.ts` fstatus. **KEY RULES:** พื้นฐานก่อน·เท่า PCS·ห้ามนอกกรอบ · **คลังห้ามเห็นต้นทุน/กำไรเด็ดขาด** · แนบสลิป→เตรียมจัดส่งแต่ไม่ปล่อยคนขับจนบัญชีอนุมัติ · ตรวจสลิป 3 ขั้น (นิติ WHT 1% · กันเวียนเทียน) · Picking≠Delivery · **ค่าส่งไทยห้ามลืม**ก่อนวางบิล. Audit-verdict = platform ~80% at spec-foundation.
>
> **✅ FOUNDATION 8/8 SHIPPED (each: agent build → I integrate FF + re-build + push · money items diff-read by me):**
> - **#1 สี** = คง amber เดิม (owner answer "คงamber") — no change.
> - **#2 ขาด/ครบ pill** — report-cnt `cnt-list-table.tsx` ชมพู "ขาด N"/ขาว "ครบ" จาก `container-completeness.ts` (data already passed).
> - **#3 ค่าส่งไทย "ห้ามลืม" gate** (`c1f29101`) — `lib/forwarder/domestic-shipping.ts` +`isThShippingCostRequired/Missing` SOT+18 tests · billing-run `th_ship_missing` flag + `allowMissingThShip` backstop + CS notify · add-client badge/banner/confirm. owner answer: "auto ให้โกดัง (แกะ legacy backoffice) + CS แก้ได้ + notify."
> - **#4 handoff btn + wh cost-hide audit** — report-cnt/[fNo] `submitWarehouseAccountingHandoff` (notify-only · NOT fstatus flip) + money-visibility masks confirmed.
> - **#5 consolidation แบบ1+แบบ2** (`75c3cecc` · MONEY-REVIEWED by me) — NEW `/admin/billing-run/consolidate`. `listConsolidationCandidates` (read-only · Σ exact outstanding_thb+mao_fee_thb from listEligibleForwarders · completeness ครบ/ขาด conservative) + `createBatchBillingRunInvoices` (**delegates to createBillingRunInvoice** per customer · sequential doc-no · guards NEVER waived allowUnmeasured/allowMissingThShip=false · sanitized ≤200). **ZERO direct money writes** (verified diff). owner answer: "แบบ1 (ตู้ตรวจครบทุกตู้) + ต้องทำแบบ2 (batch) ได้ด้วย."
> - **#6 slip 3-step UI** (`billing-run/[id]/billing-run-actions.tsx`) — ordered round1→bill-no→dup-check→ใบเสร็จ (backend done, UI formalized).
> - **#7 Picking≠Delivery** — NEW `drivers/[id]/picking-list` (grouped fpallet→ตู้→tracking) + `[id]/print` relabeled Delivery Note.
> - **#8 China receive-bill** (`0f9f8782`) — NEW `forwarders/[fNo]/receive-bill` printable slip (SM barcode+QR via `lib/barcode.ts` · PR · transport เรือ/รถ/แอร์ via `cabinet-transport.ts` · วันรับ fdatestatus2 · box · sender-tel write-in · เซ็นรับ+ถ่ายรูป boxes) + button on forwarder detail. = ถึงโกดังจีนแล้ว. print-only.
>
> **✅ ENHANCEMENTS (spec-direct · SAFE display/print · zero money · zero writes):**
> - **E1 จอมอนิเตอร์ กำลังจัดส่ง** (`43d6ed6d`) — NEW `drivers/monitor` live board (open batches fdstatus='1' · driver avatar/ชื่อเล่น/เบอร์ · progress ส่งแล้ว N/M + delivery photos · router.refresh 30s) + sidebar+header §0d. NO money shown.
> - **E2 สติกเกอร์ route-order** (`77d8d8d6`) — NEW `drivers/[id]/stickers` A4 2-across address labels sorted `routeOrderOf` near→far + Code128 · extracted `routeOrderOf`→shared SOT `lib/admin/driver-route-order.ts` (fix-at-root · killed dup). print-only.
> - **E3 container status-ladder จีน↔ไทย = ALREADY EXISTS** — `buildContainerJourney`→`<ContainerJourneyPanel>` on report-cnt/[fNo] (7-stage ปิดตู้→transit→ท่า→customs→โกดัง→เตรียมส่ง→ส่งแล้ว · G4 2026-06-30). audit-first caught it → no rebuild.
>
> **🔴 REMAINING SPEC = OWNER-INPUT / money-sensitive (ห้ามเดา · flagged, NOT guess-built):** (1) **MOMO 3-รอบ key-in** (คร่าวๆ/packing/ปิดตู้ · re-upload same-slot · XLSX upload · ปิดตู้→fstatus'1' กำลังส่งมาไทย) — status→money pipeline · exact round semantics ต้องเคาะ · partial exists `/admin/api-forwarder-momo`. (2) **กระสอบรวม** (consolidated small-item sacks) — NEW data-model · owner. (3) **ระบบคีย์ข้อมูล China** (ชิ้นเล็กคีย์จุดเดียว/ชิ้นใหญ่ยิง QR · owner "ทำพื้นฐาน · บินไปจีนติดตั้ง") — whole China-ops build. (4) **ระบบเบิกเงิน** จีน↔บัญชีไทย — money. (5) **CS จ่ายแทนลูกค้า** (ออกบิล+QR→ลูกค้าโอน→CS แนบสลิปแทน · = existing uploadBillingRunSlip flow · money-adjacent · ต้องเคาะ CS surface). (6) **accounting step-2 doc-forms tower** — จ่าย-side owner "ถามพี่แนท" = blocked · รับ-side มีแล้วเยอะ (แจ้งหนี้=billing-run/เสร็จ=receipt/กำกับ=etax/เสนอราคา=quote) · gaps ใบประเมิน/ใบส่งสินค้า/ใบตามหัก = own phase. **Standing carryover** from earlier blocks ยังอยู่ (accounting B1-B7 · #52089 · MOMO drift ฿294k · rate cards).

---

# 📊 2026-07-05 (ภูม) — Admin dashboard `/admin` → legacy PCS fidelity (ALL 14 tabs · columns+data+zebra+counts) + coid งานหาย fix → Poom-pacred · 🔴 เดฟ review · read FIRST

> **🏁 SESSION CLOSE (ภูม: "เซฟ Poom-pacred · เดฟเอาไปตรวจ · ภูมิทำต่อพรุ่งนี้").** Branch **Poom-pacred = `5ce43b15`** (= HEAD · pushed · clean · local==origin · 0-behind dave). resume: `git -C <root> fetch && git pull origin Poom-pacred`. Gate เขียวทุก commit: **tsc 0 · lint 0** (อ่าน exit จริง · `cd <nested-root>` ก่อนเสมอ — cwd reset บ่อย). localhost/.env.local = **DEV** · prod = เดฟ · push เฉพาะ Poom-pacred. **1 file · no migration · 8 commits `41f24f0a`→`5ce43b15`.**
>
> **🎯 งาน (owner via ภูม · "พี่ป๊อปทำงานผ่านหน้านี้ทั้งวัน · ทำให้เหมือน PCS เป๊ะ ครบทุกหน้า ไม่ตกหล่น"):** ทำ tab-strip ของ `/admin` (CEO dashboard · `app/[locale]/(admin)/admin/page.tsx`) ให้เหมือน legacy. **ต้นตอ:** legacy มี**คอลัมน์เฉพาะต่อแท็บ** (6 layouts) · เราใช้**ตาราง 4-col generic ตัวเดียวทุกแท็บ** (ยัดทุกอย่างลง `detail` HTML). blueprint ครบ 14 แท็บ: [`docs/research/dashboard-tabstrip-fidelity-2026-07-04.md`](../../docs/research/dashboard-tabstrip-fidelity-2026-07-04.md).
>
> **✅ เสร็จครบ 14 แท็บ (legacy src `pcs-admin/include/pages/home/Cargo/CEO/*` + `oop/shopTableAll.php`/`forwarderTableAll.php`):** ShopTabTable(8-col) · ForwarderTabTable(9) · PaymentTabTable(9) · UsersActiveTable(6) · WithdrawTable(7) · PayShopTable(6 · **repoint sales_payouts[ว่าง]→`tb_shop_pay_h` list+count §0e**) · topup(=generic 4-col ตรง legacy อยู่แล้ว) + shared **MiniBadge/NoteBlock/UpdateCell** + **zebra สีสลับ** (owner "ไม่ลายตา"). Rich cells: VIP(coID) · Sale(adminIDSale) · IPC(adminidcreate) · **หมายเหตุ**(hnote/fnote + visibility badge + ผ่านมา) · กำหนดชำระ(hdatepayment · shop2) · อัปเดต(status-date + `relativeTimeTh` ผ่านมา + adminIDUpdate). VIP+Sale ดึงจาก **tb_users join** (เพิ่ม coID/adminIDSale ใน `loadUsersByUserId`). SHOP_STATUS/FWD_STATUS/FWD_SHIPBY/SHOP_USER_LABEL/CHANNEL_LABEL maps จาก legacy function.php.
>
> **🔧 correctness:** **forwarder6/62** — เดิม เตรียมส่ง=`fstatus'4'`(ผิด · past-session error) · ตอนนี้แยก `fstatus=6` ตาม open driver-item (เตรียมส่ง=ไม่มีคนขับ · กำลังจัดส่ง=มี open driver-item) ทั้ง **fetch + badge counts** (fanout ดึง fstatus6-ids + open-driver-fids มา partition) → **DB-verified 15+2=17**. + payShop count repoint ด้วย.
>
> **🔴 2 บั๊กที่เจอ+แก้ (บทเรียน · จากรีบ push โดยไม่เช็ค render):** (1) **coid งานหาย** — เพิ่ม `coid/adminidsale/promoid` ใน shop select แต่ 3 คอลัมน์นี้**ไม่มีบน tb_header_order** (อยู่บน tb_users/promo-join) → query พังทั้งอัน → 4 แท็บ shop ว่างทั้งที่ badge บอกมีของ (ภูม catch) → probe DB เจอ → เอา VIP/sale จาก tb_users แทน · verify shop2=2/shop4=4 rows. (2) tsc no-overlap + lint bare-destructure. **→ ตั้งแต่นั้น probe DB ยืนยันทุก query คืนแถวจริงก่อน push ทุกอัน** (tsc ไม่จับ column ที่ไม่มี = runtime soft-fail เป็นว่าง). **learning: dashboard money-page · verify DATA ไม่ใช่แค่ tsc.**
>
> **🔴 NEXT (ภูมิ พรุ่งนี้ · เดฟ review):** **(1) browser-verify สด 14 แท็บ** (ผม login admin ไม่ได้ · verify แค่ query คืนแถว · หน้าตา render ต้องกดจริง — โดยเฉพาะ ฝากสั่งซื้อ→รอชำระเงินสินค้า ที่เคยว่าง). (2) minor follow-up: forwarder VIP/Sale badge (มี note/update แล้ว) · avatar รูปใน usersActive/payment (skip ไว้). (3) dev server หนัก — /admin compile ครั้งแรก ~40-60วิ (dev เท่านั้น · อย่ารัน tsc หนักตอน dev เปิด = แย่ง CPU).

---

# 💳 2026-07-02 (เดฟ · integrator) — checkout/payment/receipt fixes + wallet direct-cut settle + /admin/incidents "แก้หมด" + cart UX + รวมงานน้อง → ALL BRANCHES · read FIRST

> **🏁 STATE. main = dave-pacred = InwPond007 = `2f866e2d`** (release branches ที่ verified tip · main→Vercel prod) · **Poom-pacred 1-ahead** (ภูม push ต่อ · integrate next session). gate ทุก commit: build 0 (NODE_OPTIONS=8192 · direct-node OOM ที่ 4096 · อ่าน BUILD_EXIT จาก log) · tsc 0 · adversarial money-review PASS. 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`) · `.env.local`=PROD. **mig 0240 (ภูม `momo_box_detail`) applied prod+dev · NEXT FREE = 0241.** ⚠️ "use server" exports async ONLY. 📌 **ห้ามรัน `next build` ตอน preview `next start` (config `pacred-web-prod`) เปิดอยู่** → ทับ `.next` → server crash "module factory not available" (bit me · crashed owner page) → stop preview ก่อน build เสมอ.
>
> **✅ SHIPPED (owner-driven · ทั้งหมด prod · money paths NOT authed-click-tested = no login · owner verify สด):**
> - **wallet direct-cut settle** (`cfd974f5`) — ฝากนำเข้า slip-pay (type='4') เป็น DIRECT-CUT (submitForwarderPayment ไม่ credit wallet · เงินเข้าธนาคารตรง) แต่ approve ยัง debit wallet → ฿0 → neg-guard block "ยอดกระเป๋าไม่พอ". FIX `lib/wallet/classify-approve-row.ts` (direct-slip=reforder set+reforder2 ว่าง+ไม่มี paydeposit → walletDelta 0 · funded/cascade ยังหัก+guard armed) wire 3 approve paths + page label "ชำระโดยสลิป" + test 15/0. prod probe: misclassify=0 · backlog=1 (#105544). **หลัง deploy กดอนุมัติ #105544 ใหม่ = ผ่าน (ไม่ต้อง hand-fix DB).**
> - **/admin/incidents "ไล่แก้ให้หมด"** (`62543980`+`1ce0a553`) — F1 proxy Phase-2→PHASE_1_CARVEOUTS (เข้าไม่ได้ทุก role ยกเว้น super) · F2 reopenIncident (illegal→open · CHECK-safe) · F4 badge→LIVE_INCIDENT_STATUSES · F5 "มอบหมายให้ฉัน" · **F3 wrap 13 throwing server action ด้วย withObservability** (capture-then-RETHROW · money logic ไม่แตะ · "use server"-legal export→*Impl) · **triage 7 incident = transient ทั้งหมด** (navigation-abort บนมือถือ) → `is-transient-abort.ts` suppress (whole-string · chunk-load/error จริงไม่ drop · test 27/0).
> - **ใบเสร็จคาร์โก้** — เหมาๆ ฿100 (`405ed2eb` Lane C: adminIssueForwarderInvoice เก็บ mao_fee_thb via computeForwarderDebitBatch) + backfill re-issue #105544 ตรงใบแจ้งหนี้ (2157/2135.43 · `backfill-receipt-mao-fee-2026-07-02.ts --reissue`) + **print กันล้น 2→4 แผ่น** (`41bc9e3f` @page margin 5→3 · height 285 · signature 18→13mm).
> - **Lane A/B checkout/payment** (`09e8097b`) — A: PostgREST 1000-row truncation (cart >1000 รายการ ตกหล่น) → `lib/supabase/fetch-all.ts` 6 จุด + chunk insert/delete. B: payment-QR routing — ไม่รับเอกสาร→gen PromptPay นิติ 55856-6 · รับเอกสาร→+VAT7% TRADING 6699 · LOGISTICS 11440=domestic only (`buildServicePromptPayQrDataUrl` gen เสมอ · ลบ hardcode isDomesticDeliveryLeg).
> - **cart/order UX+stability** (`0419f534`) — local toggle compute · qty-fail revert · ฿ per-line · confirm §0f · row grouping §0g. + รวม ภูม (MOMO box/status/cabinet · box-split sibling rows) + ปอน (m/dashboard/nav/promo).
>
> **🔴 NEXT / carryover:** integrate Poom-pacred (1-ahead) · owner verify สด (#105544 อนุมัติ · re-print ใบเสร็จ · cart >1000 · phone-scan QR) · 2 non-blocking owner/บัญชี: (1) VAT 7% ลูกค้าสแกนจ่ายแล้วแต่ book ฐาน base ที่ settle — persist VAT ตอน settle ไหม; (2) wallet top-up getDepositQr ยังใช้ static helper · F3-deferred done · AP write-side go-live (ก๊อต co-sign) · Yiwu/อี้อู status (owner จะส่ง packing list+tracking). memory `checkout-payment-receipt-fixes-2026-07-01`.

---

# 🚚 2026-07-01 (เดฟ · integrator) — FREIGHT SERVICES BUILD (long-run · 6 tasks) + platform blind-spot hunt (16 confirmed · 9 fixed) → ALL 4 BRANCHES · read FIRST

> **🏁 STATE.** **main = dave-pacred = Poom-pacred = InwPond007 = `719159b5`+** (all 4 synced · main→Vercel prod). gate tsc 0 · full `next build` exit 0. 🔑 prod pw chat-only `DqOzfEZVXfMHIryz` · dev `n61OKDy28QcrB1ZJ` (`lozntlidlqqzzcaathnm`) · `.env.local`=PROD · MOMO web `PacredShipping/PcrdShip@TH`. **migs 0236 (tb_*_tax_invoice.bank_account_key + customs_declarations own-name/confirm cols) + 0237 (customs_declarations.service_collected_at) + 0238 (ปอน `marketing_planner` · 5 mkt_* tables · Content Marketing Planner) ALL applied prod+dev · NEXT FREE = 0239** (always `ls supabase/migrations|tail`). ⚠️ "use server" exports async ONLY.
>
> **🔑 OWNER DESIGN RULE (load-bearing · owner pushed back "อย่าทำหน้าใหม่จัดเรียงมั่วๆ"):** ทุกหน้า/ฟีเจอร์ใหม่ ยึดหน้าตา+การทำงานจาก **ฝากสั่งซื้อ (shop-order · ลูกค้า+แอดมิน) + รายงานตู้ (report-cnt)** = base หลัก. spec + path:line CODE-MAP + BUILD-PLAN: [`docs/research/freight-services-spec-2026-07-01.md`](../../docs/research/freight-services-spec-2026-07-01.md).
>
> **✅ FREIGHT SERVICES (6 tasks · owner directive after the freight↔cargo data merge · each: agent-build→I-review-money+gate+apply-data/mig+push):** **#13** CIF/services (CIF=incoterm-NOT-service-key · `customs_clearance`/`freight_import/export` already isLive · fix 93 rows `payment_term`→`incoterm` prod · ส่งออก grid soon→live) · **#14/#15** `/admin/leads` source-tabs (ทุกที่มา / PCS=every-NON-freight-source / Freight / Freight-รอตามลูกค้า-ไม่มีเบอร์) + import **422 freight CRM call-rows** prod (344 source=freight from tb_users marker · 78 source=freight_no_phone from CSV · idempotent `[FREIGHT:PR]` · NOT double-creating customers) · **#16** report-cnt floating-bar 📦→`/admin/report-cnt/customs-doc`→reuse `<CargoDocPicker>`→doc-mode chooser (ใบกำกับ→TRADING+VAT7% / else→SERVICE via `resolvePaymentAccount` · NEVER hardcoded · VAT via `computeTaxForMode`) · persist `bank_account_key` on 3 issuance writers + READ on /admin/accounting/etax (§0e) · **#17** ใบขนพ่วง (own-name+consignee · HS-check panel read-only · service-fee · `adminSendCustomsDraftToCustomer`→LINE · **public `(public)/customs-confirm/[token]`** resolve-by-confirm_token-only · เฟิมยอด→confirmed-gated collect service+duty+VAT→**SERVICE** · idempotent atomic-claim · NO Pacred VAT line · pass-through) · **#18** token-scoped public PDF (`?token=` on the 3 PDF routes · `lib/customs/confirm-token-access.ts` · token must match that decl's confirm_token+own-name+sent/confirmed else 404). **🔴 INTEGRATOR CATCH:** #17 agent overloaded `paid_through_promptpay` (mig 0057 admin-editable attr) as the collect-latch → §0e unsafe (an unrelated edit skips a real collection) → **mig 0237** dedicated `service_collected_at`.
>
> **🔎 PLATFORM BLIND-SPOT HUNT (Workflow · 31 agents · 1.6M tok · find→adversarial-verify→fix · [`docs/research/platform-blindspot-2026-07-01.md`](../../docs/research/platform-blindspot-2026-07-01.md)):** 16 confirmed (**0 P0 · 6 P1 · 5 P2**) · platform fundamentally SOLID (freight batch well-engineered · token flow secure · bank-SOT correct · ADR-0016 honored). **9 AUTO-FIXED** (gated): report-cnt→Super/CEO sidebar (was URL-only) · customs reject+send-draft atomic-claim (was silent false-success race) · HR-recruitment force-dynamic (was 500-risk) · cashback NaN guard · cargo-create 23505→clean error · report-cnt LANE-A no-op skip · PDF routes audit col · lead-source shared SOT `isPcsLeadSource`+test(14) · lead row §0g border/badge. **🔴 2 OWNER-FLAGS (P1 · NOT auto-fixed):** (1) `forwarder-pay-modal.tsx:138` hardcodes `isDomesticDeliveryLeg:true`→**always LOGISTICS** · int'l freight should→SERVICE (owner: which forwarder flag/service_key marks the domestic leg?) · (2) cargo `declared_value` editable UP with no cost-baseline lock (owner/accounting: cap ≤cost + override-reason?).
>
> **🔴 NEXT / carryover:** the 2 owner-flags ↑ · **QA click-test the 6 freight flows** (gated+build-green but NO admin/customer login = not authed-tested · §0c) · #17 flow VERIFIED-in-code complete (customer sees PDF[#18]+SERVICE-QR) · cargo cost (0669) รอ MOMO วางบิล · MOMO drift ฿294k (เคาะตู้) · rate cards (pricing) · 7 yuan ถามทีหลัง. memory `session-2026-07-01-freight-services-build`.

---

