# `containers`

> 💀 rebuilt twin (mostly empty — do not write here for live data) · referenced **1×** in code

Rebuilt container table.

## Columns observed in code

| Column | W | Meaning |
|---|:-:|---|
| `container_no` |  |  |
| `id` |  |  |
| `origin_warehouse` |  |  |
| `status` |  |  |
| `total_volume_cbm` |  |  |
| `total_weight_kg` |  |  |
| `transport_type` |  |  |

<sub>✏️ = column written by code (high-confidence). Others observed in reads/filters — may include join bleed. Casing as the code uses it.</sub>

## Referenced by

- `app/[locale]/(admin)/admin/containers/[id]/hs/page.tsx`

---

<sub>Derived from code usage on 2026-06-02 — not from migration files. See [../README.md](../README.md).</sub>
