# `settings`

> 💀 rebuilt twin (mostly empty — do not write here for live data) · referenced **2×** in code

Rebuilt settings twin.

> Live = tb_settings.

## Columns observed in code

| Column | W | Meaning |
|---|:-:|---|
| `crate_fee_base` |  |  |
| `free_shipping_enabled` |  |  |
| `free_shipping_threshold` |  |  |
| `id` |  |  |
| `juristic_discount_pct` |  |  |
| `juristic_discount_threshold` |  |  |
| `qc_fee_per_item` |  |  |
| `service_fee` |  |  |
| `yuan_rate` |  |  |

<sub>✏️ = column written by code (high-confidence). Others observed in reads/filters — may include join bleed. Casing as the code uses it.</sub>

## Referenced by

- `app/[locale]/(admin)/admin/settings/page.tsx`
- `app/api/settings-rate/route.ts`

---

<sub>Derived from code usage on 2026-06-02 — not from migration files. See [../README.md](../README.md).</sub>
