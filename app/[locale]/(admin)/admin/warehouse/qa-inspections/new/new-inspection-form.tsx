"use client";

/**
 * TOMBSTONE — Wave 3 cleanup. QA inspection module deferred to Phase C.
 * The page using this form was tombstoned; this stub remains so any
 * mid-flight imports don't 500.
 */
export function NewInspectionForm(_props: { cargoShipmentId: string; shipmentCode: string }): null {
  void _props;
  return null;
}
