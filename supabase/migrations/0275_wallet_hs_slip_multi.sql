-- ════════════════════════════════════════════════════════════
-- 0275 · tb_wallet_hs.slip_paths — แนบสลิปได้หลายใบต่อ 1 การจ่าย
-- ════════════════════════════════════════════════════════════
-- Owner (2026-07-23, verbatim · เคสจริง PR172 จ่ายแทนลูกค้า):
--   *"ลูกค้า จ่ายมาสองสลิปนะครับ ยอดทั้งหมดเท่ากับยอดที่ออกใบจ่ายแทนไปเก็บเงิน
--     แต่ตอนนี้แนบสลิปได้แค่ใบเดียว แล้วจะส่งแนบไปให้บัญชีตรวจต่อยังไงครับ
--     … สำหรับเราเราว่า ทำแนบสลิปได้สองใบก็จบแล้วไหม"*
--
-- เคสจริง: ใบ ฿24,130.72 · ลูกค้าโอน 2 ครั้ง 18/07 ฿8,918.42 + 20/07 ฿15,212.30
--          = ฿24,130.72 พอดี แต่ระบบรับสลิปได้ใบเดียว → บัญชีตรวจไม่ครบหลักฐาน
--
-- ── legacy ทำยังไง (แกะแล้วตามที่ owner สั่ง) ────────────────────
-- legacy PCS **แนบได้ใบเดียวจริง**: `member/forwarder.php` L274-298 อ่าน
-- `$_FILES['imagesSlip']` ตัวเดียว → `tb_wallet_hs.imagesSlip` (varchar เดี่ยว).
-- ลูกค้าที่โอน 2 ครั้งใน legacy ต้องกดจ่าย 2 รอบ = 2 แถว 2 ยอดย่อย ซึ่งแตกกับโมเดล
-- "1 การจ่าย = 1 บิล" ที่เพิ่งทำเสร็จวันนี้ (2026-07-23 · type-1 header + children).
-- → ข้อนี้จึงเป็น **การพัฒนาต่อจาก legacy ไม่ใช่การ port** (owner เคาะเอง).
--
-- ── ทำไมเป็นคอลัมน์ใหม่ ไม่ใช่แก้ของเดิม ────────────────────────
-- `imagesslip` ถูกอ่านโดย **67 ไฟล์** (จอบัญชีตรวจสลิป · dashboard คิวสลิป · export
-- PEAK/commission/refund · cnt-hs · yuan · ฯลฯ). เปลี่ยนความหมายของมัน = พังทั้งแผง.
-- จึง **คงเดิม 100%**: `imagesslip` = สลิป "ใบหลัก/ใบแรก" (ทุก reader เดิมทำงานต่อได้
-- โดยไม่ต้องแก้อะไรเลย) · `slip_paths` = สลิป **ทุกใบ** รวมใบหลัก.
--
-- แพทเทินเดียวกับที่ใช้อยู่แล้วในระบบ 3 ที่ (ไม่ประดิษฐ์ scheme ที่ 4):
--   0229/0231 tb_forwarder_invoice : slip_path (เดี่ยว) + slip_paths (jsonb)
--   0273      momo_invoice_settlement : slip_paths (jsonb)
--
-- ── ไม่กระทบเอกสาร (ตรวจแล้วตามที่ owner ให้ดู) ─────────────────
--   ใบเสร็จ / ใบแจ้งหนี้ / ใบกำกับ  → **ไม่ฝังรูปสลิปเลย** (grep แล้ว 0 จุด) = ไม่กระทบ
--   จอบัญชีตรวจสลิป /admin/wallet/[id] → อ่าน imagesslip = ยังได้ใบหลักเหมือนเดิม
--                                        (แก้เพิ่มให้โชว์ครบทุกใบในรอบเดียวกันนี้)
--   dup-slip guard (duplicate-slip-check) → เทียบที่ "แถวหัว" (ลูกค้า+วัน+ยอด)
--                                        ไม่ได้เทียบรายรูป → พฤติกรรมไม่เปลี่ยน
--   ตรวจ 2 รอบ (0198 reviewed_at)      → ตรวจที่ "การจ่าย" ไม่ใช่รายรูป → ไม่เปลี่ยน
--
-- ── Money-safety ───────────────────────────────────────────────
-- คอลัมน์ **หลักฐาน/แสดงผล ล้วน** — ไม่ใช่ยอดเงิน ไม่ใช่สถานะ ไม่มี consumer ไหนเอา
-- ไปคิดเงิน. RPC `submit_forwarder_payment_group_atomic` (0274) **ไม่ถูกแตะ**: ยังรับ
-- p_slip_path ใบเดียวเหมือนเดิม (= ใบหลัก) แล้ว app ค่อย UPDATE slip_paths ตามหลัง
-- แบบ best-effort → ถ้า UPDATE นั้นล้ม การจ่ายยังสมบูรณ์ มีสลิป 1 ใบ (degrade ไม่ใช่พัง)
-- และ idempotency key ยัง hash จากใบหลักใบเดิม → กดซ้ำยังกันซ้ำได้เท่าเดิม.
--
-- Additive + idempotent. Safe to re-run. Next free = 0276.
-- ════════════════════════════════════════════════════════════

ALTER TABLE public.tb_wallet_hs
  ADD COLUMN IF NOT EXISTS slip_paths jsonb NOT NULL DEFAULT '[]'::jsonb;

COMMENT ON COLUMN public.tb_wallet_hs.slip_paths IS
  'สลิปทุกใบของการจ่ายครั้งนี้ (array ของ storage path · รวมใบหลัก) — owner 2026-07-23: ลูกค้าโอนหลายครั้งต่อ 1 ใบแจ้งหนี้. imagesslip = ใบหลัก/ใบแรก คงความหมายเดิมเพื่อไม่ให้ reader เดิม 67 ไฟล์พัง. หลักฐานล้วน ไม่ใช่ยอดเงิน.';
